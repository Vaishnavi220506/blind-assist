# MZ119: final authorized round, ToF-scaled causal parallax

User steering: finish one more improvement attempt, deliver it, then STOP.
This is that final round. No successor or automatic capture after this study.

EXPLORE. One complete new20episode/240frame source, seed119013, unchanged oneRGB,
finite-zone ToF/Radar/IMU sensor simulator and native capture. Source precheck:
13lateral camera/body tracks (A=.12m,y=.12sin1.1t,x=.01t),5rotation-only,
1stationary,1low-translation rotation,18static/2moving scenes. Translation stays
evaluator-only; raw camera-in-body remains[0,0,1.7], pitch-3,roll0. Body origin
follows cameraXY; current fixed-world-x corridor is not intended motion direction.
All6rod/wall episodes must contain native returned multi-actor mixtures; no refill.
Freeze source, code, scorer/audit and protocol before one600s native capture.
Then one1200s CPU replay, all frames, no tuning. Retain failure logs/evidence.

## Hypothesis and implementation

MZ118 falsely assigned near/far mixtures to wall geometry. Here the target obtains
new multi-view constraints instead of inheriting that wall plane. Translation
scale is nevertheless conditional on valid-ToF-to-feature correspondence and a
static anchor consensus; this assumption is explicitly audited, not certified.

Track up to128 Shi-Tomasi corners through actual RGB with21px/3level LK and
forward/backward error<=.5px. Maintain causal.5/.75s reference pairs and current
pixel observations; no episode-total baseline substitutes for a track's baseline.
Select at most one feature per positive singleSIM_VALID zone, nearest zone centre
and at least2px inside; eight inlier zones,2rows/2columns,>=60% consensus, rank
condition<=100. Fixed32three-anchor RANSAC samples plus all-anchor fit;1.5px
nominal reprojection criterion. No feature multiplicity boosts range independence.

Fit nominal t from(I-vv^T)t=-(I-vv^T)R(r*u). IMU supplies R, not t. LP then jointly
bounds t and all positive anchor reference/current depths. ToF working error.23m
contains.10m valid-zone span,.12m Gaussian3sigma and.01m half-quantization; it
does not prove that the tracked feature is the returned surface. Pixel working
error±.5px; relativeyaw error .2*dt+3*.08sqrt(dt/.25)degrees. Unit-ray/rotation
outer error coefficients multiply unknown positive depths in linear inequalities.
t component domain±.5m over pair; anchor current depth.02–5m. Numerical solver
settings1e-9 and padding1e-8 are not formal floating-point certification.

Require LP feasibility and exclusion of zero t before using a pair. Query up to32
oldest shared tracked features, excluding every feature used as a pose anchor in
either reference pair. Static-target constraints use positive depths up
to20m, so far background is not forced into the ToF4m domain. Intersect both
causal pair depth intervals; admit only.2<lower<upper<4m and width<=2m. This is
geometric interval intersection, not repeated-hit confidence accumulation.
Moving targets/anchors can still mimic static geometry and must be audited.

MZ116 is the unchanged baseline. Project admitted current pixels±.5px and slant
ranges with existing pitch/yaw working bounds. Candidate is incumbent OR new
possible corridor support; independent ToF/Radar and old positives cannot be
suppressed. This round seeks independently measured foreground recall/location;
it cannot claim nuisance reduction by construction. No image-only invented depth,
hidden camera displacement or native geometry enters inference. Missing RGB or
degenerate motion creates no new support and no CLEAR.

## Evaluation and stop

Serialize complete ten-distance predictions, geometry and RGB-off controls before
native evaluator access. Same1/1.4/1.8/2.2/2.6/3/3.4/3.6/3.8/4m readouts; fixed
current3.6m corridor truth. Report frames,events/firstalert,nuisance duration/count,
families,nonstress,model/point coverage and actual baseline. Native evaluator-only
audit compares t to actual camera displacement, tracked pixel slant-range bounds
to first native-AABB intersection including context, reference material consistency,
and hidden geometry failures despite retained alerts. AABB ray truth is typed
analytic geometry from native bounds, not a new depth image. Pure-rotation,
stationary,low-motion and moving-object controls are reported explicitly.

Any native geometry mismatch limits localization claims even if recall improves.
Coherent moving-anchor ambiguity remains fundamental for these observable inputs.
No physical sensor, Android, default-App or safety promotion. Deliver the measured
result and stop research attempts as requested, whether or not a breakthrough occurs.
