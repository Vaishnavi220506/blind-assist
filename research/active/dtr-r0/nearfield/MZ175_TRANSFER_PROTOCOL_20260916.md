# MZ175: unchanged transfer to consumed MZ146288

This separate EXPLORE transfer brief is written after original TRAIN192 gave
ALL92TP/46FP/4FN versus MZ12991/46/5; ANY94/47/2. The primary recovers one rod
frame and no FP. That motivates a different-scene check, not a parameter change.
The discovery protocol, source snapshot and sealed result remain unchanged.

Run the exact hashed `mz175_radar_consensus.py` once on all288 existing MZ146
frames, with its original authenticated MZ129 corrected-box cache. This cohort
was previously evaluated for other methods and is consumed Development, not a
new blind/fresh confirmation. It supplies a complete sealed baseline/proposal
container; no unbound auxiliary cache or expensive RGB replay is needed.

Same primary ALL, ANY diagnostic, eligibility, raw range, geometry and full
incumbent OR. Zero training, threshold tuning, candidate selection, capture,
RGB decode or original dev/test access. Seal all predictions before decoding
this cohort's native evaluator JSONL. Native geometry supplies truth and
returned-ToF contributor counts only after inference. Original native Radar
lineage is still NOT_EVALUABLE. Baseline must match its saved MZ146 summary.

Require at least one additional TP on this cohort, zero added FP, all old flags,
native-supported warnings and event first times retained, no family FP rise.
Report the complete counts, strata, pairs, event timing, exact changes and ANY
contrast, even if the primary fails. A failure closes the fixed consensus
recipe for general transfer; preserve the scoped TRAIN192 gain. Do not rescue
by selecting frames, changing candidate counts or excluding offending families.
A pass supports a broader consumed recall component, still not fresh/device
promotion or a false-alarm reduction. MZ129/App stay unchanged in this check.

One CPU scalar execution,120second cap, no persistent allocation. Use a new
`transfer-v1` output under the MZ175 work root. Independent saved-output audit
and scoped delivery remain authorized after either outcome. Mechanical recovery
retains failed logs without changing the frozen scientific rule or outcomes.
