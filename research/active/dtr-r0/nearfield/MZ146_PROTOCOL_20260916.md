# MZ146: unchanged MZ145 on fresh controlled scene configurations

EXPLORE confirmation, 2026-09-16. One new prospective source and one capture.
This follows the consumed Development MZ145 result and must not use that result
as fresh validation. MZ129 remains the incumbent until this comparison is scored.

## Fixed source and predictor before capture

Use unchanged `mz136_paired_source.source()` with module seed temporarily set to
146016 and restored in `finally`. Check the original generator invariants before
transformation. Rename identities from mz136 to mz146 and designate every frame,
episode-pair and scene group `confirmation`; original generator partitions remain
audit metadata only. Recheck geometry/pair invariants after transformation and
require disjoint background geometry signatures from the seed136014 source.

The source has 24 fresh scene groups, 48 six-frame episodes, 288 frames at .25s,
144 source-design positives and 144 negatives, with six groups each of suspended
HEAD, substantial BODY, near rod/far wall and shallow boundary stress. These are
new configurations drawn from unchanged family priors, renderer and sensor model;
they are not new natural scenes or physical-sensor evidence. No model outcome or
rendered appearance selects, rejects or replaces a scene. Source-design/native
geometry agreement is admission, not predictor performance or outcome selection.

Keep one 640x360 RGB camera, the original finite-footprint 8x8 ToF with at most
two regional slots, unchanged Radar and IMU. Native rays, AABBs, identities,
source partition and geometry never enter candidate inference. The old MZ136
test records remain unopened. No model training, feature revision, threshold
search, source retry or outcome-dependent repair is part of this confirmation.

Freeze MZ143's selected `fused_hgb.pkl` and feature implementation, plus MZ145's
causal rule, before capture. Model SHA256 is
`a8df5dc6c06f80057100042af48eda9b1719276e5f83140f4acca94b9cef2fb6`.
Low cutoff is .020014435971556582 and high cutoff is .2957935335969224. A strong
current score alerts immediately; otherwise current and previous scores must
both pass low. Reset at episode boundaries or non-.25s timing gaps. No future
frames, backdating or fresh-label recalibration. Persist all source/code/model
hashes in the source freeze and seal predictions before evaluator scoring.

## Confirmation criteria and honest failure

Compare matched MZ129 and unchanged MZ145 on all288 frames. Require relative FP
reduction at least30%, improved precision, recall no more than2 percentage points
below MZ129, no incumbent event lost, no additional first-alert delay over.25s,
and no increase in false-alert segments. Also report every family's TP/FP/FN,
pressure versus ordinary strata, positive and negative denominators, paired
decisions, false duration, no-alert/UNKNOWN coverage, lost incumbent true frames
and retained event times. No-alert is not clear space.

Pre-outcome clarification, recorded after capture dispatch but before any new
prediction or evaluator outcome: the joint confirmation also requires each
family's FP count not to increase and each family's recall not to decline by
more than2percentage points against MZ129. These strengthen the fixed aggregate
criteria; no source, input, model, threshold or capture setting changes. Preserve
the original source freeze and bind this protocol version in the separate
controller `acceptance-addendum.json` before prediction/scoring.

Audit all native ToF/Radar contributors separately from final alerts. Preserve
raw independently observed support, including RGB-external and unassociated
returns. A statistical alert change does not certify removal of native support,
and unchanged raw storage does not establish geometrically correct localization.
Pre-scoring availability disclosure: the inherited capture contract emits native
ToF contributor lineage, but no per-return native Radar contributor mapping.
Native contributor counts and retention are therefore ToF-only. Mark Radar
native lineage `NOT_EVALUABLE`, while retaining/counting all four public Radar
slots. This does not change the source, capture, predictor or acceptance gates.
Any criterion failure leaves this fixed recipe unconfirmed; do not tune or
replace the cohort to rescue it. Family regression and retention limits must be
visible even if aggregate criteria pass. No default App or safety promotion.

## Capture ownership and stop

Prefer the registered worker, after fresh capacity/project/plugin checks. Run
the unchanged AST of `run_mz123_frozen_early.capture`, unchanged MZ115 renderer
and sensors, under one unique native worker job. Maximum capture runtime1200s;
one capture attempt, no automatic restart. A transient poll/SSH timeout is not
terminal and must resume observation of the same handle.

All controller artifacts stay in
`artifacts.local/work/mz146-fresh-corridor-confirmation-20260916/`; worker paths
resolve through its `BLINDASSIST_ARTIFACTS`. Preserve shared UE/editor state.
Stop/release only the job-owned processes and DDC after identity/path checks.
Keep full raw capture, manifests, hashes, logs, process-release receipt and return
archive. Return the RGB/public packets/native admission evidence needed for the
controller evaluation. A dispatch acknowledgement is not completed capture.
