# MZ150: teach one observed scene to answer several physical corridor queries

EXPLORE Development, 2026-09-16. MZ149 state accumulation failed its first gate
and is closed. This changes supervision, not a temporal cutoff or persistence
parameter. MZ143's feature dimensions and fixed HGB settings are retained.

Hypothesis: training only at one query location encourages frame/context
classification. Query-conditioned supervision forces the same observed image
and sensor packet to answer different spatial questions. For every original
TRAIN frame, translate the virtual corridor center by -.45,-.30,-.15,0,.15,.30,
.45m laterally while preserving its .6m width and fixed forward/height bounds.
Do not move pixels, scene geometry or sensor returns. Express camera and return
coordinates relative to the translated query; correct inherited Radar body_y
separately because it does not use camera extrinsics. Query zero must reproduce
every original MZ143 feature bit-for-bit.

Only the TRAIN label builder accesses native AABBs and intersects them with
each translated query. Predictor input has no native geometry, source ID,
family, pair label or truth. All ToF slots including merged/outside-RGB support
remain represented. Query translations are additional supervised questions,
not new independent scenes, extra hardware, target motion or fresh observations.
Default inference always uses query zero, the actual walking corridor.

Fit exactly one HGB candidate on192*7=1,344 query examples using the MZ143 fixed
settings. Keep every view of a scene in the same original OOF fold. Calibrate
low and MZ145 high onset thresholds only from OOF predictions at query zero on
the original192 TRAIN frames. Refit once using all training queries, then seal
before dev. Compare the original frozen MZ143/MZ145 recipes; no hyperparameter
search, query-set selection or family-specific routing after scoring.

First consumed dev48: all24 incumbent TP/events without delay, at most3 FP,
at most two false segments, no family FP increase and no lost returned ToF
corridor contributor. Failure stops the recipe before MZ146 candidate scoring.
If it passes, second consumed MZ146288: at least143 TP, every MZ129 true frame
and event without delay, fewer than57 FP, at most21 false segments, no family
FP increase and returned ToF corridor retention. Include all nuisance/pressure/
pair/UNKNOWN metrics and unavailable native Radar lineage. Neither panel is
fresh evidence. A retained challenger still needs unchanged fresh confirmation.

One run,600s cap, no new capture or original MZ136 test access. CPU sklearn and
OpenCV implementation: GPU_BACKEND_UNAVAILABLE. Preserve query features,
native-label hashes, original-frame/fold mappings, models and all outcomes under
`artifacts.local/work/mz150-virtual-corridors-20260916/`. Do not expand the fixed
query set or tune a failed recipe; all processes exit after the scoped run.
