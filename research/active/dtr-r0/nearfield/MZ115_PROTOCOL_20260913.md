# MZ115 finite-footprint spatial allocation — frozen Development

## Question and authority

On the same public finite-zone ToF tuples, can RGB localize measured near-field
evidence and reduce nuisance without event recall/timing loss? The sensor change
is a hypothetical model correction, not an algorithm gain or a VL53L5CX emulator.
ST UM2884 and DELTAR motivate multi-return/distribution interfaces only. No real
hardware, natural-domain, safety or deployment conclusion is admitted.

## Frozen input and budget

One new source: seed115013,20 complete episodes×12 frames,.25s,240 total; one RGB
640×360/70deg,8×8 ToF45deg,Radar andIMU unchanged from MZ113. Families: thin rod
plus farwall, substantial offroute (>10cm clearance),HEAD,multitarget/reflectance
competition,clear ghosts,separate1cm stress. Include every frame and actual
native label; do not select sources from prediction outcomes. Source and all
capture/method helpers are hashed before capture. One owned native capture,
600s limit, existing MZ113 DDC reuse. Stop on budget/failure; no silent rerun.

ToF model:3×3 private equal angular rays/zone,4m; signal rho/(9 max(r,.2)^2),
5cm histogram/[1,2,1]/4 smoothing,600mm merging,.01 signal cutoff,strongest2,
.04m Gaussian noise,.02m quantization. SIM_MERGED means merged distinct peaks
or private contributor range span>=.10m. No subray/hitcount/object/reflectance
data in raw. Explicit latent reflectance independent of RGB texture. Legacy
center rays are evaluator-only; they never augment primary observations.
Finite quadrature may still miss thin poles; signal proxies are not calibrated
physics. Noise sigma is not a bound on mixed-surface depths.

## Candidate and fixed controls

Whole-zone possible occupancy is the primary ToF comparator. SIM_VALID slant
range interval is reported r±3sigma (probabilistic engineering assumption);
SIM_MERGED keeps the entire .02–4m sensing range and is never narrowed. Project
with interval arithmetic, pitch±.5deg,yaw±(.5+.2time)deg. Reference is initial
body direction plus integrated head rotation, not inferred walking intention.

Fixed RGB allocation: existing MZ108 proposals; complete-link forward-depth
cohorts span<=.25m; at least2 distinct zones and no duplicate zone component.
Observed positive shape weights=signal*r². Expected shape=continuous pixel
box overlap/zone area. Use only observed positive zones; absent zones never
imply free space. Coverage>=.8,cosine>=.9, winner margin>=.1. Shape is a proxy
with reflectance/box/occlusion ambiguity, not proven identity. Unresolved groups
retain whole zones. Chosen ROI=zone intersect RGB box padded2px. Never copy a
range into the entire image box. Certain-IN coarse evidence is retained.

Both primary arms share unchanged current filtered-plane RGB+Radar and .5s
flow additions (MZ111/MZ113) with an explicitly empty legacyToF adapter. No
ToF requirement for Radar pairs. Unmatched Radar and HEIGHT_UNKNOWN remain;
missingRGB causes full ToF footprint fallback. Center-point treatment of the
same valid zone tuples is diagnostic only. Also report rawRadar+coarseToF,
coarse/localized ToF-only, and RGB-disabled controls.

## Frozen evaluation

Seal all predictions and RGB-off outputs before evaluator parsing. Primary3.6m;
matched whole-pipeline distance grid1,1.4,1.8,2.2,2.6,3,3.4,3.6,3.8,4m. All use
the same fixed 3.6m current-corridor truth, width.6m,height.4–2.05m. Thus alerts
beyond truth boundary count as nuisance; no future collision benefit claimed.
Report every point, frame TP/FP/FN, event misses, first correct alert delays,
false segments/bin durations, exits, families and nonstress subset. No choosing
an operating point after outcomes as a new confirmatory result.

Audit narrowed components against evaluator-only native contributors/ROI and
real actor bounds, separately from box overlap proxies. A nuisance gain must
survive matched recall/timing and contributor audit. Otherwise freeze a negative
or inconclusive result and do not tune this source. No repeat-count confidence,
no absence-based clear output, no measured depth from RGB alone.
