# Candidate decoding diagnostic and fixed closed-query projection proposal

2026-09-22, EXPLORE. Consumed numerical implementation diagnosis, not a new
source, classifier, training run, prior expansion or parameter search. Before
this replay the existing258-public-history run exposed97 rejected candidates,
all requestedIN/status0; all97 have a valid OUT counterpart. This expected
pattern is disclosed Development. It is not an independent confirmation gate.

## Exactly one bounded diagnostic replay

Root will supply only the original258 public histories (camera and eight integer
bins per observation), once each and in original order. No actual scene ID,
geometry, evaluator label or raw actual range is passed to collect(observations).
The collector invokes unchanged path_constraint_inference.infer exactly once.
A temporary solver wrapper records its existing two calls and returns the exact
same solver result objects. It adds no solve, callback, retry, budget extension,
seed, tolerance or objective change. The wrapper is restored in a finally-backed
context and serial collection is required. Original modules/files stay unchanged.

Preserve time_limit1.0s, node_limit10000, mip_rel_gap0, presolveTrue, model domain,
contact-EPS alignment, interior-slack objective, supplied observations and frozen
conditional-decision mapping. Total516 solver calls, independent of outcomes.
Timing receipts may include negligible wrapper bookkeeping; no performance gain
is claimed. Root freezes input/source/protocol hashes and seals all diagnostic
and proposed outputs before any evaluator truth join. Do not score here yet.

## Required numerical records

For both requested labels, persist the complete raw solver vector (L,R,F,slack
and every binary), unrounded center/width geometry, original12-decimal/domain-
projected geometry, original metadata and unchanged baseline result. Report
unthresholded linear-row, variable-bound and integrality residuals. For requested
IN also retain the three exact membership-row lhs/bounds/residuals. Residuals
are diagnostic values, never new acceptance tolerances.

Validate unrounded and decoded candidates separately using the original scalar
observer and EPS-aware full-extent query. Distinguish invalid structure/nonfinite
coordinates, out-of-domain geometry, query-label mismatch, bin mismatch and
forward replay errors. Retain every mismatching view/ray/bin and decoded face
coordinates/query margins. Solver failure/limit/no candidate remains distinct.
No claimed validation cause may be inferred solely from the old generic refusal.

## Mechanically justified candidate proposal, frozen before replay

With width and thickness held fixed, nearest Euclidean center satisfying closed
corridor overlap is coordinate-wise projection onto
X in [-.30-width/2,.30+width/2] and
Z in [.30-thickness/2,3.00+thickness/2], intersected with the original center domain.
This is a deterministic geometric projection, not a fitted threshold or numerical
margin. No rounding, width change, parameter scan or optimizer follows it.

Apply this proposal only to a requestedIN candidate with original solver status0
and original NUMERICAL_CANDIDATE_REJECTED outcome. Keep every original validated
candidate unchanged. Preserve the original baseline and exclusion receipts. Save
the proposed geometry, exact displacement, detailed validation and projected_valid;
the collector does not substitute it into the baseline or emit a repaired decision.
The proposal is admissible only if it passes every original public bin and label
check. Failure remains unavailable evidence. OUT candidates are not projected.

Any later authorized conditional readout still requires its independently valid
opposite-exclusion receipt; a repaired witness cannot create that receipt. Since
all97 known rejected-IN cases already have OUT witnesses, successful repairs there
would establish explicit ambiguity rather than add classifications. A witness
pair is not a continuous-geometry impossibility theorem or sensor clearance.

## Mathematical failure fixture and evidence limits

A query-boundary feasible L can move outside the implemented EPS query when
floating L/R values are decoded into center/width and those coordinates are rounded
independently. Example: L=.30+1e-12, R=nextafter(.8,+infinity). The raw LP lateral
bound holds, while reconstructing L from center/width can exceed it by an ULP;
12-decimal rounding can increase the discrepancy. A focused fixture reproduces
label-only rejection and a sub-2e-12m closed-query projection that preserves all
fixture bins. This establishes a possible mechanical cause before the replay,
not the measured cause of all97cases. Collected vectors must determine that.

Five focused tests cover capture/restoration/exact call budget, unchanged baseline,
query-vs-bin-vs-domain failure categories, the floating boundary fixture, and a
projection that changes bins and must fail validation. These are mechanical
fixtures, not a new scientific cohort. Root owns the single258-query replay and
subsequent evidence interpretation; no source truth is needed by this collector.
