# MZ175: conditional Radar task consensus recovers two consumed warning frames

EXPLORE,2026-09-16. [Discovery protocol](MZ175_PROTOCOL_20260916.md) and
[separate unchanged transfer](MZ175_TRANSFER_PROTOCOL_20260916.md).

**Retain a scoped COMPONENT_OR_CHALLENGER / COMPONENT:** original TRAIN192
improves91TP/46FP/5FN to92/46/4; consumed MZ146288 improves130/86/14 to131/86/13.
Both checks add one true frame and zero false frames while preserving every
incumbent warning. One HEAD event starts0.25s earlier. This is a small recall
component, not the requested broad breakthrough or reduced false-warning rate.
MZ129 remains the research baseline; application defaults are unchanged.

The transfer cohort was previously used by other experiments. Neither result
is fresh confirmation, natural-distribution, physical-sensor or device evidence.
The next decision for this exact component requires unchanged prospective
confirmation, not tuning on these outcomes. The broad improvement goal remains
active. Structured registration/inheritance status is recorded below.

## Final alerts

TP/FP/FN/TN are frame counts. No-alert is UNKNOWN, not evidence of clear space.

| Cohort / arm | TP / FP / FN / TN | Precision | Recall | UNKNOWN |
| --- | ---: | ---: | ---: | ---: |
| TRAIN192 MZ129 | 91 /46 /5 /50 | 66.42% | 94.79% | 55 |
| TRAIN192 ANY | 94 /47 /2 /49 | 66.67% | 97.92% | 51 |
| TRAIN192 ALL | 92 /46 /4 /50 | 66.67% | 95.83% | 54 |
| MZ146288 MZ129 | 130 /86 /14 /58 | 60.19% | 90.28% | 72 |
| MZ146288 ANY | 133 /91 /11 /53 | 59.38% | 92.36% | 64 |
| MZ146288 ALL | 131 /86 /13 /58 | 60.37% | 90.97% | 71 |

ALL and ANY differ on3 TRAIN and7 transfer frames. Relative to ALL, ANY
recovers two additional true frames at a cost of one added false frame on
TRAIN, and two additional true frames at a cost of five added false frames on
transfer. Thus the unanimity restriction has an observed nuisance/recall
tradeoff; the favorable ALL result is not a proof of correct object identity.
The predeclared primary remains ALL; no arm is selected after scoring.

| Cohort / stratum | MZ129 TP / FP / FN | ALL TP / FP / FN |
| --- | ---: | ---: |
| TRAIN ordinary144 | 69 /26 /3 | 70 /26 /2 |
| TRAIN boundary pressure48 | 22 /20 /2 | 22 /20 /2 |
| Transfer ordinary216 | 103 /58 /5 | 104 /58 /4 |
| Transfer boundary pressure72 | 27 /28 /9 | 27 /28 /9 |

Only rod TRAIN changes22/9/2 to23/9/1, and HEAD transfer changes33/11/3 to34/11/2.
BODY and boundary counts are unchanged on both cohorts. Every family's FP
count is unchanged. Paired binary both-correct decisions improve46to47/96 and
47to48/144. These are correlated controlled frames, not independent trials.

All20/20 TRAIN events remain detected with identical first times. Transfer
retains29/30 detected events; the already-missed boundary event stays missed.
`mz146_suspended_head_scene4_in` starts at0.00s instead of0.25s. Every other
detected first time is unchanged. False segments remain13 and28; sampled false
4Hz bins remain11.50s and21.50s, not measured user interruption time.

## Exact gains and inference mechanism

- `mz136_near_rod_farwall_scene2_in_03`: current Radar slot0 at1.90m has two
  eligible boxes, both supporting the plane proxy. Its raw center is outside
  the corridor; the full baseline was nonalerting. Another return at3.75m has
  three non-supporting candidates and adds nothing.
- `mz146_suspended_head_scene4_in_00`: slot0 at1.95m has two eligible boxes,
  both supporting the proxy. Its raw center is also outside. Adding this frame
  advances that HEAD event's first warning by0.25s.

Neither recovered frame has a returned native-ToF corridor contributor in the
saved audit. The gains therefore do not recover suppressed ToF witness votes.
Native Radar contributor ownership is NOT_EVALUABLE; do not claim that either
specific Radar return has proven correct actor correspondence.

MZ111/MZ129 use RGB extent only when current association is unique and
reciprocally exclusive. MZ114 seeks agreement on identity across assignments.
This new branch instead asks whether all eligible visible identities agree on
the task decision. It handles only unresolved returns with at least two boxes,
using the unchanged12-degree eligibility margin, current raw range and
`plane_inside`. It never changes uniquely associated/filtered current returns.
The complete existing baseline is ORed with the additional branch from the
outset. This preserves existing independent ToF/Radar/guard/carry warnings and
necessarily cannot reduce baseline FP.

TRAIN has124 eligible ambiguous returns and transfer178. ALL supports6 and14
frames, respectively; most are already alerting. ANY supports29 and58.
All candidate boxes and their decisions remain in the prediction evidence.
No native labels, family, future frame or identity is passed into the predictor.

The plane function checks a frontoparallel corner AABB's corridor overlap.
It is not an exact physical surface/height guarantee. Visible candidate sets
do not exhaust null, ghost, unseen or outside-RGB explanations. Accordingly
the retained result is conditional statistical behavior on these two cohorts,
not proof that ambiguity has been resolved. UNKNOWN remains unchanged elsewhere.

## Retention, verification and execution

The checks separately verify original input immutability, every old final flag
retained, and no newly nonalerting native-supported frame. Across TRAIN and
transfer, retained native audits cover16,537/27,872 returned contributor samples,
including3,193/4,751 corridor samples. Both baseline and ALL have zero nonalerts
containing those corridor samples. This is sampled ToF warning retention, not
complete physical surface coverage or native Radar verification.

Six synthetic tests pass on their first run in0.026s, including real synthetic
geometry, missing evidence, empty/singleton handling, raw-versus-filtered
isolation, mixed decisions, permutation and incumbent preservation. Runner
parser/metric/event fixtures also pass. Initial `run-v1` stops before prediction
because cached corrected records lack embedded IDs. Mechanical recovery joins
their sealed positional order to public raw ID text and checks count/order;
`run-v2` then completes. No rule or outcome was changed, and failed logs remain.

One completed scientific discovery execution takes2.058s, including0.101s for
192 additive readouts. The separately frozen transfer completes once in5.847s,
including0.133s for288 readouts. CPU is appropriate for the small irregular
Boolean/NumPy operation. These timings exclude original RGB proposal extraction
and baseline inference and are not Android end-to-end latency.

Discovery decodes only admitted TRAIN objects from mixed containers and reuses
MZ173 saved labels after sealing. Transfer uses all288 consumed MZ146 public
rows/cached proposals, then decodes that cohort's native annotations only after
its prediction seal. No original dev/test, RGB decode, new capture or model fit.
All processes exit; durable source snapshots, predictions, annotations, parent
bindings and logs remain under `artifacts.local/work/mz175-radar-consensus-20260916/`.

Independent saved-output audit passes both panels:242 hash checks,480 frames
and1,047 saved proposal hypotheses; Boolean reductions, all confusion/family/
stratum/pair/event reports and native-warning counts agree. This does not rerun
RGB geometry or independently establish Radar ownership. The first audit attempt
fails on omitted zero-valued Counter keys; only the audit is corrected after
checking their producer semantics, with original script/log/failure retained.
Scientific sources and predictions are unchanged. The old MZ146 native cache
was not bound by its historical completion seal; this audit matches its288
records to the newly sealed transfer annotations and hashes both, without
independently decoding native lineage again. The transfer producer itself
authenticates and decodes the original native source after prediction sealing.

Supported registration exits2 at the existing `experiments/index.jsonl:303`
input-fingerprint mismatch; inheritance exits2 because the terminal is unknown.
Both remain PENDING with intended COMPONENT_OR_CHALLENGER / COMPONENT role.
Ledger/inheritance hashes are unchanged. No manual append or bypass was used.
