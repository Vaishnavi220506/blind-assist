# MZ152: contained-return anchors leave the limiting score supports unchanged

EXPLORE Development, 2026-09-16. [Protocol](MZ152_PROTOCOL_20260916.md).
Decision: `CONTAINED_RETURN_DEV_GATE_NOT_MET`.

**The additive anchor covers 106 true TRAIN frames with zero false frames,
but neither frame that fixes the learned cutoffs. Both cutoffs and all 48 dev
decisions therefore remain identical to MZ151: 24 TP / 8 FP / 0 FN and five
false-alert segments.** Close this exact anchor-plus-readout recipe as a
negative control. This result does not establish a fundamental observability
limit or reject other independent sensor evidence.

## Fixed mechanism and evidence

The branch uses the original MZ143 three-sigma full-zone ToF envelopes from
cached public features. It requires valid IMU and at least one usable,
unmerged slot whose complete x/y/z envelope lies inside the fixed corridor.
It does not inspect RGB visibility or evaluator geometry, contract support,
remove sensor inputs, or veto another alert. The final decision is the anchor
OR the learned causal alert. Absence of an anchor remains UNKNOWN.

The learned model and scores are the frozen MZ151 outputs. TRAIN480 contains
original MZ136 TRAIN192 plus the already-consumed MZ146288. MZ146 is training
evidence here, not validation. No new model inference, feature extraction, model
training, original-test decoding, source capture or threshold search occurred.

The anchor passes its TRAIN admission check: 106 TP / 0 FP, covering 106
incumbent true frames. It covers 106 of 240 true TRAIN frames overall. These
branch-coverage counts do not describe the final system's recall.

| Cutoff support | TRAIN frame | Saved OOF score | Anchor |
| --- | --- | ---: | --- |
| Low | `mz146_shallow_boundary_stress_scene3_exit_02` | 0.00829862256322582 | absent |
| High onset | `mz146_suspended_head_scene3_in_00` | 0.032424448336891454 | absent |

The low-support frame has a preceding score of 0.8888424315229055. The high
support is an episode start with no predecessor. After anchored true frames
are removed from the learned branch's retention obligations, both of these
constraints remain. The TRAIN-derived low and high cutoffs stay exactly equal
to MZ151. This is the direct mechanism of the unchanged final result.

## Consumed dev48 results

| Arm | TP | FP | FN | False segments |
| --- | ---: | ---: | ---: | ---: |
| MZ129 | 24 | 13 | 0 | 4 |
| Frozen MZ151 single-score threshold | 24 | 12 | 0 | 7 |
| Frozen MZ151 learned causal branch | 24 | 8 | 0 | 5 |
| Contained-envelope anchor alone | 14 | 0 | 10 | 0 |
| MZ152 anchor OR learned causal branch | 24 | 8 | 0 | 5 |

All 14 dev anchor alerts were already emitted by the learned branch, so the
OR changes zero frames. Anchor true coverage is BODY6, HEAD5, rod3 and
boundary0. Anchor-only misses are branch-coverage limits, not additional misses
in the combined result.

| Family | MZ129 TP/FP/FN | MZ152 TP/FP/FN |
| --- | --- | --- |
| BODY | 6/1/0 | 6/1/0 |
| HEAD | 6/0/0 | 6/1/0 |
| Rod plus far wall | 6/6/0 | 6/2/0 |
| Boundary pressure | 6/6/0 | 6/4/0 |

Ordinary frames are 18/4/0 versus MZ12918/7/0; boundary-pressure frames are
6/4/0 versus6/6/0. All24 incumbent true frames and all5 exact first-alert times
are retained. Precision is75%, recall100%, and16 non-alert frames remain
UNKNOWN. False sample-bin duration is2.0s versus3.25s at4Hz; this is not a
measured interruption duration.

Relative to MZ129, seven false frames are removed and two are added:
`mz136_suspended_head_scene4_out_00` and
`mz136_substantial_body_scene4_out_03`. None of these changes is new relative
to MZ151. Both decisions are correct in16/24 pairs; underlying learned-score
ordering stays23/24. The binary OR ordering includes ties and is not a new
continuous ranking result.

The declared FP<=3, false-segment<=2 and family-FP nonincrease requirements
fail. TP retention, event timing and returned-ToF contributor retention pass.
No fresh comparison is admitted by this failed dev gate.

## Native evidence and verification

All128 original ToF slots remain represented. The independent audit recomputes
4,187 returned ToF ray contributor samples, including1,046 in the corridor,
with no non-alert frame containing a returned corridor contributor. This does
not certify unsampled surface coverage. Native per-return Radar lineage stays
`NOT_EVALUABLE`; unchanged public Radar inputs are not evidence of known
native return ownership. Modeled three-sigma containment is not a hardware
guarantee.

The audit independently reconstructs all480 anchor decisions and slot masks,
recomputes both threshold-support cases, verifies exact MZ151 dev identity,
checks48 causal prefixes and114 hashes, and recomputes frame/family/event and
native-contributor results. It uses cached features and saved scores only.
The valid run takes4.1456s under the120s cap on CPU scalar NumPy work
(`TASK_NOT_GPU_SUITABLE`), with no persistent allocation.
Two focused tests pass: complete-envelope/status/IMU requirements without RGB,
and additive anchoring without suppressing a learned alert.

`run-v1` stopped during source-input hashing because a string path was passed
to a Path-based hash helper. It produced only a source snapshot, before anchor
calculation, calibration or dev scoring. The sole runner correction wraps that
path with `Path`; the method and protocol bytes are unchanged. `run-v2` is the
completed replacement, and `mechanical-recovery.json` preserves the distinction.

Evidence is under `artifacts.local/work/mz152-contained-return-20260916/`:
`run-v2/` holds anchors, parameter/prediction seals and results; `review/` holds
the independent audit; the failed snapshot and recovery receipt remain intact.

Intended terminal: `terminal-dtr-mz152-contained-return-no-gain`, role
`NEGATIVE_CONTROL` for this exact contained-envelope anchor plus frozen MZ151
readout. Supported registration and inheritance receipts are retained in
`decision.json`, `registration-final.log` and `inheritance-final.log`. Registration
returns the existing ledger303 input-fingerprint mismatch; inheritance returns
unknown terminal. Both commands exit2 and both ledger hashes remain unchanged.
Technical completion does not bypass this metadata failure.
