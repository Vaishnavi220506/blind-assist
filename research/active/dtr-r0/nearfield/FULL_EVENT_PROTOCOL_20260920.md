# One frozen 24-sequence complete-event transfer

2026-09-20. User authorizes the suggested one-shot complete-sequence simulation.
New EXPLORE controlled arrangement evidence; not protected final, hardware,
natural-distribution, audio usability or product evidence.

## Question and two fixed arms

Does the prior added-return information benefit survive new layouts and become
better complete alerts, including exit costs? Compare only original strongest
return plus original hold (`strongest_hold`) against nearest of the two exported
returns plus the identical hold (`closest_hold`). The previous two-return OR
arm matched nearest everywhere; no extra readout, fit or training is justified.

Freeze the prior `simulate_two` from multireturn_pilot_20260920.py without any
change to its second-bin detectability assumptions, noise or dropout. Keep T=
0.4071309640537889, 45x45 nominal 64-zone geometry, full support integration,
definite-support bypass, timestamp interval .2 s and one genuine-strong-backed
nonrecursive adjacent-frame hold. Held decisions never refresh persistence.
Never union separated ranges into a solid interval. Silence remains UNKNOWN,
not free space; public inference uses only exported ranges and stream identity.
Record raw current decisions only for causal attribution, not another candidate.

## Fixed source and execution budget

24 sequences = four BODY/HEAD cuboid types x INSIDE/BOUNDARY/OUTSIDE x two
backgrounds; 24 frames each, total 576. New dimensions/relative configurations
are checked against all three earlier Core cohorts before capture. Use the same
authenticated Willow map, renderer, assets and 640x360 camera. Shared object
families and hypothetical sensor physics are explicitly retained dependencies.
Lateral comparisons hold type dimensions/material/approach fixed; changing
background holds target-relative geometry fixed. No outcome-dependent selection.

Per clip: approach frames0..8, dwell9..13, depart14..23, nominal .2 s apart.
Departure is backing away along the camera optical axis; this is a posed
quasi-static sequence, not a physically moving human, passing an obstacle,
real-time dynamics or sensor timing-jitter experiment. It supplies at least five
post-exit negative samples per positive clip. No audio engine is implemented.

One full capture, 3600 s launcher budget. Known existing runtime/configuration
is reused. Launcher owns only its new hidden offscreen UE process tree; release
in finally and preserve receipt. Never stop a shared editor. Frozen source or
mechanical capture failure is NOT_EVALUABLE; diagnose and repair only that
mechanical failure with explicit receipts and unchanged scientific inputs. No
new scene/seed, censoring, best-run selection or expanded scientific budget.

Freeze spec/protocol/code/dependency hashes before capture. Audit actual geometry,
readiness, asset hashes and task release. Generate the paired observations from
native depth without labels/ownership, and keep native lineage evaluator-only.
Seal observations; run causal prediction without reference geometry; seal all
predictions before label/ownership join. Rendering uses the UE configured graphics
device; scalar ToF geometry and metrics use CPU (TASK_NOT_GPU_SUITABLE).

## Whole-timeline metrics and prospective interpretation

Core = whole INSIDE + OUTSIDE clips (384 frames), Boundary = all BOUNDARY clips
(192). Do not reassign near-entry samples to Boundary after results. Report
TP/FP/FN, precision/recall/FPR/F1, false segments/sample duration, UNKNOWN,
BODY/HEAD/background strata, complete paired change IDs and event counts.

An event is one contiguous truth interval. Report first in-event alert delay,
whole-clip first alert and pre-entry FP separately; no retrospective early-warning
credit. Split missed positive samples into initial silence, internal gaps and
terminal silence. Report event coverage, repeated alert starts (a proxy only;
not actual voice repetitions), and all event end times.

Exit time is the first negative sample after a true event. If the last positive
was alerting, its contiguous alert prefix after exit is carryover: one sample=
.2 s. An alert beginning only after a silent final positive is a new false
episode, not carryover. Separately report first silence after exit in both cases:
immediate silence=0; if no silence is seen mark right-censored, never silently
replace with zero. Count later false
reactivations and all post-exit FP separately, including clips with no exit alert.
OUTSIDE clips have no truth event; count their entire false-alert burden instead
of assigning artificial exit times.

Pass for a retained complete-Core challenger requires, against the paired old arm:
1. Nonempty eight-event Core comparison, and at least one native-trigger-backed
   new current positive alert, earlier in-event onset, or fewer internal gaps.
2. No lost Core positive sample or detected event and no delayed first in-event
   alert for any formerly detected Core event.
3. No increased Core FP count, false-alert segments or sampled duration; report
   pre-entry, post-exit, and OUTSIDE costs separately even if aggregate passes.
4. No increased per-Core-event internal interruption count, carryover delay or
   time to first silence after exit;
   both arms' Core exits must be observed uncensored.

These are comparison criteria, not human safety limits. Boundary remains a
separate cost/coverage report; improvements there cannot rescue a Core failure.
Native support must belong to the actual selected return's triggering zone;
held-only recovery is continuity and never newly observed sensing evidence.

If all checks pass, retain scoped controlled-event CHALLENGER, preserving full
task Calibration and the stable 74/10/0 demo. If costs grow or onset/support is
lost, retain only any evidenced information component and close this fixed
complete-event upgrade role. If identical, report no demonstrated incremental
gain. Either outcome ends this experiment: no threshold/return-law/timing tuning,
new cohort, training, default runtime changes or automatic successor.
