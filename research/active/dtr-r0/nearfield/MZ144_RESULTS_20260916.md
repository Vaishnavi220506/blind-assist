# MZ144: direct interval-consensus margin does not improve alerts

EXPLORE, 2026-09-16. [Fixed protocol](MZ144_PROTOCOL_20260916.md).
Decision: `MZ144_DIRECT_MARGIN_NO_JOINT_DEV_GAIN`; keep MZ129.

**One direct geometry readout gives 24 TP / 17 FP / 0 FN versus MZ129
24 / 13 / 0 on consumed dev48.** All five first-alert times stay unchanged,
but false segments increase 4 to 5 and BODY FP increases 1 to 5. Rod and
boundary FP remain 6 each; HEAD FP remains 0. Stop this exact readout.

The implementation uses every VALID slot, at least two distinct supporting
zones, ROI-restricted slant-range intervals and regional mean-range correction
for a uniform-reflectance body-forward plane. It retains alternative depth
modes and their conflicting corridor signs. Fine lateral edges replace their
own seed while retaining its full vertical extent. It does not read native
surface/ray identities. The selected physical plane is still a hypothesis.

TRAIN-only cutoff is -0.24928200952306634 m. At that cutoff TRAIN reaches
92/60/4 and retains incumbent true frames; these are not perfect training
predictions. The negative cutoff demonstrates that some required TRAIN frames
need roughly 25 cm expansion of the proposed visible-plane corridor margin.
It is not a geometrically calibrated safety margin.

Geometry is unavailable in 62/192 TRAIN and 23/48 dev frames. Independent frozen
Radar/raw-certain/outside-RGB branches already alert on 37/48 dev frames,
limiting this integration's ability to reduce nuisance. The new visual margin
adds four BODY false frames and removes no baseline false frame. This recalls
MZ137's residual-vote limitation and is not evidence against every geometric
representation or against the MZ143 fused representation.

Ordinary frames: 18/11/0 versus baseline18/7/0. Boundary-pressure frames stay
6/6/0. Seven of24 pairs have both decisions correct. Combined scores order
11/24 pairs strictly, with independent-branch ties explicitly limiting ranking.
Seven non-alert frames remain UNKNOWN. No raw packet/slot is deleted;
`native-contributors.json` retains all per-frame contributor accounting.

Five focused geometric tests pass. The full run takes26.34s on actual CPU:
18.84s TRAIN and4.77s dev prediction/serialization. No new model, capture,
original-test scoring, GPU worker or persistent process is created.

Evidence: `artifacts.local/work/mz144-corridor-margin-20260916/run-v1/` with
protocol/code snapshot, TRAIN cutoff seal, dev prediction/decision seals,
candidate depth modes, raw-slot audits, contributor report and completion hash.
Intended terminal `terminal-dtr-mz144-direct-margin-no-joint-gain`, role
`NEGATIVE_CONTROL`, scoped to this plane-consensus plus independent-vote readout.
Supported registration failed at existing ledger303 input-fingerprint mismatch;
inheritance reports unknown terminal. Final logs and decision.json preserve
both attempts and unchanged ledger hashes. No manual ledger mutation.
