# MZ142: fixed pretrained BatchNorm statistics from training initialization

EXPLORE,2026-09-15, user-authorized one-round correction of MZ141. Start both
arms from the same ORIGINAL official DEPTHOR Small weights, never MZ141 trained
weights. MZ140 direct transfer and MZ141's batch-statistics recipe remain stopped.
Only scientific intervention from MZ141: train and infer with the checkpoint's
fixed BN running_mean/running_var/num_batches_tracked. BN affine weight/bias
remain in get_lr_params and trainable. Call model.train(), restore DA.eval(),
then BN.eval() at initialization and after every monitoring evaluation. Leave
track_running_stats=True and momentum unchanged. DropPath retains original
training behavior and is disabled normally during inference; do not hold the
entire network in eval during gradient updates.

The [MZ141 protocol](MZ141_PROTOCOL_20260915.md) supplies all unchanged inputs,
labels, losses, optimizer, schedule and geometry criteria. Same public MZ140
regional-center adapter, full RGB, source checkpoint, DA freezing,5,420,458
trainable parameters, seed141, Adam1e-4,144fit/48scene-heldout frames,8epochs,
batch1 with4gradient accumulations,288updates EACH. Pixel and balanced losses
are reused verbatim. No sparse writeback, model swap, normalization recalibration,
augmentation, learning-rate/loss-weight sweep, checkpoint selection or extension.
Even if intermediate metrics improve, use epoch8 only. Abort on nonfinite
output/gradient, preserve consumed steps/checkpoints; no automatic restart.

## Baselines, monitoring and raw outputs

Reuse hashed MZ141 frozen pretrained predictions on the same192TRAIN frames;
do not rerun an unchanged baseline. Preserve MZ141 final trained arms as the
negative control for the complete former recipe. Load no old trained weights
into new initialization. Compare final FIT/heldout metrics with both frozen
pretrained and the recorded corresponding old training arm.

After each epoch evaluate a fixed12FIT monitoring frames: the third lexical
frame of each of12fit scene_group values, selected before training. Record
per-family target MAE, target pixel/half-column coverage, known8px ring error
and whole known non-target over-near rate, with original denominators. Monitoring
uses ordinary eval mode and no gradients; record unchanged model-state/BN hashes
and preserve Torch/CUDA RNG so it cannot change DropPath's subsequent sequence.
These12frames are monitoring only, not the144frame FIT result or a stopping/
selection rule. End-of-budget evaluation covers all144FIT and48heldout frames.

For every training forward, monitor forward and final new-arm prediction,
record minimum/maximum, finite count, counts and fraction below.001m/above10m
at native network final output480x640 BEFORE resampling or clipping. Use the
same unclipped-resampled training tensor and clip-before-resampling predictor
as MZ140/141. Raw-range diagnostics do not silently change loss or admission.

## Acceptance and stop

Reuse MZ141 per-family thresholds and raw metrics verbatim for final heldout
admission and simple-pixel preference. Also report the same checks separately
on complete FIT144 to distinguish fitting failure from transfer failure; these
fit checks do not replace heldout admission. If FIT still lacks thin geometry,
pause this fixed-iteration DEPTHOR fine-tuning recipe without more training or
weight changes. A retained geometric component requires useful FIT AND heldout
geometry; if only FIT passes, report a transfer failure. A BN execution fix
is not itself thin-obstacle recovery, an alert gain or a novel loss mechanism.
If both geometry stages admit, finish the previously authorized single frozen
consumed-dev alert comparison, preserving MZ129 native residual evidence and
the24TP,FP<=10,5/5events,.25s timing criteria. Otherwise no dev alert integration.

## Evidence and lifecycle

Outputs: artifacts.local/work/mz142-depthor-fixed-bn-20260915/run-v1/ through
the canonical F: junction. Source/input/protocol snapshots precede gradients.
TRAIN native AABBs are explicit fit labels. Model forward accepts only public
RGB/ToF tensors, never masks/owners/labels. Unknown reference stays masked.
Heldout evaluator.jsonl rows are decoded only after new predictions seal;
already consumed MZ141 outcome authority is not restored to freshness. The
inherited spec.json contains configured geometry but only partition metadata
is used from it. No original test or newly captured samples.

Check actual BN buffers bitwise against original initialization throughout,
affine gradients and final affine updates, same initial full-state hashes,
same original MZ141 sample orders, actual GPU/backend and timings. Reuse the
already verified unfold/autograd operation; do not repeat unchanged operator
checks without a change. Save epoch checkpoints with optimizer/RNG, final maps,
monitoring/raw logs, terminal and any failure receipt. No paid/remote allocation.
Process-local tensors release on exit; durable outputs remain for reproduction.
No automatic resume; any interruption requires remaining-budget identity audit.
Use supported registration/inheritance tools; existing ledger errors remain
pending metadata, never manual ledger edits or bypasses.

Source contract checked against [PyTorch2.11 BatchNorm2d](https://docs.pytorch.org/docs/2.11/generated/torch.nn.BatchNorm2d.html)
and [autograd evaluation-mode notes](https://docs.pytorch.org/docs/2.11/notes/autograd.html#evaluation-mode-nn-module-eval).
