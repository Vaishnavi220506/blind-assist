# Nominal 45x45 degree ToF contrast on fixed96 scenes

2026-09-20, EXPLORE consumed controlled simulation. COMPLETE.

**Nominal-FOV geometry changes the input opportunity: the center cylinder and
thin plate gain foreground-owned near returns. The unchanged raw readout gains
two interior events but raises false alerts from5 to19 frames.** Retain the
geometry-sensitive observation finding; do not promote the readout as a better
overall detector or treat this as real VL53 performance.

## Fixed comparison results

The candidate's discrete bin boundaries yield44.5544 degrees horizontally and
44.7826 vertically, the nearest fixed-lattice approximation to nominal45x45.
Every old event and baseline metric replays exactly. No new model prediction or
training was performed; the candidate below is the unchanged raw-ToF readout
applied to the newly simulated scalar vectors.

| Same96-frame metric | Old wide proxy | Nominal45x45 proxy |
| --- | ---: | ---: |
| Interior events detected | 2/5 | 4/5 |
| All events detected, including boundary-only | 3/6 | 5/6 |
| TP / FP / FN | 12 / 5 / 24 | 30 / 19 / 6 |
| Recall | 33.33% | 83.33% |
| Precision | 70.59% | 61.22% |
| FPR over all60 reference-negative frames | 8.33% | 31.67% |
| False-alert segments / sampled duration | 2 / 1.0s | 6 / 3.8s |
| Prediction-UNKNOWN frames | 96 | 86 |
| Ambiguous alert frames | 17 | 39 |

TN remains0 in both arms:55 old and41 new reference-negative non-alert frames
are abstentions/UNKNOWN, not established true negatives. FPR includes the full60
known-negative denominator. The candidate's10 nonambiguous supported alerts are
not an independently native-validated safety claim. Pixel IoU is not defined for
these scalar event alerts. Original NFO and Depth Pro results are not re-evaluated.

| Target event | Pure observed <3m anchor frames, old -> new | First new near anchor relative to entry | Interior detection, old -> new |
| --- | ---: | ---: | --- |
| s00: 4cm center cylinder | 0 -> 5 | +0.2s | missed -> detected |
| s02: horizontal bar | 5 -> 6 | 0.0s | detected -> detected |
| s03: boundary-contact bar | 6 -> 6 | 0.0s | boundary-only, no interior event |
| s04: broad plate | 6 -> 6 | -0.2s within clip | detected -> detected |
| s05: 4cm-wide thin plate | 0 -> 5 | +0.2s | missed -> detected |
| s06: small same-zone object | 0 -> 0 | unavailable | missed -> missed |

Counts are over original event frames. The broad plate's negative relative time
uses the complete clip; its first event-frame anchor is at entry. Cylinder/plate
near anchors occur in frames7..11 (1.4..2.2s), with entry at1.2s. Both already
raise candidate alerts at1.2s under the unchanged interval-overlap readout;
an alert is not the same as a point return<3m. The horizontal bar's first event
alert advances from+0.2s to0.0s; pre-entry alerts remain separately recorded in
the event metadata and must not be called correct onset discrimination.

The false-alert frame totals by clip are s01:1->7, s02:0->4, s03:0->3,
s04:4->4, s05:0->1, and0->0 for s00/s06/s07. Thus the benefit does not satisfy
an unchanged-error comparison; no threshold adjustment was attempted.

Independent saved-result recount passes with no differences. All19 candidate
false-alert frames are ambiguous and UNKNOWN. Across them, all48 possible
trigger-zone instances have pure target-footprint contributors; none is mixed
or non-target, and none supplies definite corridor support. Of these48 returns,
34 are>=3m and14 are<3m. These describe every potential trigger zone, not a
unique causal attribution. Correct target ownership alone therefore does not
resolve these errors; depth/lateral corridor intersection remains uncertain.
The receipt verifies9 frozen code hashes,8 input hashes,96 new vectors and all
observation/prediction seal bindings without rerunning simulation or scoring.

## Coverage and interpretation

The sensor footprint shrinks from31,416 to10,384 of49,152 sampled image pixels,
losing21,032 previously covered pixels (about67% of the old footprint). Both
layouts cover all13,668 sampled target pixels and all6,848 sampled reference
corridor-surface pixels across this controlled cohort. This favorable target
placement does not establish acceptable coverage while turning, at shorter
distances, or for arbitrary obstacles. Every excluded ray remains unobserved.

The previous cross-zone zero was conditional on its wide proxy. It cannot be
generalized to all64-zone hardware layouts: two formerly return-free target
events now have owned near returns. However, smaller zones still leave the
small same-zone object without one, and the unchanged interval-based decision
has more false alerts. A later bounded lightweight association study may use
this geometry as a disclosed simulation component; no training, association
implementation, offset/FOV sweep or successor was started here.

## Frozen protocol

Question: does narrower nominal sensor geometry make the existing thin targets
produce foreground-owned returns, and what false alerts, timing and coverage
loss accompany it? The preceding cross-zone audit found no target return for
three fixed missed objects under the generic wide proxy. That zero does not
establish the behavior of a physically different angular layout.

Use all96 original frames/eight clips from ba-camera-corridor-20260919, unchanged
native depth, camera, scene, timestamps, labels and event/boundary definitions.
The old scalar simulator deliberately used the central80% of a100-degree
horizontal camera, approximately87x56 degrees. It was not a calibrated VL53
simulator. The [ST DS14161](https://www.st.com/content/st_com/en/technical-documents/DS14161.html)
specifies nominal45x45-degree detection volume (65-degree diagonal).

Change only the sensor bin geometry: centered nominal45x45 degrees, still8x8
rectangular zones and one scalar per zone. Project uniform image-plane edges
through the existing camera calibration, quantize to the same256x192 depth
sample lattice, and report actual quantized coverage. No resize, interpolation,
extra native samples, synthetic object enlargement, angle search or offset search.
The budget is one candidate layout over96 frames. Retain the old0.1-8m optical-Z
range, inverse-square energy10cm winning bin,5% dropout and Gaussian noise,
including original seed identities and conditional RNG algorithm. A different
valid-hit pattern can change later RNG draws; same seed is not identical
per-zone noise. This isolates the declared proxy layout, not photon counts or
the complete hardware specification. Real range, zone overlap, PSF, reflectance,
optical alignment, ambient light and weak-peak detection remain uncalibrated.

Native depth enters only the explicitly declared observation simulator. Seal all
96 newly generated scalar vectors, then produce and seal predictions from only
zone bounds and scalar values with the unchanged raw-ToF corridor readout. Only
then open target footprints for contributor scoring. Do not rerun NFO trained on
the old geometry or any large model. Target ownership uses the authenticated
original native-bounds +/-2cm footprint, not exact instance segmentation.

Read out all-known/interior/boundary TP/FP/FN/TN, recall, precision, FPR, UNKNOWN,
ambiguous alerts, events, false segments/duration and first-alert timing. Report
pure/mixed observed target returns<3m and first available times separately from
alerts. Compare the center cylinder, thin plate and small same-zone object,
without dropping the remaining clips. Record total angular/image coverage and
sampled target/reference-corridor coverage lost. Outside-FOV is unobserved, never
a negative/free-space certificate. Pixel IoU does not apply to the scalar alert.

An owned foreground return appearing supports geometry sensitivity in this
simulation only. Require the corresponding event/error/coverage readout before
claiming practical benefit. No return improvement preserves the lack-of-input
finding for this candidate. Stop after this single contrast and deliver the
evidence regardless of outcome; no training, threshold/layout sweep or successor.

Payload: `artifacts.local/work/ba-tof-fov45-20260920/`. Protocol/code/input hashes
freeze before materialization. Synthetic parity/geometry checks run first.
Implementation: [runner](run_tof_fov45.py), [geometry and proxy](tof_fov45_core.py).

## Execution and disposition

The protocol froze at2026-09-19T16:59:48Z (2026-09-20 local), before candidate
materialization: SHA256
`9adc3ca1a1d98216a8732640faa6b7f66e8e37171d002d966ee1a1999e64b72c`.
Nine synthetic checks passed, including old-proxy float32/float64 parity,
unchanged sampling, bin ownership and dropout exclusion. One materialization,
one sealed prediction pass and one evaluation completed without retries.
Measured CPU stage times are2.072s,0.350s and2.435s respectively; these are
desktop experiment timings, not edge-device inference benchmarks.

Retain protocol-before-run.md, protocol.json,96 observation vectors, private
lineage, observation/prediction seals, predictions.json, frame-results.json,
results.json, tests/backend/run logs and verification receipts under the payload
root. Original captures, scalar vectors and predictions remain untouched.
No capture process, persistent worker, model download or paid allocation was used.

Global registration remains blocked by pre-existing ledger303 input_fingerprint
mismatch; inheritance cannot resolve the unregistered terminal. Preserve both
command receipts and local-disposition.json. The local role is a consumed
simulation COMPONENT_OR_CHALLENGER/COMPONENT for nominal-FOV sensitivity, with
explicitly worse false-alert performance and no hardware/deployment authority.
