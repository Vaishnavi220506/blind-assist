# Frozen Core alert policy: one complete new-layout validation

2026-09-20 EXPLORE controlled Development validation. User authorized consolidating
the useful simple policy and checking its whole-scene behavior, ending local
patches. Question: does the fixed high-precision working point plus one genuine-
strong-backed held frame retain useful Core event timing/continuity while reducing
false interruptions on one new complete cohort? No new algorithm is proposed.

Candidate is exactly strong T=0.4071309640537889 plus a nonrecursive one-frame
hold at dt=0.2 s. Only the preceding genuine strong decision can maintain the
current alert; a held alert cannot refresh state. Reset at clip/time gaps.
The definite-support bypass, original histogram origin, geometric score and
current-observation UNKNOWN remain unchanged. Compare Calibration
T0=0.007085703945147101 and strong without hold as an explanatory ablation.
No rising extrapolation, RGB branch, learned model, zone sum or alternative phase.
Held alerts do not assert fresh current-frame sensor support.

Existing evidence informed this selection: consumed primary Core Calibration
77/128/1, strong73/8/5, hold76/9/2 with12/12 events and two0.2s onset delays.
This validation concerns the combined hold policy's bounded tradeoff, not revival
of the old scalar candidate's lossless-onset claim. All old dispositions stand.

## Inputs fixed before capture

`core_hold_validation_spec.py` defines six BODY/HEAD cuboid types x three complete
INSIDE/BOUNDARY/OUTSIDE layouts x two backgrounds:36 clips,12 samples each at0.2s,
432 frames. New sizes/heights/poses/camera paths/backgrounds and unique identities
are checked against both consumed Core source specifications. No outcome-based
scene replacement, exclusion or seed search. The same Willow map, cube/material
assets, renderer, level camera1.82m and single-return simulator are shared.
These are new controlled arrangements, not independent hardware/natural scenes.
Clips include pre-entry and sustained approach, not departure or post-event release.

Keep X[-.3,.3], Y[-.2,.9], Z[.3,3] m full-extent geometry, closed contact and2cm
boundary reporting unchanged. Core membership is complete INSIDE/OUTSIDE layouts;
near-entry boundary-band frames within INSIDE remain in Core. BOUNDARY layouts
are fully reported separately and do not decide Core usefulness.

Freeze spec, protocol, code and prior evidence hashes before one capture. Reuse
authenticated assets/readiness/camera/bounds/trace/visibility source checks.
No new source failure exclusions: failed source is NOT_EVALUABLE. Mechanical
repairs require unchanged scientific inputs and retained receipts.
Source stage alone constructs original64-zone observations. Inference uses only
boxes/ranges and clip/timestamps; seal all three complete vectors before evaluator
join. No training, threshold selection or oracle source ownership enters inference.

## Prospective judgment of this bounded candidate

The following are pilot criteria informed by prior Development, not human reaction
limits or universal safety requirements. They are fixed before new outcomes.

- Detect all12 Core events, with first in-event alert no later than one sample
  (0.2s) after labelled entry. Report exact delay against both entry and Calibration.
  This explicitly permits the known one-sample cost; it does not retrospectively
  pass the earlier zero-delay scalar transfer.
- Reduce Core false-alert frames/duration by at least50% versus Calibration.
  Core false segments and each OUTSIDE/INSIDE-negative segment/frame count must
  not increase. This tests substantial overall benefit, not a tiny metric gain.
- In every Core event alert at least5/6 of positive samples and leave no silent
  run longer than one sample, including initial/internal/trailing gaps.

Report complete/Core/Boundary TP/FP/FN, precision/recall/FPR/F1, all event times,
coverage/gaps, preexisting alerts, clip-first alerts, negative sampled durations,
false segments and UNKNOWN; include BODY/HEAD/type/background counts and all
changed IDs. Report how hold differs from strong even if the whole candidate
meets the criteria. Record target front depth at first event alert as geometry
context only, not a measured human warning margin or collision time.

If all criteria hold, retain a Core-specific controlled-demonstration challenger
with declared Boundary costs; do not replace full-task Calibration or App defaults.
Otherwise retain prior local component evidence and report precisely which whole-
policy benefit did not transfer. Either outcome ends the algorithm round. No
retuning, new phase, extra cohort, widened timing budget or automatic successor.

Budget: one432-frame capture with existing3600s launcher limit and one evaluation.
Resident primary UE environment already holds authenticated assets; avoid worker
environment synchronization for this run. UE uses its configured graphics device;
source/64-zone scalar work uses CPU (TASK_NOT_GPU_SUITABLE). No online target-device
latency/thermal claim; measured host scoring time is descriptive only. Launcher
owns and verifies release of its UE process tree. Retain seals and source evidence
under `artifacts.local/work/ba-core-hold-validation-20260920/`.
