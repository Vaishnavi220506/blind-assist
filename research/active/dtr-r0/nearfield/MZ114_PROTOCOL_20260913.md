# MZ114: joint spatial hypotheses with short measured-range correspondence

EXPLORE,2026-09-13. The active goal remains four-sensor obstacle localization
with improved recall/timing and reduced nuisance. MZ113 improves optical range
continuity but retains40independent persistent-ghost FP and misses one current
range/image pair because reciprocal matching is ambiguous.

First diagnose MZ112/113's480 consumed frames: suppressing unmatched Radar is
only an evaluator counterfactual, never an adopted method. Missing center-ray
ToF does not prove a free volume, especially for HEIGHT_UNKNOWN Radar. Coarse
feature collisions are not equality of full observations.

Implement a fixed joint assignment candidate, not another independent hit count.
Retain injective assignments for up to4current Radar returns, including null
assignments. Angular eligibility keeps the existing12deg envelope. Pair cost
is squared distance outside the image angular interval divided by6deg. Null
cost2.0; retain every global assignment within0.5 of best cost. A pair resolves
only when every retained assignment gives the same nonnull image proposal.

The temporal arm adds squared prior range residual divided by0.20m, capped at9,
only with unique reciprocal current optical correspondence (unchanged MZ113 LK,
IoU>=0.2) and measured range age<=0.5s. Finite Doppler propagates range; missing
Doppler explicitly holds range. Missing prior costs zero, never contradiction.
Only current resolved measurements refresh range age; appearance alone cannot.
Missing RGB/invalid IMU/time/episode reset history and retain independent support.

Resolved pairs use the unchanged MZ111 plane proxy; unresolved returns preserve
independent Radar with HEIGHT_UNKNOWN. Independent ToF always survives. The
primary joint-temporal combination uses this current spatial readout OR the
unchanged MZ113 flow additions relative to nominal. Keep a joint-current ablation
with identical assignment costs but no temporal term, all prior arms, and an
RGB-disabled control. No privileged geometry/identity or future sample enters
prediction. Association costs are compatibility proxies, not calibrated risk.

Freeze methods before evaluating768 consumed MZ107--113 frames. Authenticate
original source/cache hashes, seal predictions before evaluator/provenance read.
Report full TP/FP/FN/UNKNOWN, family retention, event timing, false support and
post-exit duration plus exact changed-support identities. No threshold search or
result-driven parameter changes. A useful reduced-nuisance result must retain
incumbent TP and event timing while reducing FP/duration. A recall/FP tradeoff is
reported as such, never relabeled as success. Only a decision-changing benefit
justifies a separately frozen fresh panel; otherwise close this fixed candidate
and carry the failure into the next information/representation study.

The conceptual references are [DELTAR](https://zju3dv.github.io/deltar/) (zonal
depth distributions rather than exact pixel depth) and
[CR3DT](https://arxiv.org/html/2403.15313v1) (motion/appearance association with
Radar velocity). This is not an implementation or reproduction of either model.
No pretrained network, training, hardware claim or sensor architecture change.
