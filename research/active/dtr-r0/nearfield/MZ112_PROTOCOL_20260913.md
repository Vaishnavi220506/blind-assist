# MZ112: unchanged MZ111 methods on independently authored scenes

EXPLORE, 2026-09-13. Fresh constructed Development, not natural-domain or blind
confirmation. The source generator was authored independently of MZ111 outcomes.
20 scene-disjoint episodes x12 frames =240, at .25s sampling, all frames retained.
New geometry/texture/sensor seeds; unchanged one-RGB rendering and hypothetical
ToF/Radar/IMU source. Static objects, straight translation and head yaw; fixed
pitch -3 degrees, roll0 because those are the available sensor input contracts.

Four pole, four suspended HEAD, four sizable offroute, four multitarget with
persistent ghosts, two clear-context and two isolated 1cm boundary stress scenes.
Source AABB expectation132 positive/108 negative frames must be verified against
native engine bounds before accepting comparison. No labels from source enter
prediction. Report the two stress episodes separately from primary event results.

Freeze MZ111 protocol and all method source hashes before capture. Evaluate the
same baseline, nominal, interval, shell, plane, filtered-plane, temporal and
predeclared combined arms without changing thresholds after old or fresh scores.
Primary challenger is combined, selected for 20 additional nominal TP on old
data at four extra FP; preserve individual arms to diagnose that tradeoff.
No automatic primary switch on fresh results. No second capture unless an
identified source/runtime defect invalidates the first; preserve failed outputs.

Use the same scorer: frame recall/errors/UNKNOWN by family, loss of baseline
and nominal positives, positive event segments, first correct alert latency,
false alert segments and discretized duration. Comparison is matched-current
reference occupancy, not future collision. Existing ghost ambiguity remains a
falsifier: do not call additional recall a nuisance reduction or learned identity.

After prediction sealing, verify actual native bounds against source-defined
truth, audit newly added supports using source Radar provenance and current RGB
proposal correspondence, and preserve wrong/missing identity evidence. Temporal
support must point to a preceding received measurement in the same episode;
age/velocity/RGB continuity must be reconstructable. No true range/pose/box enters
inference. A positive count alone is insufficient for a retained method claim.

Complete one240-frame capture and fixed analysis, preserve evidence, release
owned UE/Zen resources. A negative or tradeoff result changes the next mechanism
choice within the active larger goal; it does not establish a sensor ceiling.
