# MZ120: learned occupancy reduces alerts but fails spatial retention

**Do not replace MZ116. Stop this fixed pilot recipe.** The independently read
spatial output exposes a weak-rod localization failure on development. The frozen
threshold then loses HEAD recall and timing on consumed MZ119 transfer. These
failures prevent the apparently large nuisance reduction from being called a
successful spatial-ownership upgrade. No CNH, fresh capture, additional suppressor,
or dynamic predictor was launched.

The user's newly supplied direction authorized this bounded first round after
the earlier MZ119 stop. [Protocol](MZ120_PROTOCOL_20260913.md). This is a small-data
Development feasibility pilot, not completion of a thousands-frame paired corpus
or the requested eventual fresh-scene confirmation. A negative pilot stops this
recipe; it does not close learned fusion or establish a sensor information limit.

## Actual implementation and data

[Model](mz120_occupancy.py), [runner](run_mz120_occupancy.py),
[boundary checks](test_mz120_occupancy.py). The11,305-parameter model learns45
body-coordinate occupancy cells from640×360 RGB with a stride2 feature branch,
projected local samples, angularly local attention to separate ToF target slots,
and Radar return attention. It receives measured distance/sigma/signal/status,
packet/slot validity, available Doppler and IMU orientation. Native bounds,
actor roles, hidden histograms, episode names and old alert scores never enter
runtime tensors. No final OR preserves the old output. Below threshold remains
NO_ALERT_UNKNOWN, not CLEAR. The spatial height predictions are learned outputs,
not a guarantee that resolves all HEIGHT_UNKNOWN measurements.

MZ116 remains the exact sealed comparator on identical observation streams. It
retains its original short persistence; the candidate consumes current RGB and
returns only, with deterministic causal IMU integration and no learned history.
This is the specified replacement of allocation plus alert readout, not an
isolated attribution experiment on a single old software component.

MZ115+MZ117:336training frames/28episodes and144development frames/12episodes.
Complete episodes are separated; no adjacent-frame split. All sources are already
consumed constructed UE evidence with hypothetical sensor proxies. Families,
backgrounds and templates remain related. MZ119's240frames/20episodes are a
separate consumed transfer diagnostic; they are not fresh or natural scenes.
There are no duplicate PNG byte hashes across episodes/panels in the720images.
Labels use native engine AABB overlap, not dense per-pixel surface correspondence.

One800-step AdamW fit from scratch, seed120013, batch8, no checkpoint sweep.
One threshold0.71 selected on development to minimize FP subject to matched
frame recall and complete-event retention. Model and threshold are frozen before
this runner parses the transfer evaluator. Full101-point curves are retained,
including unselected transfer points; those points do not license test retuning.

## Effects at the selected threshold

| Cohort / arm | TP | FP | FN | TN | Recall | False seconds | Detected events | Maximum first-alert delay |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Development MZ116 |82|40|2|20|97.62%|10.0|8/8|.25s|
| Development MZ120 |83|14|1|46|98.81%|3.5|8/8|0s|
| Consumed MZ119 MZ116 |153|62|2|23|98.71%|15.5|14/14|.25s|
| Consumed MZ119 MZ120 |139|20|16|65|89.68%|5.0|14/14|1.25s|

Development FP/duration fall65%; transfer FP/duration fall67.74%. Transfer loses
16previously correct frame alerts and recovers2old misses, so net TP falls14.
All16transfer misses are in suspended_head:42/42positive frames become26/42
(61.90% recall). A14/14event count hides the delayed HEAD alerts and frame gaps.
Development similarly removes28old FP and adds2new FP, losing1old TP while
recovering2old misses. Its remaining12boundary-stress FP are not repaired.

Spatial supervision changes the decision:

- Development rod-occupied central cells:102TP/24FN, **80.95% recall**, below
  the predeclared90% requirement, despite all rod-containing development frames
  alerting. All24misses are the central BODY1.4–2.6m cell:12frames of
  `mz115_rod_farwall_3/shape0` and12of `mz115_multi_weak_rod_bright_body/shape0`.
  Actor-role masks include weak rods in mixed-family episodes. Independent
  reconstruction exactly matches all480×45stored Development labels.
- Development HEAD cells:133/142,93.66% recall, but97false HEAD cells.
  Near-body cells overall:205TP/175FP/43FN. Whole-grid IoU0.3761.
- Transfer rod cells:190/190; HEAD cells169/185,91.35%, with269false HEAD cells.
  This pooled HEAD metric includes poles and other HEAD-overlapping geometry;
  it cannot replace the suspended-head family recall above. Whole-grid IoU0.3981.

Thus neither high rod-frame recall nor pooled HEAD-cell recall proves correct
object-specific spatial allocation. The candidate still emits many wrong spatial
cells. `mz119_head_static_lateral` alerts on only1/12positive frames, and
`mz119_head_side_margin` on1/6, with1.25s and.75s delays respectively.
No broad localization improvement is established against MZ116, whose
sealed outputs do not provide an equivalent probabilistic occupancy grid.

Training-set alert recall is176/205,85.85%, with23FP and2missed complete events;
training HEAD cells247/310 and rod cells219/250. This is not evidence of a model
that solved training geometry and merely overfit a held-out set. Small training
coverage, the very small from-scratch representation, optimization and local
sampling remain confounded with measurement ambiguity.

## Small source attribution, before training

The source audit reconstructed current Radar support from sealed proposals and
the existing runtime. Counts below are nonexclusive supporting sources.

| Consumed panel | MZ116 FP | Merged ToF | Other ToF | Current Radar | History required for shared Radar flag |
|---|---:|---:|---:|---:|---:|
| MZ115 |69|32|18|32|2|
| MZ117 |64|32|14|31|2|
| MZ119 |62|39|21|27|0|
| Total |195|103|53|90|4|

73FP have only merged support and60only current Radar. All four history-required
cases also have other ToF support. `common_radar` already combines current and
historical branches, so overlapping history is not independently recoverable
from that flag. These are source-support attributions, not causal removal gains.
The new representation can touch the main nuisance sources, but success still
requires retaining true thin/head occupancy.

## Controls, verification and disposition

At the same threshold, RGB-off yields0TP/0FP/84FN on development, and the fixed
sensor permutation yields82TP/15FP/2FN versus83/14/1. These are inference-time
counterfactuals, not retrained modality ablations. They do not prove RGB-only
sufficiency or a lack of information in ToF/Radar. Audit finds144/144permuted
pairs have the same timestamp and current-corridor truth;96/144have the exact
same family and72/144identical CORE occupancy. It is a correlated substitution
with limited diagnostic force, not a successful falsifier of sensor reliance.
RGB-off is a black-image intervention, not a trained sensor-only baseline.
No private CNH was exposed to any model.

Focused tests cover scene split, current versus far/above-head labels, metadata
and stale-packet isolation, separately missed events, and alarms at the wrong
height failing spatial credit. Actual two-frame CUDA forward/backward is finite.
All source receipt entries and baseline payload hashes pass in the runner.
The full training, threshold selection, sealed transfer and serialized output
complete successfully. The model-effect acceptance fails, which is separate
from execution integrity.

Measured backend is torch2.11.0+cu130 on NVIDIA RTX5060Laptop. Equivalent
batch8forward probes: CPU median79.564ms, CUDA5.177ms. GPU training takes21.006s;
240-frame cached tensor inference takes.245s. These timings exclude capture and
are not Android or real-time sensor-to-alert latency. Total runner time41.424s.
No service, capture worker, background compute or paid allocation was started.
The foreground process exits and releases its GPU tensors.

Intended inheritance: **NEGATIVE_CONTROL for this exact small-data, small-model
recipe and its dev threshold**. Preserve MZ116 as runnable baseline. A future
learned route must confront these weak-rod/HEAD localization failures, but this
result does not rule out stronger local representations, appropriate paired
training coverage, or summary/CNH contrasts under separate authorization.

Structured registration remains blocked by the pre-existing
`experiments/index.jsonl:303` input-fingerprint mismatch. The attempted command
output is preserved; registration, structured inheritance and knowledge-engine
acceptance are **not claimed**. No unrelated ledger repair or bypass occurred.

Durable artifacts: `artifacts.local/work/mz120-learned-occupancy-20260913/`:
`pilot-v1/model.pt`, source/code/split/threshold hashes, saved probabilities,
summary, complete curves, backend/timing receipts, progress, completion, plotted
PNG/SVG, and registration-attempt output. Existing captures remain owned by their
source experiments. Retained checkpoint/evidence belong to MZ120 for diagnosis;
there is no running allocation to release.
