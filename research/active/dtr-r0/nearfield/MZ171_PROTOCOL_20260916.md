# MZ171: direct supervision of returned ToF corridor witnesses

EXPLORE consumed original TRAIN scene-heldout contrast,2026-09-16.
Previous goal turn was PROGRESS: MZ170's fresh result closed the fixed mean,
including four lost native-supported true frames. MZ129 remains the baseline.

## Distinct question

MZ161/165/166 encode all132 public ToF/Radar slots independently, yet their
return head receives only a frame-maximum loss. MZ165/166 report nonzero updates
but no positive final alerts from that head. Can direct labels for each returned
ToF slot make this existing branch useful without increasing false warnings?
This changes the supervised target, not MZ170's weights or cutoffs. No MZ170
model, mean, score, scene or source is used for fitting or selection.

Both arms use the unchanged MZ165 ContextTaskNet and authenticated cached
pretrained DINOv2 context/public native-detail inputs. All20090 parameters start
identically at seed171016. Both fit the same original TRAIN FIT144(scene0-2),
with HELD48(scene3),20 identical epoch orders,720 AdamW updates,batch4,lr.001,
weight_decay.0001. Cutoff remains zero and the unchanged final readout is the
maximum of query-eligible pixel and independent valid-return logits.

Control loss is frame BCE plus balanced known-pixel BCE. Witness loss adds
one times class-balanced known-ToF-slot BCE. Each frame allocates half mass to
positive/negative known slots when both occur, one mass to a sole class, and
zero to UNKNOWN. Average over all batch frames, including zero-label frames.
No weighting sweep, augmentation, checkpoint selection, new epoch schedule,
threshold tuning, dense-backbone change or synthetic sensor upgrade.

## Label and input authority

For a received usable SIM_VALID/SIM_MERGED public slot, match its zone_id and
target_index to the native returned_lineage and actual private ray hit points.
POSITIVE means at least one finite returned contributor is in the body corridor
x[.2,3.6],y[-.3,.3],z[.4,2.05]. NEGATIVE requires a nonempty uniquely matched
lineage with every contributor resolved and outside. Absent/invalid packets or
slots, incomplete evidence without an inside witness and unmatched lineage are
UNKNOWN. Actor/face ownership is not required for a finite native point.
Repeated/conflicting keys must not be silently overwritten. Synthetic tests
cover these boundaries before a single fit. Native values are offline targets
only and are never model inputs or prediction masks.

All128 ToF and4 Radar inputs stay unchanged, including second, merged and
RGB-external returns. Radar target entries are UNKNOWN: this source lacks an
equivalent native3D returned-point lineage. The shared return head can change
Radar outputs through ToF gradients; report that limitation and separate ToF/
Radar maxima rather than claiming untouched Radar decisions.

Only FIT144 native records may enter preparation or fitting. The full source
spec and original TRAIN192 public records may be read for exact selection and
input admission. Original dev/test raw/evaluator/RGB/predictions are not decoded.
Use selected-ID parsing, authenticate the MZ161 input/label seals and MZ165
context/encoder seals, and hash-bind source, code, protocol and preflight before
training. Both final models, all192 maps,132-slot logits and frame scores per
arm seal before HELD native parsing. No fitting from held labels or previous
MZ165/166 predictions. All these scenes were consumed by earlier experiments;
the held partition is not fresh confirmation.

## Decision and reporting

Report FIT/HELD frame confusion, precision/recall, family and ordinary/pressure
strata, exact baseline/control true-frame retention, event timing, paired
decisions, UNKNOWN, false segments and complete native contributor retention.
Report known-slot TP/FP/FN/TN and precision/recall, positive/negative/unknown
denominators, token-only/pixel-only/both warning contributions, and separated
ToF/Radar maxima and argmax validity. Slot/pixel samples are not independent
scenes. Pixel maps remain direct risk scores, not metric geometry or clearance.

Retain an alert challenger only if witness FIT accuracy>=95%, FIT has no
native-supported nonalert, and HELD retains every MZ129/control true frame and
event onset, reduces MZ129 FP by>=25% with no family FP increase, has fewer FP
than matched control, and has no native-supported nonalert. Require all; retain
MZ129/default even after a consumed pass until separate fresh confirmation.
Known-pixel/family/half-column metrics remain diagnostic, as in MZ165.

A branch-learning effect is reported separately only if HELD known-slot recall
and balanced accuracy both improve over control, with defined denominators;
it never substitutes for the full warning criteria. A failed joint result closes
this exact supervision/head/training recipe without loss/epoch/cutoff/OR rescue.
Do not expand capture because training fit fails. The broad goal remains active.

## Execution

One1200s local real-data allocation covers cache admission, two sequential GPU
fits, sealed predictions and evaluation. Synthetic loss/head checks and FIT-only
source-label preparation precede it. Reuse the measured MZ165 head CPU/CUDA
placement because architecture,input shape and environment are unchanged; bind
the measurement and record the live device and actual CUDA execution. Disable
TF32; compute/output float32, existing caches float16. No encoder inference,
runtime installation, paid allocation or remote capture is required.

Save optimizer/RNG checkpoints at epochs5/10/15/20, preserve partial progress on
mechanical failure, and never restart scientific training or expand the budget.
Release task-owned processes/GPU allocations after completion; retain evidence
under artifacts.local/work/mz171-return-witness-20260916/. Use supported metadata
writers and preserve the existing ledger303 error without bypass.
