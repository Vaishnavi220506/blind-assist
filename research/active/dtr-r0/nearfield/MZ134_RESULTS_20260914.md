# MZ134: the weak joint model cannot require a visible source

**Keep MZ129.** The fixed exploratory joint feasibility model supplies no
identifiable source assignment. Its conservative native-zone readout adds16 FP
without recovering a TP. Surface-origin attribution is `NOT_EVALUABLE` under
this explicitly weak model; its alert readout is a negative result. This does
not reject joint attribution with a more informative, validated forward model.

Terminal: `MZ134_SOURCE_ATTRIBUTION_NOT_EVALUABLE_WEAK_MODEL`. Intended scoped
inheritance: `NEGATIVE_CONTROL` for this free-residual source model used as an
alert replacement. No training, probability calibration, parameter sweep,
protected data, baseline promotion or successor was run.

| Branch | TP | FP | FN | UNKNOWN | False segments | Max detected delay |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| MZ129 ToF only | 115 | 89 | 29 | 84 | 21 | 1.50s |
| Joint feasibility ToF only | 115 | 105 | 29 | 68 | 26 | 1.50s |
| MZ129 full system | 139 | 93 | 5 | 56 | 22 | .25s |
| Joint feasibility full system | 139 | 109 | 5 | 40 | 27 | .25s |

All288 consumed frames retain their original denominator. Both candidate
branches add16 FP and lose0 incumbent TP. Full-system BODY FP rises30 to32 and
HEAD FP3 to17; rod25 and boundary35 remain unchanged. Full-system15 first-alert
events retain identical timestamps. ToF detects14 of15 events in both arms.
Full-system false-bin duration rises23.25 to27.25s on this4Hz synthetic panel;
this is not measured user interruption time. Fewer UNKNOWN frames come from
additional false alerts and are not improved coverage or clearance.

The [pre-score brief](MZ134_JOINT_ATTRIBUTION_BRIEF_20260914.md) declares
`y = A alpha + u`, using signal times squared range and anonymous visible-mask
area fractions over inherited .25m forward-depth cohorts. Every mask remains,
including visible floor/background. Additional columns represent a diffuse
background nuisance and outside-RGB coverage. `u` is a nonnegative unresolved
contribution per return. Marginal coefficient intervals and concrete positive
alternatives are saved; their simultaneous upper endpoints are not asserted
jointly feasible. No normalized weights are called probabilities.

There are260 SIM_VALID cohorts, all260 admitting visible alternatives and an
all-unresolved explanation;156 also admit an outside-RGB alternative. No visible
source has a positive lower coefficient bound. This follows directly from the
deliberately unconstrained unresolved term: setting all visible coefficients to
zero and `u=y` is feasible. These counts describe observation coverage in this
model, **not empirical proof that real sensor attribution is impossible**.
Per-surface reflectance, constituent depths of merged returns and a validated
relationship between projected mask area and signal are unavailable at this
interface. The code does not fit those missing quantities from evaluator truth.

The saved algebraic falsifier constructs the same aggregate signal/range with
a visible off-corridor explanation or a hidden in-corridor explanation. The
hidden explanation correctly remains unresolved. This checks the inverse-model
representation only; it is not a pair of physically re-rendered identical RGB
scenes or a validated ToF forward-model counterexample. A meaningful subsequent
physical attribution experiment would need constraints that distinguish these
explanations, rather than a regularizer that silently chooses one.

Prediction reconstructs all3954 ToF returns from raw packets:3688 SIM_VALID and
266 SIM_MERGED. Merged components retain the existing .02–4m uncertainty rather
than treating the returned average as each contributor's depth. Whole native
zone support remains because no source is mandatory. Alert decisions use the
inherited central-four weights, threshold1 and certain-coarse test. The full
system unions this fresh ToF decision with the frozen MZ129 Radar-only branch.
**No old full-system alert is ORed into the candidate.** Restoring unresolved
native footprints instead of trusting the old visual contraction explains the
additional nuisance. No Radar reassociation is claimed.

After prediction sealing, evaluator-only native hit projections audit27753
angular contributor samples, including3704 corridor samples and619 outside RGB.
Zero samples are dropped in any category. This establishes angular support
retention, not correct identity, calibrated range bounds or physical localization.
The evaluator and actor identities are absent from the predictor arguments.

Four narrow tests pass: exact marginal feasible bounds, a contradictory observed
zero, hidden-corridor ambiguity, and retained ToF support with no visible masks.
Real missing zones are never injected as observed zeros. Prediction took
.188s on host CPU (`TASK_NOT_GPU_SUITABLE`), excluding mask preparation/loading,
serialization and scoring; this is not device latency. No persistent process,
worker, paid allocation or temporary capture resource remains.

Evidence: `artifacts.local/work/mz134-joint-attribution-20260914/replay-v1/` holds
the predictions, source hashes, complete matrices/feasible intervals/residuals,
falsifier, native retention, summary and PASS completion receipt. The sibling
`artifacts.local/work/mz134-joint-attribution-20260914-run.log` is retained.
No pre-existing terminal output was mutated. The supported registration writer
was checked by the enclosing delivery and remains blocked by the pre-existing
`experiments/index.jsonl:303` input-fingerprint mismatch. Registration and the
intended structured inheritance remain pending; no ledger bypass was applied.
The exact error is retained in
`artifacts.local/work/mz133-angular-resolution-20260914/registration-attempt.log`.

```powershell
& 'E:\codex-tools\venvs\dual-loop-radial-geometry-lite-r0\Scripts\python.exe' research/active/dtr-r0/nearfield/mz134_joint_attribution.py --output artifacts.local/work/mz134-joint-attribution-20260914/replay-v2
```

Use a new output directory for reproduction. The declared one-method experiment
is stopped; the command documents reproducibility and does not authorize a sweep.
