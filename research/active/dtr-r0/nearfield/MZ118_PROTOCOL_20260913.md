# MZ118: auxiliary border regions and interval-feasible plane geometry

EXPLORE, consumed constructed Development. MZ117's actual mixed inputs had zero
refinement coverage. Current pixel-only diagnosis finds size-filtered wall boxes:
97.6% of valid single-return targets fit inside no retained eroded RGB proposal.
Nested suppression alone supplies no new mixed-frame plane fits. Restoring broad
image regions, separate from Radar, is an observed frontend correction hypothesis.

Use all240 MZ115 plus all240 MZ117 frames, authenticated original native captures.
No new source, simulator, label, range/angle threshold or calibration setting.
Previous outcomes and these images are known; never fresh confirmation. Freeze
all new methods, dependencies, scorer and this protocol before this paired readout.
One480frame CPU replay,1200s wall budget, no parameter sweep or automatic restart.
Mechanical faults may be repaired with preserved logs and consumed authority.

## Coupled mechanism and ablation

MZ116 runnable resolution_guard is baseline, with original RGB frontend for Radar
and original valid-ToF allocation preserved. Auxiliary proposals retain its legacy
boxes and add only Otsu/MSER boxes rejected by image-relative width/height limits.
Keep all original MSER settings including maximum area.5hw; deduplicate added
boxes when all four corresponding edges differ by<=1px. Keep nested alternatives
as ambiguity. No LSD boxes, native geometry, sensor ranges or labels generate RGB
regions. Missing/blank RGB abstains.

`expanded_fit` applies unchanged MZ117 conditional coefficient bounds using only
these auxiliary regions. `interval_lp` uses the same valid-anchor selection:
at least6 single-return SIM_VALID full zones wholly inside a box eroded2px, no
other proposal intersects those zones, >=2rows and2columns. Target SIM_MERGED
must intersect exactly that one model's proposal. No center-fit residual or
condition-number gate is needed by LP; the observations constrain its feasible set.

Represent plane beta·X=1. Full unit-ray coordinate intervals and inverse range
[1/(r+.13),1/(r-.13)] define two interval-overlap inequalities per anchor. In each
of8 closed coefficient-sign octants those inequalities are linear. Keep every
feasible octant; a solver error makes the model unknown. Optimize the full-ROI
minimum and maximum denominator over all retained polytopes. Unbounded, failed
or nonpositive inversion falls back. LP numerical padding1e-8 expands constraints
and final denominator bounds; HiGHS primal/dual tolerances1e-9 are a working
numerical setting, not a formal interval-arithmetic certificate. Clip inferred slant range to.02–4m only after
inversion; require overlap with reported merged mean±.13m. Existing pitch/yaw
intervals give body XYZ. Coarse certain-IN and all valid-ToF support persist.

These are conditional outer bounds, not a proof of common surface. Independent
coordinate-ray intervals enlarge the true feasible set. Plane feasibility cannot
establish clearance or justify dropping a foreground contributor hidden by a wall.

## Evaluation and disposition

Seal both panels' complete ten-distance predictions and RGB-off controls before
this runner parses native evaluator files. Fixed current3.6m corridor labels,
same distance grid1/1.4/1.8/2.2/2.6/3/3.4/3.6/3.8/4m. Report each panel and pooled
paired TP/FP/FN, events, first-alert delay, false segments/duration, exits, families
and complete predeclared nonstress subsets. Do not merge incomparable sensor models;
both selected panels share the unchanged finite-zone MZ115 model.

Use frozen MZ117 contributor audit for both new arms: range, ROI, XYZ and hazard
loss, including losses hidden by retained Radar alerts. Report actual model and
refinement coverage. A localized near-contributor loss prevents correct-location
promotion even if aggregate recall survives. A useful gain without such loss may
justify a new unchanged complete-source check; no gain or a loss ends this fixed
recipe. Preserve controls and now-consumed results without threshold tuning.
No default App, physical-sensor or user-safety promotion.
