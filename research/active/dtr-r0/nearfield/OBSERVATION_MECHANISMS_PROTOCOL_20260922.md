# Five observation mechanisms: one bounded synthetic comparison

2026-09-22, EXPLORE. User authorized all five brainstorm directions: along-path
sampling, quantization-crossing observation, continuous boundary witnesses,
two-step observation, and reasoned UNKNOWN. This protocol fixes common input
and budgets. Each mechanism keeps its own outcome; no combined-system claim.
Old finite-bank failures and boundary-pair results remain consumed and unchanged.

## Shared new source and limits

Exactly90pairs/180single-box scenes: side{-1,+1}, widths{.07,.19,.37}m,
center depths{.95,1.45,1.95,2.45,2.95}m, margins{.007,.017,.027}m.
For each pair use X=side*(.30+width/2-margin) and
X=side*(.30+width/2+margin), thickness.04m, wall4.20m. Full original query is
[-.30,.30] lateral and[.30,3.00] forward. No truth-based filtering. Verify unique
geometries and zero overlap with original653, transfer1304, and boundary180;
fail admission on overlap. This is same-generator analytic Development, not
independent-source/hardware confirmation. Source/config/code/briefs are sealed
before new observations. No alternate cohorts, tuning or amplitude sweep.

Same8 ideal rays, exact known poses and radial quantization.10m. The diagnostic
pose pool is origin; four axial spokes at every.01m through.12m; and four
(.06,.06) signed corners:53unique poses per scene,9540 evaluator view calls.
Real-valued distances remain evaluator-only except in the explicit analytic
continuous model; inference receives measured bins, never hidden distances.
All classifiers/selection functions get public bins/poses and declared model
assumptions only. Case IDs carry correspondence, not routing or geometry.

## Five fixed checks

1. Along-path sampling: primary +X12cm straight path, initial+endpoint versus
   all13readings. Same motion distance but2versus13observations. Report90pair
   and180scene finite-cohort ambiguity metrics; additional directions are
   declared evaluator ceilings, not source-aware action choices.
2. Quantization crossing: primary +X12cm path, same2versus13readings. Preserve
   per-ray bin transitions. Separate stable corresponding return-surface changes
   in quantization from visibility/surface switching; raw distance attribution
   is evaluator-only, no resolution sweep. Before any new observations, analytic
   face geometry showed +Z is degenerate for pure crossing on these paired boxes
   (shared front depth, lateral-face distance independent of cameraZ). Thus+X is
   frozen as primary and+Z retained as a structural control. This shares the path
   with check1 but separates mechanisms; gains cannot be counted twice. See the
   frozen path-specific brief.
3. Continuous witnesses: real-valued one-rectangle model, centerX[-.9,.9],
   width[.03,.60], centerZ[.60,3.40], thickness.04, wall4.20. Solve from bins at
   initial and initial+fixed+X12cm only, cache by public observation signature.
   Preserve exact feasible positive/negative geometries or explicit solver
   uncertainty. The witness brief fixes formulation, solver budget and reasons.
   Never interpret unsuccessful search as proof of absence or unqualified OUT.
4. Two-step policy: two6cm axial moves,3observations,total12cm. Implement one
   public-only lookahead policy using the unchanged original621-scene bank.
   Forecast all public bank observations for the13possible tree poses. At each
   second step minimize remaining mixed-group positive count, then mixed total,
   then original+X/-X/+Z/-Zorder. First step minimizes the sum of those optimal
   second-step residual costs across predicted intermediate observations.
   After the actual first6cm view, recompute posterior and choose the second step.
   Seal first choices before actual mid observations; seal second choices before
   actual final observations; seal predictions before evaluator truth. Compare
   fixed+X then+X at equal3views/12cm, original positive-priority2views/12cm,
   and its fixed endpoint counterpart. No alternate objective or bank expansion.
   Report TP/FP/falseOUT/correct negatives/UNKNOWN and exact gains/losses.
   Separately compute evaluator-cohort adaptive and nonadaptive two-step ceilings
   using one first action per initial observation class and one second per actual
   intermediate class. Neither is an implemented predictor; compare3view costs.
5. Reasoned UNKNOWN: use the continuous solver's observable witness/result states
   to distinguish incomplete support search, opposing consistent explanations,
   and particular opposing witnesses whose forecasts stay identical across the
   four allowed12cm endpoints. Preserve model-domain/solver limits. A witness
   pair is not the whole feasible set; failure to split that pair is not proof
   every geometry/action/path is uninformative. Test reason truthfulness, not just
   converting all outputs to UNKNOWN. Report coverage and all failed searches.

## Fixed decisions, budgets and delivery

Signal-path benefit requires additional separation versus the matching endpoint
while preserving its information, with extra observation cost reported. A
quantization benefit must satisfy the stricter stable-surface attribution, not
merely improve the combined visibility signal. A two-step policy is useful only
with additional TP over fixed equal-cost3view, retention of its TP IDs, no new
wrong commitments by identity and nondecreasing correct-decisive count. Report
partial gains even when this full criterion fails. Evaluator ceilings only justify
later investigations, never automatic promotion.

Continuous witnesses must replay exactly into all supplied bins and respect
declared domain/label. Presence of two opposite labels establishes model-conditional
ambiguity. Any classifier-like one-sided answer must have the solver's stated
exclusion evidence; otherwise retain SEARCH_INCOMPLETE. Record actual error,
coverage, solver statuses and runtime; no assumption that a true geometry was
automatically found. Reason categories must not add unsupported evidence authority.

One scored joint cohort after all linked briefs are frozen, each mechanism once;
no network training, new sensors, noise tuning, changed quantization or automatic
successor. Mechanical repairs preserve failed outputs and unchanged scientific
settings in a new child run. CPU standard-library geometry/small constrained
optimization is TASK_NOT_GPU_SUITABLE. Solver budget is fixed in its brief before
execution. Artifacts under artifacts.local/work/ba-observation-mechanisms-20260922/.
No background/paid resources. Focused tests plus independent saved-output audit,
supported registration only, preserve ledger303/unknown-terminal receipts if still
blocked. Deliver each result with its limitation and scoped inheritance role.
