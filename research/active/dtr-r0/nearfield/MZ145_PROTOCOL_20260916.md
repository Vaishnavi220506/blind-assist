# MZ145: causal confirmation of weak learned corridor evidence

EXPLORE, 2026-09-16. New temporal decision state, prompted by MZ143's 13-to-8
false-frame reduction but 4-to-6 false-segment increase. MZ143 weights, features,
selected candidate and low threshold remain frozen. No refit or threshold rescue
of the original single-frame result. This comparison adds one causal mechanism.

On each frame, alert immediately if current score reaches a high onset cutoff;
otherwise alert only if BOTH current and previous-frame scores reach the frozen
MZ143 low cutoff. Reset history at episode boundaries or non-.25s timing gaps.
No future frame, backdating or retrospective event correction is allowed.

Derive the one high cutoff on MZ143 OOF TRAIN scores: take the minimum score
among incumbent true-positive frames whose preceding score is below the low
cutoff or absent. This is the largest strong-onset cutoff that retains every
incumbent true OOF TRAIN frame under the new causal rule. If this equals the low
cutoff, expose that the calibration admits no temporal selectivity. No high/low
grid search or dev tuning. Seal the cutoff and code before loading dev scores.

Evaluate the fixed rule on all48 consumed MZ143 dev predictions, with MZ129 and
unchanged MZ143 as comparators. These are cached scores from observable images
and sensors, not evaluator-derived inputs. Predictions seal before dev labels
are decoded. No original test access, feature rerun, training, capture or model
download. Scalar Python/NumPy work is TASK_NOT_GPU_SUITABLE; cap2minutes.

Report confusion, pressure/ordinary families, paired decisions, event-by-event
first-alert delay, lost incumbent true frames, UNKNOWN and native contributors.
Keep a Development challenger only with TP24, FP<=10, all5events and added
delay<=.25s, no family FP regression and false segments<=4. Compare reduction
against original MZ143 too. Otherwise retain the scoped diagnosis and stop this
exact rule. No in-run successor or parameter rescue. Broader user goal remains.
