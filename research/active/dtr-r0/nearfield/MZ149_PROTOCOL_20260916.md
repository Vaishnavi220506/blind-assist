# MZ149: explicit obstacle-state persistence from TRAIN transitions

EXPLORE Development, 2026-09-16. The proposed MZ149 capture in the previous
turn was cancelled before creation. This number now denotes a new state-model
experiment; it is not a fresh capture or a reopening of MZ148's frozen readout.

MZ148 supplies a useful image feature component but loses a still-present
boundary obstacle after its score drops. Test a two-state causal filter of its
unchanged compensated classifier scores. Estimate initial state frequency,
positive training prior and2x2 label transition frequencies using only original
TRAIN192; add one fixed Laplace count to every category. Convert classifier
odds to an emission-likelihood proxy by removing the TRAIN class prior, then
combine with the predicted state. Use only forward recursion, resetting on
episode/time gaps. Correlated HGB evidence is not an independently calibrated
likelihood; resulting values remain empirical scores, not safety probabilities.

For each original held-out scene fold, estimate transition/prior parameters
from the other three folds, and filter that fold's existing MZ148 OOF scores.
Choose one alert threshold, the minimum resulting score on MZ129 TRAIN true
frames. Refit transition/prior counts on all TRAIN once, and seal parameters,
threshold and code before reading Development scores. MZ148 image features,
HGB model weights and original low/high thresholds remain unchanged artifacts;
this new candidate uses its own explicit state filter and single TRAIN-derived
threshold. No learning-rate/state-count/smoothing/threshold search, no alternate
model selection and no future-image or native-geometry input.

First consumed dev48 must retain all24 MZ129 true frames and all five event
times without delay, have at most3 FP and two false segments, no family FP
increase and no non-alert with returned native ToF corridor contributors. If
it fails, stop before this candidate reads MZ146 scores. No rescue tuning.

Only if the first gate passes, score consumed MZ146288: require at least143 TP,
all incumbent true frames/events without delay, fewer than57 FP, at most21 false
segments, no family FP increase against MZ129 and returned ToF corridor retention.
Report MZ148142/43/2 and MZ145143/57/1, frame/event/nuisance/pressure/pair counts,
UNKNOWN and native Radar-lineage unavailability. An improvement remains
Development until a separate unchanged fresh confirmation.

One scalar run within120s, no retraining the vision model, no new capture,
no original MZ136 test records. CPU scalar filtering is TASK_NOT_GPU_SUITABLE.
Preserve models, transition counts, fold assignments, scores, seals and failures
under `artifacts.local/work/mz149-obstacle-state-20260916/`. No active allocation
remains after the process exits. Mechanical fixes do not authorize changes to
the fixed statistical method after scoring.
