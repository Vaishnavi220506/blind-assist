# 主体工作点：一次固定候选新布局验证（2026-09-20）

**候选没有通过本轮条件，停止这条工作点试探，不再调阈值。**
固定阈值在新布局把主体误报从128帧降至8帧（减少93.75%），但两个HEAD
事件晚报一个0.2s采样格，其中一个事件仅4/7正帧报警。12/12事件检出不能
掩盖这两项退化。历史Calibration继续保留，候选不替换基线或App默认。

[已有分数诊断](CORE_WORKPOINT_RESULTS_20260920.md)仍然成立：旧集的工作点
不是主体目标的合适唯一参照，现有分数确有显著降误报作用。本次失败证明
所选固定阈值没有满足新布局的首报与覆盖要求；它不证明所有工作点都不可能，
也不证明必须增加某种定位网络。本轮按事先约定结束，不用新标签继续挑阈值。

## 冻结与证据范围

[协议](CORE_WORKPOINT_TRANSFER_PROTOCOL_20260920.md)在采集前冻结：基线
0.007085703945147101、唯一候选 **0.4071309640537889**；未改变分数、
definite旁路、观测生成器、UNKNOWN或相机通道。训练、模型调用、RGB否决均为0。
全TP对照0.266037333仅用于旧集诊断，没有在新集追加测试。

新36完整布局/432帧，六种几何×三种关系×两背景，每clip12个0.2s采样点。
目标尺寸、高度、左右位置、相机路径与背景几何均改变，源ID和完整几何签名
无旧集完全重复。HEAD入界1.0s、每事件7正帧；BODY入界1.2s、每事件6正帧。
完整Core布局288帧是**78正/210负**，不是旧集72/216；BOUNDARY布局144帧
也是78正/66负。两臂始终在同一批新帧上比较。

Core布局仍采用完整INSIDE+OUTSIDE口径，其中4个正帧处于2cm深度边界带，
没有按结果删去。全三维几何分层为INSIDE74正、BOUNDARY82正+4负、OUTSIDE272负；
这些几何分层与layout分组不是同一概念。

源仍是同一Willow地图、立方体资产/材质和标称45度single-return代理。
这是新受控布局验证，不是新自然分布、硬件或真人效益验证。名义时间线为摆位
采样轨迹，不是实时动态采集；0.2s是采样分辨率，不是实测端到端时延。

## 主体和误报负期

| 新Core288（78正/210负） | Calibration | 固定候选 |
|---|---:|---:|
| TP / FP / FN | 77 / 128 / 1 | 73 / 8 / 5 |
| 精确率 | 37.56% | 90.12% |
| 帧召回率 | 98.72% | 93.59% |
| 负帧FPR | 60.95% | 3.81% |
| 误报段 | 26 | 7 |
| 误报采样时长 | 25.6s | 1.6s |
| 主体事件检出 | 12/12 | 12/12 |
| 原首次事件内提醒保留 | 12/12 | 10/12 |
| 入界时已持续报警 | 12/12 | 6/12 |
| UNKNOWN帧 | 246/288 | 246/288 |

| 负期（采样机会） | Calibration FP / 段 / 时长 | 固定候选 |
|---|---:|---:|
| OUTSIDE布局（144帧，28.8s） | 90 / 13 / 18.0s | 0 / 0 / 0s |
| INSIDE负期（66帧，13.2s） | 38 / 13 / 7.6s | 8 / 7 / 1.6s |

新基线128个主体FP也全部走分数通路，definite强制FP为0；候选剩余8个FP
同样没有definite支持。负期误报改善不是通过连接负帧缺口减少段数，而是
帧数、段数和采样时长同时降低。不过6/12事件仍提前持续报警，不能声称准确起报。
36clips两臂的clip-first时间已完整存入 `results.json.clip_first_s`。

## 四个新增漏帧，以及不能藏掉的事件覆盖

| 新丢失主体TP | 事件（省略workpoint前缀） | 时间 | 分数 | 代价 |
|---|---|---:|---:|---|
| f0005 | b0_head_horizontal_inside | 1.0s | 0.026336 | 首次提醒1.0→1.2s |
| f0008 | b0_head_horizontal_inside | 1.6s | 0.096610 | 正期中间静默 |
| f0227 | b1_head_horizontal_inside | 2.2s | 0.293970 | 正期尾部静默 |
| f0293 | b1_head_protruding_edge_inside | 1.0s | 0.244035 | 首次提醒1.0→1.2s；几何边界带正帧 |

基线自身还漏 `f0011 / b0_head_horizontal_inside / 2.2s`，分数0；
这不是候选新增漏帧。候选在该事件共有三处单格静默（1.0、1.6、2.2s），
只报警4/7正帧（57.14%），低于预先冻结的5/6覆盖要求。最长连续静默仍为
0.2s，所以仅看最大gap也会掩盖分散静默。两个其他受影响事件各报警6/7，
其余9个事件完全覆盖。候选所有5个FN及逐事件静默段均保留。

| 主体分组（TP/FP/FN） | Calibration | 固定候选 |
|---|---:|---:|
| BODY（36正/108负） | 36/67/0 | 36/7/0 |
| HEAD（42正/102负） | 41/61/1 | 37/1/5 |
| 背景0（39正/105负） | 38/63/1 | 36/3/3 |
| 背景1（39正/105负） | 39/65/0 | 37/5/2 |

BODY分组的好结果是披露的异质性，不用于事后改成BODY专用候选或给HEAD另设阈值。

## 边界成本、通过条件和处置

BOUNDARY布局从77/16/1变为6/1/72，事件从12/12变为3/12；完整432帧
从154/144/2变为79/9/77，事件24/24→15/24。边界覆盖损失已在验证前披露，
不能靠重新定义主体而从结果中消失。

固定条件逐项结果：

- 主体FP至少降25%：通过（93.75%）；总段数和两个负期各自FP/段数不增加：通过。
- 主体事件无新增漏事件：通过；首次事件内提醒全部保留：**失败（2个HEAD延后）**。
- 每事件至少5/6报警：**失败（1个HEAD仅4/7）**；最长静默不超过0.2s：通过。

这只约束本次固定候选，不是给整个项目永久增加零延迟条件。终端角色为
`NEGATIVE_CONTROL`：保留显著降误报与具体代价，关闭此固定候选的本次迁移角色。
不继续阈值扫描、不恢复RGB分支、不增加关联结构、不自动启动下一项算法。
“现有证据能何时报警、何时停止”现已得到部分回答：大量通道外误报可抑制，
但该统一工作点在新HEAD布局还不能兼顾及时首报和持续覆盖。

## 验证、资源与交付

432帧源认证通过：目标可见像素最少546，竞争通道像素最多0；地图/资产未改，
36clip代表帧接触表已目视检查。完整两臂预测封存后才与标签join；观测、预测、
评估三级seal及432身份绑定通过。捕获前独立审查补了观测seal递归校验、长度
断言和承诺的clip-first输出；保留修复前源码/协议和机械修复hash收据，
场景、阈值、科学条件没有改动，也没有重采集。

独立标准库复算未导入生产指标函数，核对432帧两臂决策、六种分组口径、
全部子组、逐事件首报/静默/预报警和72个clip-first值全部通过；
`independent-recount.json` 保留检查及输入hash。没有新增漏事件或超长gap，
失败确为两个首报延后与一个事件覆盖率不足。

主机使用已认证UE源环境，标量预测耗时1.585s/432帧（含文件读取），不作
设备时延或加速声明；CPU为`TASK_NOT_GPU_SUITABLE`。捕获进程树9个已知进程
全部退出，`released=true`、survivors为空；没有留存运行任务或付费分配。
源文件、观测、谱系、预测、评估和失败证据为本次持久结果，保留以供审计。

复现入口 `run_core_workpoint_transfer.py` 的四个阶段：freeze、materialize、
predict、evaluate；capture复用 `launch_core_transfer.py --spec ... --protocol ... --output ...`。
这些命令不允许覆盖已消费输出，不应作为再次验证此候选的入口。
证据目录 `artifacts.local/work/ba-core-workpoint-transfer-20260920/` 包含
`spec.json`、`protocol.json`、各阶段seal、`results.json`、`frame-results.json`、
`source-contact.png` 和捕获/释放/机械修复收据。

全局注册仍被既有ledger303 fingerprint错误阻止，继承CLI返回unknown-terminal。
原账本未被修写或绕过；保存两次CLI收据与版本化
`CORE_WORKPOINT_TRANSFER_DISPOSITION_20260920.json`，全局元数据仍待修复。
