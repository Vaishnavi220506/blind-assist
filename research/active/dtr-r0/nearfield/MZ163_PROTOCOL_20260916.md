# MZ163: fixed yaw scan under denser ToF integration

EXPLORE consumed simulator robustness diagnostic, 2026-09-16.

Question: does MZ155's extra rod episode coverage survive a denser numerical
integration of the same hypothetical 8x8 zone measurement? MZ155/MZ156 already
tested scan and static positive memory. This is a source-model falsifier, not a
new scan algorithm, confirmation cohort, or rescue of either frozen recipe.

Use all 288 saved MZ155 frames, native evaluations and capture spec. Admit the
analytic generator by reproducing every original 3x3 native hit/range and public
return first, including target-hit identity and returned contributor indices.
Failure stops the contrast as NOT_EVALUABLE. Compare only 3x3 and
12x12 subrays per zone at the exact original poses, objects and yaw trajectory.
Keep 8x8 public zones, two slots, 4 m range, normalized total signal weight,
0.01 signal floor, sigma 0.04 m and all histogram rules unchanged. This matches
frame/time/normalized signal budgets; no physical photon or firmware model exists.

Original recorded packet masks are held fixed across integration densities.
Because passive/scan packet masks differ, also compute a declared all-packets
available counterfactual at both densities. Return availability/status is
independent of Gaussian range noise in this forward model; the existing reducer
retains sigma but ranges are discarded in this diagnostic. Cross-density random
draws are not matched noise. No RGB decoding, learned prediction,
whole-pipeline alert rerun, sensor input replacement or App/default change.

Metrics: geometric target hits; target-containing returned slots (all statuses);
SIM_VALID target-containing returns; strongest-valid target return per zone;
current frames, causal-prefix frames, ever-observed episodes, first times, matched
gains/losses and delays. Report each family and in/out separately. Actor identity
is evaluator-only, and a target-containing return need not be target-exclusive.
Report normalized target-hit fraction and signal as diagnostics; do not compare
raw ray counts as sensor benefit. Missing returns remain UNKNOWN, not clear space.

Interpretation fixed before the new contrast: call rod scan coverage robust
within this model only if 12x12 preserves a positive rod ever-observed gain and
positive prefix gain, without losing any passive ever-observed episode in any
family, under both packet conditions. If the original rod gain vanishes, record
integration sensitivity. Mixed outcomes remain conditional. Even a pass is not
an alert gain or physical-sensor claim; 12x12 is not assumed converged truth.
The midpoint grids are not nested: changing density also changes phase. A dense
miss alone does not establish that a sparse hit is physically false.

One 600 s CPU diagnostic budget; scalar histogram/lineage work dominates and
uses the existing NumPy generator. No training, UE allocation, resolution sweep,
amplitude tuning or extra source. Mechanical fixes preserve failed receipts and
input identity. Save protocol/code/input hashes before analytic admission and
new density outcomes; finish with report, route navigation and scoped delivery.
