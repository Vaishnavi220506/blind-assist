# Deepen valuable observation mechanisms on sealed consumed traces

2026-09-22, EXPLORE. User authorizes continued investigation of valuable mechanisms.
This single comparison joins along-path information with continuous constraints,
tests whether one public opposing-witness pair can guide a useful path, and
separates two-step feedback from choosing a better fixed path. No re-opening of
the negative strict pure-quantization condition. No new renderer/source/noise,
network training, threshold/solver budget sweep or hardware claims.

## Input and evidence authority

Use all180scenes/90pairs from sealed ba-observation-mechanisms-20260922/run-v1,
including its original public forecasts, observations at53poses, and initial
continuous witnesses. This source is **already consumed Development**. All new
readouts and decisions are informed by preceding mechanism results; neither a
new-source nor independent confirmation claim is allowed. Hash-check old evidence
before/after. No new actual observations: selected traces are replayed from the
sealed pool. A hypothetical model forecast is not an actual observation.

## Frozen contrasts and new interfaces

1. Path-to-continuous constraints: extend public observation input from two
   endpoints to all13measurements on fixed+X12cm. Keep one-rectangle domain,
   quantization,1s/10000node per label budget and candidate validation unchanged.
   Before scoring, align the inherited ideal-real inequalities outward by the
   original simulator EPS=1e-12 at query/ray contacts; this is a derived numeric
   boundary correction, not fitted range tolerance or a scientific sweep. Original
   witness code stays untouched. Separate valid two-label witnesses, incomplete solve, and a
   one-label witness with solver-reported opposite infeasibility. Any model
   conditional label must have an explicit argument that the relaxed constraints
   contain the exact quantized model; numerical status is not a formal certificate
   or unrestricted free-space authority. See fixed path-constraint brief.
   Compare complete labels/UNKNOWN/correct+wrong conditional commitments with
   same endpoint model; report view2to13cost and opposing-witness prevalence.
2. Reason-guided path: from initial bins and their saved validated continuous IN
   andOUT witnesses only, forecast each witness along four13sample12cm spokes.
   Choose the path with earliest differing sample; then most differing sample
   positions; then original+X/-X/+Z/-Zorder. If no witness pair or no path split,
   use+X. Freeze choices before parsing actual future trace payloads. Never use
   actual source geometry, label, pair identity, source family or actual future
   views for choice. This tests one witness-pair heuristic, not a complete feasible
   set optimum. Compare its13view path with fixed+X13view under the same continuous
   inference; report all gains/losses, errors, UNKNOWN and finite-cohort information.
3. Two-step comparator: implement one public open-loop3view12cm plan from the
   original621hypothesis bank. Optimize the same lexicographic unresolved-positive,
   unresolved-total objective over16fixed6cm+6cm paths, joint midpoint/end bins,
   with original action order ties. Both moves chosen from initial bins. Compare
   to the unchanged prior public feedback policy and fixed+X/+X. Public plans are
   sealed before replayed actual future observations. Test whether feedback gives
   any real per-case advantage over a better initial fixed plan, not just over+X.
   Feed each selected3view trace into the same continuous readout as a separate
   representation contrast; do not attribute continuous-domain benefit to feedback.
4. UNKNOWN/actionability: distinguish genuine opposing compatible witnesses from
   solver incompleteness and model-conditional exclusion. Report whether the
   witness-guided path reduces ambiguity/costs on actual traces; do not merely
   claim that renamed UNKNOWN states increased recognition. Preserve valid old
   gains and newly wrong decisions, including consumed scene096,140,142,108,138.

## Decisions and budget

One scored saved-trace comparison after all code/briefs fixed. Continuous queries
are cached by camera+bins, with exactly two bounded label solves per distinct
query unless the solver brief explicitly requires separate feasibility evidence.
Five modalities: endpoint2, fixed+X13, witness-guided13, public feedback3 and
public open-loop3, all12cm motion. Duplicate public queries share results. The
same180truth denominator applies throughout; no class-specific abstention hiding.

Retain useful additional correct commitments with their FP/falseOUT/UNKNOWN and
cost, not only a binary gate. A stronger no-new-error integration claim requires
gainingTP, retaining baselineTP IDs, no newly wrong IDs and nondecreasing correct
decisive count under matched view/motion budgets. A lower aggregate error count
does not meet no-new-error if it introduces another wrong case. No error-driven
method retry, per-case routing or promoted App behavior follows this evaluation.
If exclusion soundness is not supported, retain UNKNOWN and constructive witness
analysis; that is an evidence gap, not permission to pretend infeasibility proves
absence. Domain-model-only predictions remain separate from validated sensor
alerts even if all simulated labels are correct.

Small scalar MILP/geometry is CPU TASK_NOT_GPU_SUITABLE. Artifacts under
artifacts.local/work/ba-observation-deepening-20260922/run-v1. Refuse overwrite;
preserve logs on mechanical error. Source observation files remain immutable.
Complete focused tests, independent trace/solver-output audits and scoped delivery;
use supported metadata commands, preserve existing ledger-block receipts. No
persistent resources, no automatic successor after this comparison.
