# MZ175: a common obstacle decision without a unique Radar identity

EXPLORE, consumed original TRAIN192 only, 2026-09-16. Keep fixed RGB640x360,
8x8 dual-return ToF, Radar and IMU. No training, capture, model download or App
change. MZ174 is closed; this is not a graph retune or MZ173 OR rescue.

## Hypothesis and contrast

MZ111 declines RGB extent use when one Radar return has multiple eligible
visual proposals. MZ114 seeks a common object identity across assignments.
Distinct hypothesis: some identities may remain ambiguous while every eligible
visible plane proxy gives the same corridor-overlap decision. An additive
task-consensus branch could recover missed alerts without new false alerts.

Use the authenticated MZ129 corrected proposal boxes, public integrated IMU
yaw and current Radar ranges. Reuse MZ111's angular eligibility and
`plane_inside` unchanged. Only unresolved returns (`proposal is None`) with
at least two eligible proposals enter the new branch. A unique association's
raw range must not replace its existing filtered decision.

Primary ALL requires every eligible proposal's proxy to overlap the corridor.
Comparator ANY requires at least one. Empty or singleton ambiguous sets do not
trigger either new branch. Missing Radar/IMU adds nothing. Final output is the
complete saved MZ129 flag OR the new branch from the outset. Original raw,
ToF, corrected Radar, guard and carry warnings remain independent and intact.
No weights, cutoffs, target selection, family exceptions or temporal memory.

`plane_inside` is a corner-AABB overlap proxy for a frontoparallel surface at
the current raw Radar range, not complete physical surface intersection or
containment. ALL is conditional on the visible proposal set: null, unseen,
outside-RGB and ghost explanations remain possible. No certified occupancy,
association identity, metric height or free-space claim follows unanimity.

## Access, measurement and decisions

Reuse the exact 192 IDs sealed by MZ173 and the authenticated original MZ136
incumbent cache. Decode only their public raw rows, corrected boxes and baseline
flags from the mixed 288-frame containers. Seal predictions before reading
MZ173's saved TRAIN-only truth/native-case summary and family annotations.
No original dev/test records, native evaluator JSONL or RGB pixels are decoded.
Hashing a parent container does not grant permission to decode other records.

Report baseline/ANY/ALL TP, FP, FN, TN, precision, recall, UNKNOWN, events and
first times, false segments, pressure/ordinary strata, each family, paired
binary decisions, exact gained/lost frames and returned native-ToF warning
retention. Native per-hit Radar lineage remains NOT_EVALUABLE. Input retention
and warning retention are separate. Preserve all unresolved hypotheses in logs.

The primary recall-component gate requires at least one additional true frame,
zero added false frames, no incumbent TP/event delay or native-supported
warning loss, and no family FP increase. This branch cannot reduce baseline FP.
If ALL and ANY coincide, any gain concerns relaxing unique identity, not a
demonstrated benefit of the unanimity rule. If the primary fails, stop this
fixed eligibility/geometry/consensus recipe before broader scoring. A pass is
only a consumed component lead, requiring separate unchanged confirmation.

One scalar CPU execution capped at120seconds, using the existing Python/NumPy
environment; TASK_NOT_GPU_SUITABLE for this small irregular Boolean/geometry
readout. Unit cases cover missing/empty evidence, mixed decisions, permutation
invariance, and preservation of existing support. No parameter/model selection.
Mechanical fixes retain failed logs and may repeat only the affected operation.
Save source/input hashes, predictions and independent saved-output audit under
`artifacts.local/work/mz175-radar-consensus-20260916/`. Release process resources.
Register via supported knowledge commands; do not bypass existing ledger303.

## Route screen

MZ139 already fits a slab by forward simulation, so merely naming inverse
rendering is not new. Joint multi-surface or temporal mixture inversion has a
different scope but needs an identifiability check for censored peak tuples.
MZ155 already tests matched yaw scanning; MZ163 removes its rod VALID-ever gain
under denser integration. No repeat scan follows. The retained route-screen
memos document these exclusions and the conditional boundary-mixture opening.
