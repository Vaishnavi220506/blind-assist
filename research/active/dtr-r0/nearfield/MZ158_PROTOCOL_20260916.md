# MZ158: fixed agreement on fresh scene configurations

EXPLORE controlled confirmation, 2026-09-16. Keep MZ129 and prior scoped gains.
Hypothesis: nuisance differs between a current geometry classifier and a raw
temporal-change classifier; requiring their unchanged causal alerts to agree
may reduce nuisance while preserving true warnings. They share inputs and
TRAIN192; this is model complementarity, not independent sensor corroboration.

## Discovery and fixed recipe

The combination was selected posthoc after saved-output inspection of MZ145,
MZ148 compensated and unregistered decisions on consumed dev48/MZ146288.
OR, AND and majority comparisons are diagnostic, not fresh tests. The selected
MZ145 AND unregistered gives dev24/3/0 and MZ146143/45/1, with20 false segments
versus MZ145143/57/1 and21. All143 MZ145 and130 MZ129 true flags and event times
survive. MZ146 family FP are rod21/boundary17/BODY1/HEAD6. This motivates one
new fixed-source check; the original recipes and failures remain unchanged.

Use exactly the original MZ143 `fused_hgb.pkl` and MZ148 `unregistered.pkl`.
Static feature inputs remain2485; the latter also receives the original3084
raw causal residual features at lags1,2,4. Each keeps its original MZ145-style
low/high rule, episode/gap reset and TRAIN-derived cutoffs. Candidate is the
Boolean AND of final causal flags, with no score mixing, gating thresholds,
training, model selection, sensor changes or family-specific rule. No-alert is
UNKNOWN. Retain all ToF slots, Radar slots and original independent measurements.
Do not claim AND preserves their individual votes; audit final suppression.

The raw-only residual adapter skips unused ECC work. Before fresh capture,
verify exact parity against the saved original unregistered feature cache and
reproduce both models' consumed predictions. Bind model/cutoff/source hashes
and this protocol in `recipe-freeze.json` before any capture dispatch.

## One fresh source

Use unchanged MZ136 procedural family generator with seed158016, renamed MZ158,
all288 frames designated confirmation:24 scene configurations,48 six-frame
episodes at.25s,144 positive and144 negative source-design frames. Require
background geometry signatures disjoint from original136014 and146016 sources.
This is a new configuration sample within the same families, renderer and
sensor model, not a new distribution or natural/hardware evidence. Do not reject
or replace scenes based on rendering/model outcomes. Original test records stay
unopened; native admission is separate from public prediction.

Keep one640x360 RGB,8x8 two-return ToF,Radar andIMU. Reuse the unchanged inherited
capture AST and renderer/sensor contracts. One worker capture attempt,1200s cap;
no automatic restart. Resume the same job on observation timeout. Release only
owned processes/cache after identity/path checks; retain raw payload and receipts.

## Prediction, decision and stop

Freeze the recipe before source generation/capture. Hash and admit returned
inputs, infer MZ129 and both unchanged learned arms from public observations,
then seal all predictions before evaluator scoring. No training, threshold
search, candidate/source revision or late rescue is allowed. Record real CPU
OpenCV/sklearn execution (`GPU_BACKEND_UNAVAILABLE`), feature/readout time and
sampling limitations; end-to-end Android latency is not inferred.

On all288 fresh frames require: at least30% fewer FP than MZ129 and10% fewer
than MZ145; improved precision over each; every true frame of both comparators
retained; every comparator-detected event retained with no later onset; no
family FP count above either comparator; no increase in false-alert segments
against either; and no candidate nonalert with a returned native ToF corridor
contributor. Report TP/FP/FN and denominators, precision/recall, ordinary/pressure
strata, every family, paired decisions, events/onsets, false segments/sample-bin
duration, UNKNOWN, changed support and final flags together. Native Radar lineage
remains NOT_EVALUABLE under this source contract. Returned-point retention does
not establish full-object or unsampled-surface coverage.

If all conditions pass, retain a fresh same-generator controlled challenger and
complete scoped integration/documentation; no App/default/device/safety promotion.
If any fails, close this fixed agreement recipe with all raw results and the
triggering failures; no family routing, padding, added OR or threshold tuning.
The broader improvement goal remains active. Local evaluation cap600s, one run;
mechanical execution fixes may preserve identities and be logged separately.
