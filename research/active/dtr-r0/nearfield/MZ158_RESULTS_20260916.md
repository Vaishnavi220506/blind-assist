# MZ158: current geometry and raw temporal change must agree

EXPLORE controlled confirmation, 2026-09-16.
[Protocol](MZ158_PROTOCOL_20260916.md).

Decision: **FRESH_CONTROLLED_AGREEMENT_NOT_MET**. On 288 new same-generator
frames, fixed agreement improves MZ129's TP / FP / FN from 133 / 80 / 11 to
140 / 49 / 4, preserving every incumbent true frame and event time. However,
suspended-head FP increase from 7 to 16. This is the sole failed condition among
13 declared checks. Retain the measured partial improvement, close this exact
recipe as a negative control, and keep MZ129 as the default research baseline.
No tuning, family selector, added OR, default-App promotion or successor run.

## Fresh result and failure boundary

Each arm sees the same 288 frames: 144 positive and 144 negative, with 36 of
each per family. Counts below are frame decisions, not independent user trials.

| Arm | TP / FP / FN / TN | Precision | Recall | UNKNOWN | False segments |
| --- | ---: | ---: | ---: | ---: | ---: |
| MZ129 | 133 / 80 / 11 / 64 | 62.44% | 92.36% | 75 | 31 |
| MZ145 static | 140 / 66 / 4 / 78 | 67.96% | 97.22% | 82 | 27 |
| Raw-change control | 143 / 61 / 1 / 83 | 70.10% | 99.31% | 84 | 25 |
| Fixed agreement | **140 / 49 / 4 / 95** | **74.07%** | **97.22%** | **99** | **23** |

Agreement reduces FP by 38.75% against MZ129 and 25.76% against MZ145. All 133
MZ129 and 140 MZ145 true frames survive; all 30 events remain detected with
identical first-warning times. Two boundary events retain a 0.5 s onset delay;
the remaining 28 have zero measured delay. False-alert sampled-bin duration
falls from 20.0 s to 12.25 s (MZ145: 16.5 s). These totals use the source's
4 Hz cadence and do not measure real-user interruption duration.

| Family | MZ129 TP / FP / FN | MZ145 TP / FP / FN | Agreement TP / FP / FN |
| --- | ---: | ---: | ---: |
| Near rod / far wall | 34 / 27 / 2 | 36 / 18 / 0 | 36 / 10 / 0 |
| Shallow boundary pressure | 28 / 26 / 8 | 32 / 20 / 4 | 32 / 18 / 4 |
| Substantial BODY | 36 / 20 / 0 | 36 / 7 / 0 | 36 / 5 / 0 |
| Suspended HEAD | 35 / 7 / 1 | 36 / 21 / 0 | **36 / 16 / 0** |

Ordinary frames (216) change from MZ129 105 / 54 / 3 to 108 / 31 / 0; MZ145
is 108 / 46 / 0. Boundary-pressure frames (72) change from 28 / 26 / 8 to
32 / 18 / 4; MZ145 is 32 / 20 / 4. Agreement removes 52 baseline false frames
but introduces 21 others: HEAD alone removes 2 and introduces 11. All four
remaining misses are boundary frames shared with the static branch. The raw
control detects three of those four; it remains a disclosed control and is
not retrospectively selected as a replacement.

Correct positive/negative paired decisions rise from 57 / 144 to 91 / 144
(MZ145: 74 / 144). This compares binary flags, with ties, rather than calibrated
probability ranking. All nonalerts remain UNKNOWN: its increase from 75 to 99
is not a claim of clear space or calibrated abstention.

Native audit covers 27,162 returned ToF contributor samples, including 4,944
corridor samples. No agreement nonalert contains a returned corridor contributor.
All original public sensor slots remain available. This establishes sampled
native retention, not complete object/surface or RGB-external coverage. Native
per-return Radar lineage remains NOT_EVALUABLE under the source contract.

The only failed check is `baseline_all_family_fp_noninferior`: HEAD FP 7 to 16.
Rod nuisance, the prior MZ145 weakness, improves here, while HEAD nuisance does
not generalize sufficiently. The aggregate benefit therefore does not establish
uniformly better warnings. Freeze and stop this exact readout; no posthoc family
routing, padding, threshold changes or OR rescue.

## Posthoc discovery, separate from fresh confirmation

An authenticated saved-output comparison of MZ145 and both MZ148 readouts finds
useful complementarity between MZ145 and the unregistered temporal arm. Their
AND preserves every MZ145 and MZ129 true frame in consumed dev48 and MZ146288.
The old panels were already inspected before this combination was chosen; they
are Development selection evidence, not a new independent validation.

| Consumed panel | Readout | TP / FP / FN | False segments |
| --- | --- | ---: | ---: |
| dev48 | MZ145 | 24 / 3 / 0 | 2 |
| dev48 | MZ145 AND unregistered | 24 / 3 / 0 | 2 |
| MZ146288 | MZ129 | 130 / 86 / 14 | 28 |
| MZ146288 | MZ145 | 143 / 57 / 1 | 21 |
| MZ146288 | MZ145 AND unregistered | 143 / 45 / 1 | 20 |
| MZ146288 | Three-arm majority | 143 / 51 / 1 | 24 |

The selected AND's MZ146 precision rises from MZ145's71.50% to76.06%, with
99.31% recall unchanged. Family FP are rod21, boundary17, BODY1 andHEAD6;
all143 MZ145 and130 MZ129 true frames and event times survive. The45 remaining
false frames and one miss are shared errors; this combination cannot repair
them. Majority increases nuisance fragmentation. MZ145 OR compensated gives
143/59/1; their AND gives142/41/2, losing the boundary frame. No earlier failed
recipe is repaired, retuned or retrospectively promoted.

This discovery audit verifies183 hashes without RGB, raw/native-record decode,
model inference or fitting. Saved native counts show no candidate nonalert with
a returned corridor contributor: MZ146 has27872 returned/4751 corridor samples,
dev4187/1046. They are sampled evidence, not full-surface coverage. The complete
diagnostic/script remain under the sibling
`artifacts.local/work/mz158-decision-complementarity-20260916/review/` tree.

## Fixed implementation

The static branch is the unchanged original MZ143 histogram-boosting model with
2485 public sensor/RGB geometry features and MZ145's original causal rule.
The second branch is MZ148's unchanged unregistered model, receiving those same
2485 features plus3084 raw image-change summaries at causal lags1,2,4. Each
keeps its original TRAIN192-derived low/high cutoffs. Candidate is the AND of
their final causal flags; no mixed-score threshold, confidence selector,
per-family rule, retraining or new hardware is introduced.

Both models share inputs and training. Agreement does not constitute two
independent sensor detections or calibrated confidence. All original ToF and
Radar slots remain available; this is a final alert change, not support pruning.
AND can suppress a sensor-supported warning in a new scene, so native contributor
retention and final true-frame/event retention must be measured again. Nonalerts
remain UNKNOWN, not proof that the corridor is clear.

`RawResidual` executes the exact original unregistered path without computing
unused ECC homographies. All740160 cached values across TRAIN192 anddev48 match
bitwise; both frozen models' dev scores reproduce exactly. Two focused tests
cover adapter parity, causality, episode/gap reset, metadata invariance and
AND-after-causal-readout behavior. Two pre-freeze preparation failures corrected
a historical seal filename and a Path/string hash-helper mismatch; neither
changed models, features, cutoffs or any fresh outcome.

Before capture, `recipe-freeze.json` binds models, cutoffs, protocol, dependencies
and consumed parity evidence. A separate execution freeze binds44 local runner/
evaluator sources plus snapshots before fresh model inference or outcome access.
Independent code review verifies both-comparator gate logic and closes the
prediction-to-input hash binding before execution. It runs no fresh predictor.

## Fresh source and resource ownership

Seed158016 generates24 new same-family scene configurations,48 six-frame
episodes and288 frames. Background geometry signatures are disjoint from both
136014 and146016. Inputs remain one640x360 RGB,8x8 two-return ToF,Radar andIMU.
The family priors, renderer and sensor model are reused; this is not natural-
distribution or hardware validation. Original test records remain unopened.

One worker capture uses the inherited capture AST with a separately recorded
mechanical Zen-port adapter selecting the existing20000-29999 allocator. Renderer
and sensor logic stay unchanged. Capture gets one attempt and1200s; local
evaluation600s. Worker/controller paths and ownership receipts remain in the
ignored artifact tree, not in this tracked report.

The single capture completes in 434.125 s after startup; owned-process lifetime
is 921.134 s, below the 1200 s cap. Controller admission verifies 319 returned
file hashes, 50 source/bundle bindings, 288 RGB decodes and 288 native/source
agreements (144 positive / 144 negative). Maximum geometry discrepancy is
8.88e-16 m. There are no retries or outcome-based source exclusions.

All 22 tracked worker processes exited naturally, port 29123 has no listener,
and the owned scheduled task is absent. The source agent removed 454,907,862
logical bytes of task-owned cache after path/ownership checks. Raw capture,
transfer archive, logs, hashes and receipts remain as MZ158 evidence; they are
owned by this experiment for a later explicit evidence-lifecycle decision.

## Execution and validation

Fresh public predictions are sealed before scoring. Baseline inference uses
the original OpenCV 5 environment and takes 29.369 s. Both learned branches use
OpenCV 4.10 / sklearn 1.9 CPU execution and take 37.636 s, including 33.462 s
feature extraction and 1.546 s readout. Scoring takes 1.645 s. These are desktop
batch timings, not Android end-to-end latency. The implemented NumPy/OpenCV/HGB
path records GPU_BACKEND_UNAVAILABLE; no CUDA inference is claimed.

Both focused adapter/readout tests pass; the runner compiles and all three
execution modes exit successfully. Recipe, execution code, source, public
inputs, feature cache, predictions and completion records retain authenticated
hash bindings. Original test records remain unopened; no model or cutoff is
changed. Independent audit passes 1,418 hash checks over 732 unique files,
recomputes all 288 decisions and 576 causal-prefix checks, and independently
reproduces confusion, family/stratum counts, pairs, events, UNKNOWN and all 13
conditions without invoking the production scorer or models. It decodes all
288 native records and matches the contributor counts above. Audit PASS means
the reported failed experiment is reproducible, not that its joint gate passes.

## Evidence and metadata

Primary artifact tree: `artifacts.local/work/mz158-crossview-agreement-20260916/`.
`recipe-freeze.json`, `execution-code-freeze.json`, source receipts, both
prediction seals, `evaluation-v1/summary.json`, `cases.json` and native
contributors preserve the complete measured result. Capture/source completion
is in `source/source-completion.json`; release is in
`source/capture-final-release.json` and `source/port-release-verification.json`.

Intended terminal: `terminal-dtr-mz158-crossview-agreement`; intended role:
NEGATIVE_CONTROL for this fixed joint recipe. The saved independent audit is
`review/independent-audit.json`. The broader improvement goal remains active,
without changing this failed confirmation's result.

Supported `register-experiment` and `set-terminal-inheritance` both exit 2:
the pre-existing ledger line 303 input_fingerprint mismatch prevents registration,
then inheritance reports an unknown terminal. Both ledger/inheritance hashes
remain unchanged. Structured registration and inheritance are therefore PENDING,
not complete; `decision.json` retains the intended role, commands and evidence
hashes. This metadata defect is not bypassed or repaired by this experiment.
