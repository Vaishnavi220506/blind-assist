# MZ182: fixed three-phase dither ceiling misses the required gain

Terminal `MZ182_DITHER_CEILING_GATE_NOT_MET`; intended NEGATIVE_CONTROL for
this fixed source/three-phase/object-association recipe. New native UE collision
Development observations, not historical1920-frame performance or device evidence.

| Arm | TP | FP | FN | TN | UNKNOWN | Removed baseline FP |
|---|---:|---:|---:|---:|---:|---:|
| single yaw0 | 12 | 12 | 0 | 0 | 0 | 0/12 |
| repeated yaw0,3 samples | 12 | 12 | 0 | 0 | 0 | 0/12 |
| dither0,+quarter zone,-quarter zone | 12 | 8 | 0 | 4 | 0 | 4/12 |

The [prospective protocol](MZ182_PROTOCOL_20260918.md) freezes24 matched scenes
and144 observations. Zone width5.625deg yields yaw0,+1.40625,-1.40625deg.
No scene/camera translation; exact known yaw and no packet loss are ideal ceiling
assumptions, not measured IMU behavior.12/12 recoverable TP survive, but33.3%
FP reduction fails the >=50% requirement. Repeated observations alone provide
no gain in this matched control. Stop: no half-zone phase additions, retuning,
public association training or automatic successor.

This is a ToF-only horizontal localization ceiling with tall stationary targets,
not an A*/Radar replacement or full3D warning evaluation. Readout consumes public
zone/range values plus evaluator-returned actor IDs only. It unions all same-actor
slots within a phase, then intersects phase angular unions; it does not pretend
distinct surface returns are one point. The full range hull remains conservative.
Absent phase/empty intersection would be UNKNOWN with baseline fallback, so
missing evidence cannot earn FP reduction. All24 scenes had valid associations.

All24 estimated angular sets overlap the native object's angular span. Full
native angular extent is not contained in6 single/repeat cases and18 dither
cases. This is a localization proxy, not surface coverage recovery; perfect
object identity is not perfect surface-point registration. Failure closes only
this recipe, not information-theoretically all motion-induced super-resolution.
Current sensor/interface limitations and unverified ST capabilities are in
[sensor contract](SENSOR_OBSERVABLE_CONTRACT_20260918.md).

## Source, verification and retained resources

UE5.8.2 native first-hit collision traces, NullRHI,64 zones x9 subrays per view;
inherited hypothetical noise/reflectance/merged/two-target model. One fixed source
run144/144 complete. First process failed before any frame at missing Python
module import; an explicit local module path repaired startup without changing
source scenes or sensor rules. Failure logs and both process-release receipts
are retained. This is mechanical recovery, not fresh source replacement.

Local evidence: artifacts.local/work/tof-dither-20260918/ retains raw/evaluator
payloads, source receipt/hash manifests, identity-only association, oracle
predictions, complete24-case evaluation, protocol, source-failure logs and process
release. Worker durable copy is BLINDASSIST_ARTIFACTS/work/mz182-dither-20260918,
owned by this research task for reproduction. No worker process/paid allocation
is retained. CPU scalar collision/metadata: TASK_NOT_GPU_SUITABLE. No RGB rendering,
model fit, source recapture after outcomes or default-App change occurred.

Registration/inheritance attempted via knowledge CLI; preserve failed receipts
and do not rewrite existing ledger. Source/outcome evidence is controlled and
idealized, not natural/hardware/safety evidence. Literature links supplied by
the user were motivation only, not independently inspected publications.

Three focused set/geometry/UNKNOWN tests pass. Native actor bounds and all
returned hit-point positions match frozen source on144 observations; process
release reports no survivors. Registration fails on existing ledger303 input
fingerprint; inheritance remains unknown-terminal pending.
