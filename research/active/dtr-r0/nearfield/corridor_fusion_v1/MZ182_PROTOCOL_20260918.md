# MZ182 oracle-associated angular dither ceiling

New user authorization reopens neither MZ179 persistence nor MZ180 decomposition
nor MZ181 same-frame subdivision. EXPLORE new controlled native UE collision
observations. Question: do changed angular sampling phases supply useful bearing
constraints under perfect object association? No model training/A* integration.

Freeze24 matched scenes: forward centers1.4/2.2/3.0m; object lateral widths
0.10/0.40m; left/right; inside/outside nearest lateral face0.25/0.35m. Depth0.10m,
height2.2m centered z1.1; camera fixed(0,0,1.7), pitch/roll0. No environment
occlusion. Same geometry across3 yaw phases0,+zone_width/4,-zone_width/4, where
zone_width=45/8=5.625deg from existing zonal model. Paired repeated-zero arm has
the same3 observations and matched per-phase RNG seeds. Total144 observations.
Native collision engine, not analytic ray-box fabricated as UE. No RGB required
for this ToF-only ceiling: NullRHI native ray capture; no A*/Radar changes.
One fixed source; no outcome-based resizing, phase changes or scene replacement.

Keep inherited9subrays/zone, range noise/strength/merged/status/2-target model.
All packets available and exact commanded yaw assumed (ideal IMU ceiling),
explicitly different from MZ177 packet/IMU errors. Do not call this measured IMU
or hardware super-resolution. All64 zones captured, one sensor grid per view.

Oracle uses only returned-slot actor identity; no native point/bearing/range or
actor extent in readout. For each phase union same-actor SIM_VALID slot bearing
intervals after yaw rotation; across phases intersect these unions, preserving
disjoint components. Do NOT intersect all same-actor slots individually. Tall
targets cross camera height; primary diagnostic is horizontal localization,
not a general three-dimensional alert system. Returned slant range +/-3sigma
and full zone angles give conservative horizontal radius intervals; use their
union/hull for the object's possible range without faking same-point identity.
This is perfect object association, not perfect surface-point registration.

Single0 baseline, repeated0x3 and dither3 share the same readout. Missing valid
target support in ANY phase or empty cross-phase support is UNKNOWN and falls
back to single0 alert for gate accounting; it is never a successful FP removal.
Report all24 scene outcomes, baseline-detectable TP retention, baseline FP
removal, absolute TP/FN/FP/TN/UNKNOWN, association coverage, localization width,
and matched repeated-view differences. If no baseline FP opportunities or no
recoverable TP, NOT_EVALUABLE, not success. Main gate >=50% of single0 mechanism
FP removed and >=95% single0 TP retained, with UNKNOWN fallback enforced.
Report source true-bearing retention separately: object identity does not
guarantee same point across phases. Any loss makes localization authority
conditional. Do not tune on these scenes. Stop after ceiling; public association
or histogram experiments require a separately scoped continuation.

UE collision/metadata is scalar CPU (TASK_NOT_GPU_SUITABLE); no GPU model.
Worker allocation maximum180s native-only run after startup; retain terminal
failure as source failure, not algorithm result. Release owned UE process tree.
Source protocol and code hashes fixed before capture/evaluation. Official ST
capability review is separate; no multi-target/CNH default change in this run.
