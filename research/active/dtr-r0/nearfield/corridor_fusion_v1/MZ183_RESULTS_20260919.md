# MZ183: visible angular ownership ceiling is not sufficient

Terminal: `MZ183_VISIBLE_OWNERSHIP_GATE_NOT_MET`.
This is a consumed controlled Development ceiling over the sealed MZ177 source,
not a fresh confirmation, hardware, natural-scene or safety result. The fixed
gate was to remove at least24 of the32 ToF-only MZ181 false positives while
retaining at least135 of142 MZ181 extra TP.

| Arm | TP | FP | FN | Extra TP over A* | Extra FP over A* |
|---|---:|---:|---:|---:|---:|
| A* | 1086 | 59 | 298 | 0 | 0 |
| MZ181 angular support | 1228 | 98 | 156 | 142 | 39 |
| MZ183 visible ownership | 1228 | 95 | 156 | 142 | 36 |

Visible ownership removes **3/32 ToF-only FP (9.4%)** and **3/39 total MZ181
extra FP (7.7%)**. All142 extra TP remain, all baseline A* alerts remain, and
all12 advance-contact events remain. The 7 Radar-only MZ181 FP remain unchanged.
MZ183 therefore fails its ceiling gate and closes this fixed ownership recipe;
do not train a segmentation head, tune mask geometry, or start MZ184.

## What was tested

The saved source has no instance-ID render. The oracle mask is a deterministic
reconstruction from the authenticated controlled scene cubes, camera pose and
nearest-hit visibility, including the noncolliding albedo tiles. It is explicitly
`RECONSTRUCTED_RENDER_MESH_PIXEL_VISIBILITY_NOT_CAPTURED_MASK`, not a projected
box, native depth image, or captured RGB segmentation. Evaluator actor identity
is used only to associate returned ToF slots; the readout receives the ownership
mask, public distance/3sigma range, public IMU/extrinsics and unchanged zone.

Only SIM_VALID ToF supports are restricted by the visible mask. Merged remains
UNKNOWN. Independent support outside the RGB image is retained. Empty/unknown
mask coverage falls back to the MZ181 support, so abstention cannot manufacture
an FP reduction. A* and Radar remain OR-independent. No true hit point or native
depth enters the mask-constrained readout.

The complete MZ181 extra set is preserved:142/142 TP and no TP loss. Of39 MZ181
extra FP, the pre-audit strata are32 valid-ToF-only and7 Radar-only; MZ183 clears
three valid-ToF-only frames. The prediction audit retains full frame IDs and
sequence timing. Two body side-pass sequences lose alert frames (23->21 and
16->15), with no first-risk-alert change; this is false-alert duration, not an
earlier-warning gain.

## Coverage, UNKNOWN and contributor limits

The ownership pass examines 1,920 reconstructed masks. Of12,607 valid-ToF
contributors considered by MZ181,12,537 remain possible after ownership and70
are rejected. 2,289 supports are outside the RGB image and are retained by the
independent branch;10,248 use a mask-constrained possible check. All mask
rejections are a readout diagnostic, not proof that a physical return was
wrong. The unchanged Radar branch contributes the seven residual Radar-only
false alerts. UNKNOWN remains distinct from clear; no mask absence certifies
free space.

Because the mask is reconstructed from this simple axis-aligned controlled scene,
the result is a capability ceiling conditional on that reconstruction. It does
not establish that a real RGB segmentation or edge model can reach this result,
and the weak gain closes only this implemented mask/readout recipe.
Captured-mask visual validation was not completed. Rectangle compression still
uses interval enclosures, so this is not a strict exact-intersection ceiling.
Do not infer that RGB angular ownership or all available observables are exhausted.

## Verification and evidence

Prediction sealing completed before outcome timeline access (`task_truth_read=false`).
Input/output hashes, 1,920 mask NPZ files, ownership associations and public
predictions are under `artifacts.local/work/mz183-visible-ownership-20260918/`.
The MZ181 sealed input, raw/evaluator source hashes, ordered IDs and completion
receipts are checked. CPU NumPy rasterization was used because the selected local
Python has no Torch; this is `GPU_BACKEND_UNAVAILABLE`, not a GPU speed claim.
No training, capture or worker allocation was started. Existing task-owned
artifacts remain for reproduction; no cleanup removed shared evidence.

ST contract correction remains separate. The user supplied official UM3109 and
VL53L8CH references for CX four-target output/default-one/approximately-600mm
separation and CH CNH 64-zone/128-bin capability with bandwidth examples. Local
HTTP requests in this session returned vendor verification HTML, so those exact
figures are recorded as user-provided official-source claims rather than newly
independently inspected bytes. No sensor default or historical contract changed.

Registration and terminal inheritance remain pending the pre-existing ledger303
fingerprint mismatch and unknown-terminal errors. Their receipts are retained;
the ledger was not bypassed or rewritten.
