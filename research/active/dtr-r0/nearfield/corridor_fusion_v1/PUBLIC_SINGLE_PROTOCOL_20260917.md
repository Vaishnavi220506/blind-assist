# One public positive head, one threshold, one frozen configuration confirmation

User-directed continuation, EXPLORE followed by prospective controlled same-source
confirmation. Keep ordinary return BCE, the1537-parameter MLP and frozen A.
Produce one model/threshold pair, a same-data retrained-A comparator and one new
complete source. No model search, peak loss, depth encoder, sensor upgrade or
false-alert suppression branch. Existing v2 remains consumed Development.

## Single fitting and calibration recipe

Use authenticated1344 v2 frames, including anchor192 and four complete288
cohorts. Original MZ136 dev48/test48 excluded. Single head: identical30->32->16->1
ReLU, fit-only standardization/std floor.01, balanced return BCE, AdamW.001,
weight decay.0001, batch16,120epochs, last checkpoint, fixed seed185017.
Fit all1344, supervised only on known actual ToF returns. No frame-positive
label broadcast or missing-return negative assertion.

Reuse the six v2 BCE final models'1152 outer-held predictions as pooled OOF
calibration. Each frame was excluded by scene index from its own predictor's
fit/normalization. Those exact OOF scores, not fitting predictions from the new
full-data head, select one threshold by final(A OR head)5cm-clear F1, then fewer
FP, then higher threshold. Candidate inactive value is finite nextafter(max
calibration logit) and inactive on those samples only. Do not select a fold,
average thresholds or tune the final head on its in-sample replay. Weight/input
and threshold seals are written before new capture dispatch or outcome access.
Inner-to-full-refit score transfer remains a substantive question for confirmation.
Old OOF outcomes have been inspected, so pooled selection is Development;
the new source is the prospective check. Fixed0 may be retained as an existing
Development control, not selected on new results.

Same-data A control: inherited HGB hyperparameters max_leaf_nodes7,max_iter150,
l2=1,learning_rate.05,min_samples_leaf8,early_stopping=False,seed185017, no
sample/class weights; strict frame labels. Six scene-index OOF fits each use
anchor+the other five indices. Select its standalone clear F1 threshold on1152
OOF records (same ties), then fit one A* on all1344. Same public2485 fields;
authenticate cached columns/order. No tuning the model budget. A* is a separate
data-amount control; original A remains the OR base. Per-return labels provide
richer supervision than strict frame labels, a disclosed methodological difference.

## A uniform public inference path

One resident frozen A, one resident full-data head and their fixed thresholds
serve all episodes; no cohort/scene routing or evaluator import. Compute A RGB/
ToF/Radar features, compute public ToF token+geometry, take max valid-return logit
and OR with A. Zero usable ToF preserves A. Causal IMU yaw is reset at episode
start. Keep model loading separate from per-frame CPU replay timings. The A*
control reuses the identical A feature vector but does not change the main path.

Warm the whole inference path on previously consumed public data, not selective
new frames. Time RGB read/decode + A feature extraction/HGB + public head for all
new frames; also report matched A prefix, incremental branch, A* cost and observed
max. Measurement is host full-algorithm replay, excluding actual sensor capture,
transport and hardware/user latency. No dropout of slow new frames.

## Frozen complete confirmation source

Before observations, fix seed186017, four inherited obstacle families with six
new geometric configurations each:24groups,48paired six-frame4Hz episodes,
288frames. Change HEAD height/size/lateral location, rod width/location, wall
front distance/texture and full/portal/recessed/staggered background topology.
Paired in/out or enter/exit changes lateral target position only. Include all
scene objects in labels. Source geometry fixes144positive/144negative strict
frames,216clear frames and72boundary-pressure frames; no selection from A scores,
native sensor hits or model success. Verify complete trajectory/scene signatures
do not duplicate existing data. Same renderer/sensor simulation is an explicit
claim limit. No silent expansion to force supported misses or favourable metrics.

Worker capture uses the proven timeout=None process-wait adapter. Investigate
actual failures or stalls, never abort healthy progress at an arbitrary elapsed
time. If a mechanical retry is required, preserve receipts and exact source/
model identities; do not regenerate configurations. Capture/model/source freeze
must precede dispatch. Read only public records during prediction; seal all A,
A+head,A* predictions/timings before native evaluator scoring. Geometry admission
and scoring cannot feed inference or alter selection.

## Reporting and decision

Main table A/A+head/A*: strict and5cm clear TP/FP/FN/P/R/F1, coverage, boundary,
family and whole configuration counts. Report A misses with actual returned
sampled support, misses rescued, new FP (frames and episodes), selected maximum
return witness, missing-measurement misses, zero-return fallback, event retention,
first observed onset, negative alert burden and full replay latency. A* may lose
A TP or delay events; report these costs, not just aggregate F1. Do not interpret
left-censored responses as advance warning. No requirement of zero new FP across
all strata: meaningful recall/F1 benefit with a disclosed cost remains retainable.

Useful aggregate F1 improvement with rescued supported misses retains the frozen
component within this controlled scope. A mixed result is preserved with all
FP/pressure/episode costs; superiority to A* must be measured, not assumed.
No available supported A miss means recall contribution is not tested, while
false activation remains evaluable. A clear negative closes this exact single
recipe's confirmation; do not tune it on new data or automatically change route.
One completed source and fixed comparison end the experiment; finish model
bundle, independent audit, report, scoped commit/push and task-owned cleanup.
No App promotion, natural/user/hardware safety claim or ledger bypass.
