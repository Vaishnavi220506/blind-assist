# MZ180 corrected result and public-input feasibility check

Terminal: `MZ180_PUBLIC_DECOMPOSITION_NOT_ESTABLISHED`.
Consumed controlled Development, 1,920 frames; no fresh confirmation or promotion.

| Arm | Retained original 103 | Full TP increment over A* | New FP | Total TP/FP/FN |
|---|---:|---:|---:|---|
| A* | 0 | 0 | 0 | 1086/59/298 |
| Broad | 103 | 147 | 103 | 1233/162/151 |
| Historical privileged range proxy | 98 | 142 | 66 | 1228/125/156 |
| Public merged UNKNOWN | 98 | 142 | 44 | 1228/103/156 |

Public-only predictions are sealed before reading saved outcome timelines. They
match historical ambiguous-arm alerts on every frame. Both 98-frame sets are
identical, as are the complete true-alert sets and first risk-alert times across
all 24 sequences. The privileged proxy adds zero TP and 22 FP versus UNKNOWN.
All A* alerts remain by OR construction; event retention is not independent
proof of accuracy or earlier warnings. Source onset/release censoring remains.
Per-frame IDs and complete timing/transition counts are retained in audit.json.

All 835 public merged slots contain only distance_m, range_noise_sigma_m,
signal_strength_proxy and status. All 16,177 valid slots remain eligible under
the inherited packet, finite-value, strength and IMU checks. No zone in this
source has multiple returned slots; a synthetic mixed-zone check verifies that
adding an ambiguous slot does not remove its clean neighbor or Radar branch.
No internal peak, component depth or lineage is available in the public slots.
Neighborhood/RGB inference would require a new justified estimator, not native
component access. No such estimator was selected or run. Close this scoped
public decomposition recipe: current public inputs have not established a
benefit over UNKNOWN. This does not disprove all multi-hypothesis methods.

## Corrections to the previous report

The old union is NOT a verified nonconvex native-support union: it centers
private_hit_span/2 on pre_noise_range, which is not necessarily the true range
extrema, and this source contained no multi-component zones in the earlier
inspection. Reduced uncertainty span is not proof of multimodality preservation.
The old oracle removed Radar and used only nonempty zone lineage as a filter;
it did not select true source surfaces. Its original 66/103 claim was wrong:
the verified count is 89/103 (total 1196/119/188). Exclude this confounded arm
from ceiling conclusions. Old artifacts remain unchanged for traceability.
The historical scripts are retained only to reproduce those flawed diagnostics.

The support-envelope replacement and UNKNOWN policy reduce broad FP, but do
not establish that convexification between observed modes caused the reduction.
The x/y/z interval enclosure is still an overapproximation; no residual-FP
causal attribution was performed. Additive merged rescue cannot remove the
44 new FP already present in UNKNOWN. No main-algorithm replacement is claimed.

Evidence: artifacts.local/work/mz180-public-check-20260918/ contains public
predictions, input/output hashes, public field census, exact paired IDs and
sequence timing. mz180_public_check.py is the reproducible entry. CPU scalar
accounting (TASK_NOT_GPU_SUITABLE); no training, capture, threshold search or
paid worker. Retain only this public UNKNOWN implementation and corrected
historical diagnostic evidence; no automatic successor.
