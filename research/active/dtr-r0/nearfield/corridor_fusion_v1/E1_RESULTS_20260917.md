# E0/E1: frozen local depth statistics do not improve corridor HGB

2026-09-17, user-requested EXPLORE, consumed controlled Development.

**E1_DEPTH_STATISTICS_NO_INCREMENT.** On the same MZ170288 report frames,
adding 924 frozen DA-V2-Small relative-depth features changes the matched
retrained HGB from137TP/28FP/7FN to135/32/9. F1 falls88.67% to86.82%, ranking
PR-AUC .9615 to .9266, and complete inference p50 rises78.2 to303.8ms.
Close this particular pooled-statistics integration as a negative control.
Do not advance to E2 on this evidence. This does not reject all depth priors,
intermediate features, query fusion or boundary supervision.

E0 also reproduces the frozen four-expert reference exactly. Its historical
F1=87.50% and its boundary costs both remain valid. MZ129/App defaults remain
unchanged. A is retained only as an alternative Development working point.

## Matched result

| Arm | TP/FP/FN | Precision | Recall | F1 | PR-AUC | AP |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| MZ129 | 136/74/8 | 64.76% | 94.44% | 76.84% | N/E | N/E |
| MZ145 original causal readout | 143/69/1 | 67.45% | 99.31% | 80.34% | .9615 | .9615 |
| Four-expert original causal mean | 140/36/4 | 79.55% | 97.22% | 87.50% | .9795 | .9796 |
| A: base features, dev-selected frame readout | 137/28/7 | 83.03% | 95.14% | 88.67% | .9615 | .9615 |
| B: A inputs + frozen relative-depth features | 135/32/9 | 80.84% | 93.75% | 86.82% | .9266 | .9269 |

PR-AUC is trapezoidal area; AP is reported separately. Original references'
ranking metrics use their continuous pre-causal scores, while their operating
counts use their original causal decisions. MZ129 only supplies binary alerts;
there is no comparable ranking curve. Nonalert is UNKNOWN, not clear space.
UNKNOWN counts are78/76/112/123/121 in the table's order; all288 frames remain
in the evaluation, with144positive/144negative.

A's complete report score vector is **bit-identical to the original static
MZ145 HGB vector** (maximum difference0). Thus its different warning tradeoff
comes from the new frame threshold/readout, not a new representation or trained
capability. Compared with the four-expert working point it trades8fewer FP for
3more FN. It does not dominate that reference, whose ranking curve remains best.

## Fair selection and input boundary

TRAIN192 is original MZ136 scenes0–3; dev48 is scene4, with whole paired episodes
kept together. MZ170288 comprises24other scene groups and is used only after
this run's models/configs/thresholds are sealed. All data has been inspected in
prior research; no fresh confirmation, natural-distribution or device claim.
Original test48 was excluded before selected-row decode. MZ146 was not used
as an independent report set because the expert mean was calibrated on it.

Both arms receive the same three HGB candidates and select dev F1, then AP,
then candidate order. Both choose7leaves/150iterations/L2=1/lr=.05/min_leaf8.
Both fit TRAIN at100% classification accuracy at the selected threshold; dev
is24TP/1FP/0FN. A threshold=.3917890013717321; B=.38207787768686463.
The separate dev-recall>=95% objective selects these same thresholds, so the
two reported working-point rows per arm in JSON are intentionally identical.
Actual report recall is95.14% and93.75%; no report threshold was retuned.

B retains all original2485public features and adds924relative-depth statistics
from64angular regions,8projected BODY/HEAD query-plane windows, pooled RGB
proposals and2image windows. Only public observations/RGB enter features.
IDs, family, native geometry and labels do not. Relative values are normalized
per image, never subtracted from ToF metres. The simulation's existing aligned
angular ToF/RGB projection is inherited; physical displaced-camera registration
has not been established. Both raw returns, statuses and sensor masks remain.

Official DA-V2 source commit: `a561b849ebae10a6f5ef49e26c83cbbcd36c71bf`.
Small has24,785,089parameters, frozen float32. Native640x360 BGR becomes an
actual1x3x518x924 tensor using official preprocessing. Source/checkpoint hashes
are in the run freeze; there is no fine-tuning or new loss.

## Where the changes occur

| Family | A TP/FP/FN | B TP/FP/FN |
| --- | ---: | ---: |
| Near rod / far wall | 36/11/0 | 36/10/0 |
| Shallow boundary pressure | 30/6/6 | 32/11/4 |
| Substantial BODY | 36/1/0 | 36/1/0 |
| Suspended HEAD | 35/10/1 | 31/10/5 |

Ordinary216frames: A107/22/1; B103/21/5. Boundary-pressure72frames:
A30/6/6; B32/11/4. Thus B recovers2boundary true frames but adds5boundary
false frames and loses4HEAD true frames. Full paired decisions both correct
fall109/144 to103/144; score ordering remains136/144 for both. Better local
boundary recall is not an overall gain.

All30positive events eventually alert. A has26immediate events and4delayed
by.25s; B has27immediate,2delayed by.25s and1HEAD event delayed1.25s.
Maximum sampled onset delay therefore worsens. False segments are18for both;
false4Hz bins total7s versus8s, or19.44% versus22.22% of negative sample time.
Among six observed boundary exits, A has one.25s release delay; B has that
delay plus one.50s delay. Episode-end events cannot establish release behavior.
These six-frame4Hz clips do not establish long-walk interruption burden.

Five A nonalert frames contain57native returned ToF corridor contributor samples;
six B nonalert frames contain63. The frozen four-expert reference has4frames/54
samples. Raw input retention is not final warning retention. Per-return native
Radar lineage remains NOT_EVALUABLE. These are disclosed costs, not a newly
imposed requirement that every old true frame survive.

## Cost, execution and validation

Local RTX5060LaptopGPU, Torch2.11.0+cu130. A uses the implemented CPU
OpenCV/sklearn path; B additionally runs DA-V2 on CUDA. A representative
same-tensor CPU/GPU probe selected CUDA. No CPU or cached depth is described
as online GPU inference.

| Complete warm per-frame replay | p50 | p95 |
| --- | ---: | ---: |
| A | 78.20ms | 88.94ms |
| B | 303.80ms | 329.69ms |

24frames are the first complete episode of each family, selected without outcomes.
Measured path includes RGB decode, original frontend, depth preprocess/forward/
upsampling, local statistics and HGB. B conservatively also includes A's small
head call. Capture/transport, sensor age and causal decision waiting are excluded.
Peak allocated CUDA memory is3,453,736,960bytes (~3.22GiB). No phone/board claim.
Historical expert end-to-end latency was not remeasured, so no speed comparison
against the four-expert implementation is asserted.

Three focused tests cover normalized relative-depth invariance/dtype, missing
region masks, and distinct F1/high-recall threshold semantics. E0 authenticates
source/models/caches and reproduces five score/flag vectors bitwise; all seven
old reference arms' counts/events match. All24online timing frames reproduce
base features exactly and depth features within1e-5. Video was decoded and its
preview visually inspected. The bounded independent code review found no
A/B leakage or asymmetric selection; binary-reference PR metrics were corrected.

First startup failed before a feature cache/fit due to NumPy float64 promotion
into OpenCV float32 Sobel; normalization now explicitly returns float32.
After all six fits and prediction sealing, video concatenation used the wrong
axis. Delivery resumed from sealed models/features/predictions with no refit or
threshold selection, fixed rendering, and verified unchanged selection, scores
and CSV hashes. Original frozen source, failure and recovery receipts remain.
The completion's seconds field describes this final delivery phase, not total
research duration; feature extraction took139.70s and six fits about30.12s.

## Delivery and disposition

Durable ignored payload root:
`artifacts.local/work/corridor-depth-e1-20260917/`.
`e0-reference-replay.json` holds the independent E0 receipt. `run-v2/` holds
`summary.json`, `completion.json`, `predictions.csv`, `A-model.pkl`, `B-model.pkl`,
feature/selection/prediction seals, native accounting, full PR curves, latency
samples, `comparison.mp4`, `preview.png`, failure/recovery records and snapshots.
Checkpoints and input caches are retained for reproducibility; no resident
process, worker, service or paid allocation remains.

Supported registration is blocked by existing `experiments/index.jsonl:303`
input-fingerprint mismatch. The intended terminal role is NEGATIVE_CONTROL for
this E1 statistics+HGB design; structured registration/inheritance remains pending.
No ledger bypass or historical result rewrite. A's working point may be compared
as a disclosed Development component; it is not an App replacement.

The next decision is whether a single intermediate-feature diagnostic is worth
trying, since these statistics may discard useful edge structure. It was not
run, and this result does not authorize or justify an automatic E2–E6 expansion.
