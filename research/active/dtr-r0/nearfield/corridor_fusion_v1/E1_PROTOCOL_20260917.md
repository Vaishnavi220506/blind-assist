# E0/E1: frozen relative-depth features for corridor HGB

User-requested EXPLORE, consumed controlled Development only. No App change.
Question: does frozen DA-V2-Small relative structure add task information to
the existing 2485 public sensor/geometry features, without metric reconstruction?
Unlike MZ140/MZ173, depth is an auxiliary feature, never an independent geometric
decision or veto. Preserve their historical outcomes and the old four-expert recipe.

- Fit original MZ136 TRAIN192. Select on its dev48 (whole scene groups), report
  MZ170288 only after sealing selections; all cohorts are historically consumed.
  Original test48 is excluded. No new capture. No claim of fresh generalization.
- A: original public features; B: same plus 924 normalized depth statistics from
  64 angular zones, eight BODY/HEAD query-plane windows, RGB proposals and global
  windows. Query geometry is the existing device/body aligned simulation condition.
- DA-V2 official relative Small, frozen float32, input_size518 with official
  aspect-preserving transform. Record actual shape, source commit and weight hash.
  No depth/metres subtraction, no label/ID/family features, no sensor slot removal.
- Both HGB arms: equal three candidates (leaves7/iterations150/L2=1;
  leaves7/iterations300/L2=1; leaves15/iterations150/L2=3), lr.05, min_leaf8,
  no early stopping, seed177017. Select best dev F1; ties prefer dev AP then
  smaller candidate index. For the selected model also select minimum FP at
  dev recall>=.95. Apply both thresholds unchanged to report288. Frame-only output,
  no new temporal mechanism. Nonalert remains UNKNOWN, not safe space.
- Reproduce frozen MZ145 and four-expert scores/flags from authenticated caches
  on the same report288. Show original causal working points as references;
  they have different historical selection and temporal processing from A/B.
- Report precision, recall, F1, trapezoidal PR-AUC and AP (separately), complete
  curves, family/ordinary/pressure metrics, events and sampled false occupancy,
  paired decisions, native-supported missed warnings, and end-to-end latency
  including RGB decode/base frontend/depth transform+forward+resize/statistics/head.
  Compute latency is not event waiting time; 4Hz six-frame episodes limit inference.
- Stop after this E1 comparison and delivery. A useful gain may justify E2 later;
  no gain justifies an optional intermediate-feature diagnostic, not automatic
  expansion to joint networks, new losses, CNH, temporal models or distillation.
- Save resumable per-frame features bound to source/config/model identities,
  model weights, scores, CSV, report and predefined first-per-family comparison
  video. One local owning process, 3600-second run budget, failure receipt on error.
