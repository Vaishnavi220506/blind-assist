# S1 frozen targeted confirmation

User-directed EXPLORE, 2026-09-17. One new 24-configuration/288-frame capture
tests whether the frozen conditional depth specialist transfers beyond its
consumed Development scenes. A remains the balanced research baseline.

## Frozen comparison

Keep A, C, both thresholds, TRAIN-only PCA, DA-V2 checkpoint and feature code,
gate tree and invocation probability 0.9 unchanged. Infer with the exact tree
threshold, never rounded 3.93 m. No fitting, calibration, layer/PCA/threshold
search, gate variants, selective regeneration, protected test or CNH inputs.
The recipe hashes all inference dependencies before capture. Cached original
S1 parity must pass before dispatch. Missing/nonfinite C preserves A.

Question: does observable far-range/background ambiguity still route useful
cases to C while preserving enough true alerts and event timing to be useful?

## Fixed new source

`confirmation_source.py`, seed179017: four existing families crossed with six
background front distances3.3/3.6/3.8/4.0/4.2/4.5m,24 configurations,48 paired
episodes,6 frames each at4Hz. Source geometry has144positive/144negative frames
and30 positive events. Each pair changes only target lateral position; texture,
camera and sensor random start remain shared. No latent field enters inference.

Background topology varies between portal, staggered panels, recessed opening
and full wall; rod width/lateral position, procedural texture grid/seeds and
simulated reflectance vary. These are grayscale appearance/reflectance proxies,
not physically measured materials. A near full wall would itself be a positive
corridor obstacle: all3.3/3.6m backgrounds therefore have a real central opening.
All objects enter native truth; no background is silently exempted.

Distances refer to the first background front surface relative to initial body
x. They are not the learned q100 feature. True obstacles with far backgrounds
create opportunities for above-threshold A-TP calls; observed coverage is a
reported result, not a condition for resampling. Topology and range are not a
balanced factorial. New configurations share the renderer and sensor simulator;
this is targeted controlled confirmation, not natural/hardware validation.

## Execution and measurement

One hash-bound worker UE capture, timeout1200seconds, no automatic redispatch.
Inherited renderer/sensor code unchanged; inherited capture startup uses the
documented20000–29999 Zen-port adapter. Verify receipt, all RGB hashes, frame
identity, native/source all-object geometry and owned process/port release.
Source admission may inspect geometry; predictor code cannot access it.

Run frozen A and actual conditional C on all288 public records; seal predictions
before parsing evaluator outcomes. Keep the model resident, report cold load
separately. Measure matched warm full-chain mean/p50/p95/max and called/uncalled
costs on the local RTX5060 Laptop; no mobile or end-to-end sensor acquisition
latency claim. CPU geometry/HGB/tree/PCA and CUDA DA-V2 placements are explicit.

Report A/S1 TP/FP/FN/precision/recall/F1, invocation rates among all frames and A
alerts, called A-TP retention/loss and A-FP removal/retention,30-event detection,
first alerts and relative delays, family/pressure/paired discrimination,
native-supported removed alerts, UNKNOWN/nonalert coverage. Audit q100 bins
around the exact tree threshold and by physical wall distance/topology/family.
Source strata enter analysis only after the prediction seal. No global C pass is
needed; its prior global failure remains the negative control.

## Decisions and stop

- Useful FP reduction with no TP loss and all A events/onsets retained: retain
  S1 as a research method component within the measured scope.
- Some average benefit with1–2lost A TP: preserve as a precision-oriented point
  only if F1 improves, FP reduction is substantial and all A events survive;
  disclose timing/native losses. This is not automatically a failure solely
  because an individual A TP changed. Other tradeoffs remain unpromoted.
- No useful reduction, no invocation, or indiscriminate harmful veto: do not
  promote; preserve S1 Development evidence and close this online DA-V2 recipe.
  A source failure instead means not evaluable, not method failure.

Stop after this one confirmation. No rescue tuning to reach90% and no automatic
CNH successor. Finish report, scoped delivery and resource release. Existing
ledger input-fingerprint failure remains pending metadata; never bypass it.
