# Existing-return surface support oracle on changed-domain288

User authorized one evaluator-only diagnostic, no capture/training/threshold
selection. Reuse the admitted S1-confirmation-recovery288 and frozen A pickle,
threshold0.3917890013717321. Main task is the adopted5cm clear definition with
coverage, precision/recall/F1; strict whole cohort and boundary are appendices.

The historical MZ138 dev48 result24/13/0to24/2/0 used MZ129's analytic reducer,
not A. A's2485-feature HGB has no surface-set input or analytic intersection
readout. Its existing128slot supports each encode six AABB endpoints. Its RGB
plane-hypothesis pooling is a separate branch. Before oracle outcomes, inspecting
the frozen trees found only one split using a slot support endpoint; a negative
result at that seam cannot establish absence of surface-information headroom.

Freeze these diagnostic contrasts together:

- A: exact authenticated original vector and score.
- A_sampled_support: only six support endpoints of each existing usable slot
  become the envelope of its linked native contributor points.
- A_full_surface_support: same six fields become the envelope of the union of
  verified COMPLETE native cuboid faces linked to that returned slot. The
  complete face extends beyond sampled points/zone/RGB; do not add other faces
  or objects. This AABB adapter can fill gaps, so also audit union versus hull.

All other features remain bitwise fixed, including present/usable/merged,
range/sigma/signal, region-center hypotheses, RGB pooled geometry, Radar and
IMU. Keep A weights, score threshold and alert decision unchanged. No new
returns, no repaired missing packet or undetected peak. Native XYZ changes
position/depth authority as well as extent; sampled-versus-full distinguishes
surface completion while both use native coordinates. These are privileged
feature interventions, not deployable models or guaranteed optimal ceilings.

Use original returned lineage: zone/slot -> returned_lineage.target_index ->
hit_indices -> private_rays.subray. Identify actor/face using original1e-5m
native-bound test. Missing/ambiguous face/lineage falls back to original entire
slot; sampled/full arms use the same eligible completion set. Never guess
ownership or split one slot into multiple new votes. Audit native-point retention.

Separately compute exact union-of-returned-full-faces corridor reachability and
sampled-point reachability at nominal geometry. These answer whether returned
measurements touch a surface capable of supporting each obstacle. They are NOT
A predictions, a replacement Radar policy or a guaranteed achievable bound.
Positive-only reachability can be counted as potential support for A's13clear
misses, but must not be reported as saved TP of A. Geometric false reachability
should be zero because the oracle uses the same native geometry authority;
this does not prove a learned completion model will remove false alarms.

Report per-family actual gained/lost TP/FP, all40clear-error cases, no-return
and unreturned-corridor-object diagnoses, sampled-zero/full-positive cases,
native retention, old strict288 and boundary72 metrics, core events/negative
burden. For no-return diagnosis private unreturned rays may be inspected only
after oracle prediction seal and never inserted into oracle support. All input
hashes and output features/scores are sealed. CPU small scalar geometry/HGB;
no real-time latency claim. No automatic RSSF training, CNH or depth successor.
