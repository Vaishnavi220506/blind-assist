# MZ183 visible angular ownership ceiling

Consumed MZ1771920 frames; baseline frozen MZ181 depth6. Success requires
remove>=24/32 remaining ToF-only FP, retain>=135/142 extra TP, all A* alerts,
independent Radar (including7 residual FP). No training/new scenes/threshold
search; stop this fixed ceiling, no automatic segmentation or successor.

Saved source lacks a captured instance-ID image. Admit only a explicitly labeled
reconstructed controlled-render-geometry mask: ray cast pixel centers against
all actual axis-aligned cube meshes, including noncolliding1mm albedo tiles,
with nearest-hit occlusion. Authenticate source cubes against native bounds.
This is not a projected actor box or native-depth readout. Mask builder may use
scene geometry/camera only to produce identity pixels; predictor receives no
source point/depth/extent/true yaw. Return identity is union of all actual
returned-lineage actor IDs, never label-selected. Unknown identity or missing
mask falls back to baseline return. A mask empty inside a zone is UNKNOWN and
falls back, not a successful FP removal. Preserve sensor support outside RGB FOV.

Intersect visible mask pixel cells with the original zone; use same public
distance +/-3sigma, public pitch/extrinsics and integrated IMU yaw. Pixel-cell
rectangles use inherited conservative slant envelope, existential intersection;
never replace range with native depth or narrow it. Do not expand supports that
MZ181 already rejected. A*/Radar remain OR-independent, merged stays UNKNOWN.
Seal ownership-only masks and association, then seal predictions before task
truth evaluation. Report all1920 outcomes and exact paired sets/timings,32FP
denominator,142TP retention, fallback coverage, and target-linked contributor
loss. Mask authority is conditional on analytic reconstruction, pixel-center
rasterization and mesh visibility; not a perfect rendered-mask or real RGB claim.
Inspect sample RGB overlays without changing mask rules. CPU NumPy raster path
is GPU_BACKEND_UNAVAILABLE (local chosen Python has no Torch); no model inference.
