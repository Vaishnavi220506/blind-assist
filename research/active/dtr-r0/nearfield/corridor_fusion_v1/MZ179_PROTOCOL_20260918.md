# MZ179 — Temporal Frontier Evidence Ceiling

EXPLORE consumed Development. Question: Can pre-alert temporal native evidence
separate the A*–HGB recall/false-positive exchange? MZ176 is closed; MZ177/178
are already allocated. No training, new capture, rescue deployment or retuning.

Freeze the existing 288 ordered IDs, models, thresholds and A*=0/raw=1 subset.
Report six clear candidates (3 TP/3 FP) and boundary candidates separately.
ToF-survival rescue of these three TP is withdrawn. Labels/native support and
source pose are audit-only; never inputs to public feature extraction.

Before feature outcomes: windows 0.5,1.0,1.5 seconds, strictly t-window <= s < t.
Left-censoring and counts are explicit; the nominal 1.5-second window may not
exist. Current-only sensor features and saved A*/raw score histories are
separate controls. No future, actor identity or source pose in the extractor.

Radar: packet-valid finite returns; report count, range statistics, nearest
range slope (not same-object velocity), native velocity median/sign consistency,
bearing spread, last-support age. Coarse cells use 10-degree bearing bins and
0.5m range bins, aligned once at zero; integrate valid public yaw to initial
heading with no translation claim. Report maximum cell frame density, longest
consecutive cell support and cell range slope/MAD. Include all cells and a
fixed forward sector |bearing|<=15 degrees, range<=3.6m separately. The sector
is a region proxy, not a corridor/actor identity assertion. No actor matching.

ToF: valid/invalid/no-return status, packet availability; free is UNKNOWN.
Saved native corridor witness counts/ages are evaluator-only diagnostics.
IMU: yaw/pitch deltas and angular rates. Public pure-head-motion is UNKNOWN
without translation; use saved source camera translation only to audit H2
applicability (<=0.02m displacement over history plus current, no fitted bound).
Source speed/pose must never become inference features. H2 is inapplicable if
these candidate histories move; do not mix MZ177 head-turn cohort into this test.

Ceiling: descriptive exhaustive one-dimensional thresholds and AND of two
one-dimensional thresholds over the prespecified feature list, reporting all
perfect separators and number searched, missingness separately. Require all
six finite for a separator; no UNKNOWN-as-zero. Compare history native,
current-only native, and score-history controls. This is an optimistic label-
selected diagnostic on only four scene groups, not a learned deployable rule.
Report ranges/margins and whether simple source-region content explains it;
perfect six-row classification alone does not prove incremental/generalizable
information. No model is trained even if descriptive separation exists.

H1 positive retains only a scoped candidate for later fresh validation. No
separation closes the tested feature family, not an information-theoretic proof
that all histories lack information. Missing prerequisites are NOT_EVALUABLE.
H2 inapplicability is not H3. Stop after this fixed extraction/audit; no sweep
of windows/bins or automatic successor. CPU scalar accounting; retain raw
histories, feature seal, hashes, result and failed metadata receipts.
