# MZ181 public set-consistent corridor intersection

EXPLORE, reused consumed MZ177 1920 frames. Baseline is sealed MZ180 public
UNKNOWN (1228/103/156), A* remains frozen. Only SIM_VALID ToF intersection
changes. Same distance +/-3 sigma, angle domain, pitch, extrinsics, IMU yaw,
packet/quality checks, independent Radar and OR with A*. Merged remains UNKNOWN.

Before predictions: bisect both zone angles at each level, up to depth6
(64x64 angular cells). Reuse slant_envelope on each subdomain; discard only
disjoint boxes. Intersect child boxes with the enclosing parent to preserve
numerical nesting. At a retained node an exact center-ray slab/range intersection
can prove a feasible point and end search. It cannot reject a return. At depth
limit any unresolved box is retained as possible, not certified physical
intersection. Use 1e-10m conservative geometric comparison padding (not fitted).
Record depth0..6 only as prespecified convergence diagnostics; depth6 is primary.
Angle subdivision does not eliminate range dependency; do not claim an exact
solver if unresolved support remains. No center-sampling rejection or new range
subdivision successor. CPU scalar branching: TASK_NOT_GPU_SUITABLE.

Seal public predictions/hashes before outcome access. Compare all1920 paired
frames and original142 extra TP/44 extra FP; stratify extras by valid-ToF only,
Radar only and both. Report TP/FP/FN, coverage/UNKNOWN, head supported misses,
native target-linked contributor losses, each sequence's onset/transitions,
12 moving contact retention and measured readout cost. Baseline preservation
is OR-structural and not evidence of earlier warning. Success requires >=135
extra TP and <=25 extra FP over A*, no baseline loss or loss of advance contact
events. Record delays/lost support even on aggregate success. Stop after this
fixed experiment: no tuning, training, new capture, MZ182 or automatic successor.
No improvement closes this finite angular-refinement recipe, not all geometry
or all information sources. Unresolved precision limits must be explicit.
