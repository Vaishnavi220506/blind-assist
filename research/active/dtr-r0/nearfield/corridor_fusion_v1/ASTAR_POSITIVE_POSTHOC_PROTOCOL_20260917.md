# A* OR public positive: saved-output complementarity check

User-directed consumed Development analysis, proposed after inspecting the
completed single-version confirmation. It does not revise that frozen result
or claim a new prospective confirmation.

Question: does the already frozen public positive branch recover A* misses,
especially the four clear true frames that old A detected and A* lost, at an
acceptable false-alert and event cost? Compare saved A, A+positive, A*, and
exactly A* OR positive on all288 frames. Reuse all model weights, thresholds,
labels, 5cm strata and episodes. No new fit, model inference, capture, threshold
selection, temporal filter or alternative combination.

Authenticate original source/output seals and recompute each flag from its
saved score and frozen threshold. Write a separate analysis directory with
combined predictions, input hashes, full counts, precision/recall/F1, coverage,
family/configuration breakdowns, negative burden, core/strict events, first
observed alert and release details. Identify each added TP/FP, episode and
configuration; audit the maximum return's native witness for added alerts.
Zero usable returns preserve A*. Confirm all A* alerts/onsets are retained.

Check aggregate metrics against direct counting and episode onsets against
independent contiguous-positive-segment accounting. Recompute geometry and
strata from the original native records; native values never influence the
already saved scores or OR rule. Compare the saved original report methods
exactly as a regression check. Verify frozen inputs stay byte-identical.

If useful recall gain remains after reporting boundary costs, retain the
combination as a posthoc effect-version candidate based on A*. If no gain,
prefer standalone A* for that role and keep the positive branch as a component.
No requirement of retaining every old A true frame; show actual tradeoffs.
No new runtime timing claim: earlier A* comparison includes old A head overhead;
stored component timings are context, not an optimized combined-path benchmark.
One exact composition and its audit complete this check. Preserve prior evidence,
update current/report, and deliver scoped code without a successor experiment.
