# Frozen A/B saved-output complementarity diagnostic

User authorization 2026-09-21: try the proposed diagnostic of A-silent/B-positive
cases using saved observations and predictions. One bounded consumed-development
analysis only: no neural fit, image inference, capture, test evaluation, runtime
promotion, automatic successor or changes to the original replacement terminal.

Source: `artifacts.local/work/ba-spatial-bce-20260920`, eight development base
groups/576 frames only. A, B weights, all saved logits, original single-return
observations, strong threshold and nonrecursive0.2s one-frame hold are frozen.
Do not open `evaluator/test-labels.json`, infer test logits or relabel this source
as fresh evidence. Existing test observation capture/admission stays disclosed.

Question: can a deployable observation-based screen distinguish useful B rescue
from new FP when A_current is false? Group/family/Core/Boundary/native truth are
NOT runtime screen inputs. UNKNOWN stays the same for all diagnostic arms.

Two screens are declared before this diagnostic reads incremental outcomes:

1. B logit alone.
2. B logit AND the original Calibration current decision, using unchanged
   threshold0.007085703945147101 and definite-support bypass on the SAME returns.
   This is ambiguous geometric eligibility, not certified target-return support.

Fixed contextual controls: A alone; A OR(B>=0); A OR(B>=7.6612162590026855);
and that same high cutoff with screen2. The high cutoff comes from the prior
consumed-dev diagnostic, not from independent data and not a new selected policy.
No cutoff grid, extra feature, second support rule or post-outcome screen follows.

For each of the two screens, separately perform eight leave-one-base-group-out
threshold diagnostics. In the OTHER seven groups only, find eligible frames with
A_current=false and truth=false. Cutoff is max(0,nextafter(max_negative_logit,+inf));
if there are none, cutoff0. Use float64 consistently for cutoffs and comparisons,
including atomic ties. This is threshold calibration, not another network fit.
All three lateral variants and their full trajectories leave together. Check zero
incremental RAW FP on the seven-group calibration set, then apply to the held
group. Report all eight cutoffs and outcomes; do not choose a winning screen or
claim this is an unbiased/fresh generalization estimate. The screens themselves
were motivated by consumed data. Only eight independent groups are available.

Combined current output is A OR screened B. Apply the identical original hold
to that current output, never to recursively held state and never across clips.
OR must preserve every A current/held alert, event and first in-event alert time.
Zero new raw FP does NOT guarantee zero new held FP or shorter release tails.

Report current/held TP/FP/FN, precision, all-negative FPR, false segments/duration,
event recall/coverage/onset/release, recovered/lost IDs and per-group increments.
Native returned target/corridor contributors are evaluator-only posthoc strata;
they never enter screening/calibration. No segmentation output means no IoU claim.

Diagnostic decisions are separate: a useful current complement requires at least
9 extra Boundary TP (>=10pp on86 positives) with zero added Core/Boundary raw FP;
a complete-alert complement additionally requires zero added Core/Boundary held
FP and no extra false segments. Every arm is reported; no winning arm is promoted.
Failure closes this fixed screen; a positive result is only a candidate rationale
for separately authorized verification, not permission to access retained test.

Seal input/code/protocol hashes and fixed observation-only flags, then per-fold
calibration records and all predictions before aggregate evaluation. Preserve
the prior source and checkpoints byte-for-byte. CPU scalar replay is
TASK_NOT_GPU_SUITABLE. Save evidence under
`artifacts.local/work/ba-spatial-complement-diagnostic-20260921`, attempt supported
metadata registration without bypassing existing blockers, record the scoped
result/current disposition and deliver only task-owned changes. End this diagnostic.
