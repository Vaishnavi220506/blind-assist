# 主体告警工作点：已有分数诊断（2026-09-20）

现有分数在已消费的 Core432 上有很大的降误报空间，不能把当前126个
主体负帧误报全部归结为缺少新的空间模型。这个结论是工作点诊断，尚不等于
新布局上成立；固定候选的一次验证见
[transfer results](CORE_WORKPOINT_TRANSFER_RESULTS_20260920.md)。历史 Calibration
及其“旧96帧全部已有TP保留”结论保持不变。

## 问题、数据和不可更改的部分

[诊断协议](CORE_WORKPOINT_PROTOCOL_20260920.md)先冻结，再读取已保存的
Core432分数与预测。没有重采集、重新拟合、模型推理、RGB否决、时间补洞或
逐场景阈值。主体口径为完整 INSIDE+OUTSIDE 布局288帧（72正/216负），
包括入界前负期；BOUNDARY布局144帧单独报告，完整432帧不被改写。

判定仍为 `definite_zones > 0 OR (raw.alert AND score >= threshold)`。
分数是几何重叠支持量，不是碰撞概率；UNKNOWN、区间观测和独立支持通路不变。
只分析原阈值以上的固定分数决策集合：250种不同集合，保留分数相等帧的绑定。
事件首报约束与全主体TP约束各自直接给出一个最大阈值，不从边界结果反选阈值。

## 旧工作点的实际约束

原 Calibration 阈值 **0.007085703945147101** 由旧96帧中的唯一最低必保留
模糊TP约束：`f0042 / s03 / g2_grazing`，`frame_in_clip=6`（从0计数）、
1.2s的边界接触横杆。
30个必须保留TP中10个有definite支持，20个走分数通路。
这里的f0042属于旧96帧数据，不能与Core432同名ID混淆。

Core432主体的126个基线误报中，**0个由definite通路强制报警**；
INSIDE负期39个、OUTSIDE87个全部受分数阈值控制。BOUNDARY布局的20个FP
也没有definite支持。这只是可控性计数，不能据此认定任何具体物理误差成因。

## 主体结果

数值顺序均为TP/FP/FN；采样间隔0.2s，“时长”为报警采样格累计时长。

| 固定角色 | 阈值 | Core288 | 精确率 | 召回率 | 负帧FPR | 误报段 / 采样时长 |
|---|---:|---:|---:|---:|---:|---:|
| 历史Calibration | 0.007085704 | 72/126/0 | 36.36% | 100% | 58.33% | 24 / 25.2s |
| 保留全部主体TP的对照 | 0.266037333 | 72/29/0 | 71.29% | 100% | 13.43% | 21 / 5.8s |
| 保留主体事件及首次事件内提醒的候选 | 0.407130964 | 71/10/1 | 87.65% | 98.61% | 4.63% | 9 / 2.0s |

两个诊断阈值均保留12/12主体事件及其原首次事件内提醒。
全TP对照的约束帧是 `f0011 / core_b0_head_horizontal_inside / 2.2s`；
事件候选的约束帧是 `f0150 / core_b0_body_suspended_solid_inside / 1.2s`。

事件候选减少116个FP（92.06%），仅丢失上述f0011一个后续主体正帧。
该HEAD事件从6/6报警变为5/6；最长静默是末尾1格0.2s，其余11个事件没有
正期静默。所有失去的正帧、事件覆盖率和静默连续段均保存在results/decisions中。
这没有把“仅一帧报警”藏在12/12事件计数里。

| 完整布局内负期 | Calibration FP / 段 / 时长 | 全TP对照 | 事件候选 |
|---|---:|---:|---:|
| OUTSIDE（144帧，28.8s机会） | 87 / 13 / 17.4s | 10 / 10 / 2.0s | 0 / 0 / 0s |
| INSIDE负期（72帧，14.4s机会） | 39 / 11 / 7.8s | 19 / 11 / 3.8s | 10 / 9 / 2.0s |

候选仍有8/12事件在入界时已经持续报警（基线10/12）。首次事件内提醒没有
延后，不意味着首次起报正确，更不证明提前预警。clip-first时间与完整时间线
一并保留。主体分组：BODY36/7/0、HEAD35/3/1；两背景分别35/4/1、36/6/0。

## 必须保留的边界代价

| 范围 | Calibration | 全TP对照 | 事件候选 |
|---|---:|---:|---:|
| BOUNDARY布局 TP/FP/FN | 72/20/0 | 15/4/57 | 2/1/70 |
| BOUNDARY事件 | 12/12 | 10/12 | 1/12 |
| 完整432帧 TP/FP/FN | 144/146/0 | 87/33/57 | 73/11/71 |
| 完整432误报段 | 40 | 24 | 10 |

这是很大的边界覆盖损失。候选只能讨论主体任务，不能取代完整严格任务的
Calibration，更不能把静默解释为通道安全。全TP对照也没有“无代价”的含义：
它仅无损于主体72正帧，不无损于完整任务。

## 处置和复核

已有分数具备主体工作点收益，诊断角色为`COMPONENT_OR_CHALLENGER / COMPONENT`。
按用户指定的条件，仅冻结事件候选0.4071309640537889，进入一次新完整布局验证；
全TP对照没有被另行验证。候选不能在新结果后调整；验证失败不抹去本诊断，
也不允许继续用旧集反复找数值。

独立复算核对全部432决策、分层计数、事件首报、丢失正帧、尾部静默及边界
代价通过。实现还检查inclusive阈值、definite旁路、raw门控和尾部连续静默。
CPU标量/JSON工作为`TASK_NOT_GPU_SUITABLE`，训练与模型调用均为0。

证据位于 `artifacts.local/work/ba-core-workpoint-20260920/`：
`protocol.json`、`protocol-before-run.md`、`implementation-seal.json`、
`results.json`、`decisions.json` 和注册收据；诊断脚本为
`audit_core_workpoint_20260920.py`。完整统计重现读取保存结果，不改变冻结输出。
支持的注册CLI被既有 `experiments/index.jsonl:303` fingerprint错误阻止；
保留收据和本次结构化局部处置，未修写或绕过全局账本。
