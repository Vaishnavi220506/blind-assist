# MZ156: positive evidence memory under an explicit stationary-world condition

EXPLORE consumed Development, 2026-09-16. MZ155's fixed scan recipe remains
closed with failed component/alert gates. It measured extra rod episode coverage
but intermittent current returns. Test a distinct temporal mechanism without
changing the scan, source, model or thresholds: a fully enclosed positive ToF
measurement can remain positive in a known stationary body/camera/world episode.

## Method fixed before the replay

Use all288 consumed MZ155 observations, both original144-frame arms. Reuse sealed
MZ129 decisions unchanged. Each public SIM_VALID slot is considered separately;
SIM_MERGED, invalid and missing returns cannot create new witnesses. Use its
entire angular zone with no RGB clipping or visual association. Nominal range
bounds are reported range plus/minus(3sigma +.10m aggregation-span allowance
+ .01m quantization allowance), lower bounded by0. The extra allowances derive
from the disclosed hypothetical sensor contract, not observed outcomes. They
are not calibrated hardware bounds and Gaussian support is not a hard guarantee.

Integrate only public delta_yaw from an explicitly forward-aligned time0 frame.
Nominal yaw radius is.5 +.2*elapsed_seconds +3*.08*sqrt(increment_count) degrees;
pitch allowance is+/-.5degrees. These cover initial alignment allowance, the
simulator's drift allowance and nominal accumulated increment noise without
subtracting native error. Use the existing full interval slant-range projection,
including public camera offsets. A positive witness requires the entire resulting
XYZ enclosure inside the body-fixed corridor [.2,3.6]x[-.3,.3]x[.4,2.05]m.

Current witnesses are independent. Under the explicitly supplied experiment-wide
stationary condition, retain each positive witness for at most1.25s and six
contiguous.25s frames. Do not intersect cones, associate native target identities,
or assume that similar ranges refer to the same material point. No-return stays
UNKNOWN and cannot clear a previous positive. Missing/invalid IMU, changed camera
calibration, a cadence gap or window expiry clears memory and disables the branch
until the next valid forward-aligned episode. This is not an online static-world
detector, and cannot be enabled merely because a user has stopped walking.

Compare both passive/scan arms with (a) unchanged MZ129, (b) MZ129 OR current
enclosed-witness and (c) MZ129 OR prefix memory. Also show witness-only results
and retained MZ145 passive/scan figures as a stronger aggregate comparator.
The OR preserves every incumbent positive within each arm and cannot reduce
its FP. Any scan-vs-passive FP benefit was already present in MZ155; memory can
only add positive evidence. Never attribute the inherited FP reduction to memory.

## Measurements and decision

Seal all new public-only predictions before decoding this replay's native rows.
Report TP/FP/FN, precision/recall, each family and ordinary/boundary strata, paired
in/out decisions, no-alert/UNKNOWN, events/first times, false segments and native
contributors. For every current witness, check its native contributors against
the claimed enclosure and corridor after sealing; count any nominal-bound failure.
Report each added true/false frame and originating witness, age, episode and slot.
Whole returned-point containment does not establish full obstacle geometry.

Retain this static-condition memory component only if it adds at least one true
frame over the current-only readout, adds no false frame in either observation
arm, and no witness contributor violates its enclosure/corridor. Separately
test whether scan+memory reduces FP by at least20% versus passive+memory, loses
no matched true frame/event, delays no event by more than.25s, and increases no
family's FP. Show both results; a component gain does not imply a combined gain,
superiority to MZ145, fresh confirmation or walking/device/default promotion.

No fitting, source recapture, scan-amplitude sweep, threshold selection, original
MZ136/MZ146 decoding or protected test access. One complete288-frame replay with
120s compute cap, scalar CPU execution (TASK_NOT_GPU_SUITABLE). Freeze method,
protocol, code, source/input hashes and baseline seal before replay. If it fails,
close this exact recipe without changing bounds or retention duration. Preserve
MZ152 and MZ155 failures; this is not their retrospective rescue. Save outcomes,
attempt supported registration and retain any existing ledger303 metadata error.
