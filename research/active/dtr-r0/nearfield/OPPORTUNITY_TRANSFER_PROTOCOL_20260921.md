# Frozen local geometry transfer of the two opportunity components

2026-09-21, EXPLORE. User authorized the proposed unchanged-method transfer
check. Both old toy sources exhaust their declared prior banks: all150 shape
hypotheses and all621 active-view hypotheses were evaluated. There is no unused
in-prior holdout. This check therefore uses new geometries from the same analytic
generators, derived from old layouts, and is **correlated same-generator local
perturbation Development**, not independent-source or protected confirmation.

## Freeze before observations

- Translate every solid/box in each original nonempty scene by exactly -0.01m
  and +0.01m along X. Keep Z, dimensions, wall, sensor, fixed original-frame
  corridor and all other conditions unchanged. No truth-based filtering.
- Shape: all174 old parents,348 new cases. Active:652 nonempty old parents,
  1304 new cases; exclude only the empty scene because translation cannot create
  a new layout. Verify normalized geometry uniqueness and zero overlap with the
  original sources; any collision is an admission failure, not a removable case.
- These are outside the exact banks even when the parent was in-prior. Retain
  parent provenance groups, signed-shift groups and complete denominators.
  Also disclose byte-equivalent public initial observations: new geometry does
  not guarantee new information.
- Import unchanged shape_hypotheses, shape_support_union, active_view and
  active_view_positive functions. Authenticate their snapshots and old sources.
  Shape comparators: point, consensus, fixed union. Active comparators: fixed
  +X, old adaptive, positive-priority; each gets exactly two observations and
  one0.12m translation. No bank expansion, tolerance/cutoff/selector changes,
  fitting, policy selection or additional perturbation sizes.
- Store source and code hashes before observations. Each selector receives only
  initial bins and the public prior; seal every policy's choices before any
  actual second observation. Predict without evaluator truth and seal all
  outputs before the evaluator join. Public hypothetical prior labels remain
  distinct from actual source truth. Full extent intersection defines truth.

## Questions, costs and fixed decisions

Shape increment survives this check only if union adds at least one TP versus
point, adds zero FP, retains all point IN, and introduces no additional false
OUT versus consensus. Report total FP regardless of source and do not equate
OR retention with learned generalization. Report the old four false OUT
case_152/154/158/160 as **saved consumed regression** and separately show all
eight shifted descendants; no old-case success enters the new denominator.

Active increment survives versus fixed only if positive-priority adds net TP,
retains every fixed TP ID, adds no wrong commitment (FP or false negative OUT)
by identity, and correct decisive count does not decrease. Report positive and
negative gained/lost IDs versus both fixed and old adaptive, including erroneous
commitments separately; no lossless claim versus old adaptive is required.
Report all cases as primary, and the public initial-ambiguity subset plus parent
groups as diagnostics without dropping no-match/UNKNOWN cases.

For each component: a passed criterion retains only a local-transfer component;
a failed criterion closes the increment-transfer claim for this perturbation,
without erasing the original consumed gain. Wrong commitments are material even
when aggregate coverage increases. Source/admission/implementation failure is
NOT_EVALUABLE, not an algorithm negative. No successor or retuning follows.

One scored pass at this fixed amplitude, plus focused harness tests and an
independent saved-output audit. No event/onset, physical movement, native sensor,
natural distribution, hardware or safety claim. CPU scalar/NumPy work is
TASK_NOT_GPU_SUITABLE. Durable source/observations/predictions/audit/receipts live
under artifacts.local/work/ba-opportunity-transfer-20260921/run-v1. Refuse existing
output. Retain mechanical failure logs; a code-only repair may resume into a new
directory with unchanged scientific settings and a disclosed receipt. No worker
or paid allocation is needed. Global metadata uses supported commands only.
