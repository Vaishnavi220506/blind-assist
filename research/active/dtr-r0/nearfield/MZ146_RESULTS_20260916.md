# MZ146: fresh aggregate alert gain, rod regression prevents joint confirmation

EXPLORE, 2026-09-16. [Frozen protocol](MZ146_PROTOCOL_20260916.md).
Decision: `FRESH_CONTROLLED_CONFIRMATION_NOT_MET`.

**On 288 newly captured frames, unchanged MZ145 improves MZ129 from
130 TP /86 FP /14 FN to143/57/1.** Precision improves60.19% to71.50%, recall
90.28% to99.31%, false frames fall33.72%, and false segments fall28 to21.
All130 incumbent true frames and all29 detected incumbent events are retained;
the candidate also detects the one event MZ129 misses. However, rod-family FP
increase25 to29, failing the pre-outcome per-family condition. This is a real
fresh aggregate gain with a localized regression, not a joint confirmation or
an all-family improvement. Keep MZ129 as the retained research baseline; the
App default is unchanged. Close this exact confirmation without tuning its
model, thresholds or source.

## What was frozen and observed

The original MZ143 TRAIN192 model, all2,485 sensor/RGB features and both MZ145
cutoffs are unchanged: low .020014435971556582, high .2957935335969224.
A high current score alerts immediately; weaker evidence requires the current
and immediately preceding .25s frame to pass low. No future frames or backdating.
No new fitting, threshold selection, per-family routing or full-baseline OR.

Seed146016 produces24 new scene configurations,48 episodes and288 frames,
including144 positive and144 negative frames, all assigned confirmation. Each
of four families has36 positives and36 negatives. Scene geometry signatures
are disjoint from the original source, but the procedural family priors,
renderer and sensor model are the same. This is controlled simulation evidence,
not natural scenes, physical-sensor testing or a safety claim. Original MZ136
test records were not loaded by the capture or evaluation scripts.

Source/model hashes were frozen before capture. The per-family requirements and
Radar audit-availability disclosure were recorded before candidate scoring in
a separate addendum, leaving the source freeze unchanged. The controller
verified317/317 file hashes, decoded288 RGB images and confirmed288/288 native
scene geometries and labels match the source. Baseline and candidate predictions
were sealed before evaluator scoring. The evaluator repeats native/source
admission after sealing predictions; maximum geometry error is8.88e-16m.

## Matched final alerts

| Arm | TP | FP | FN | Precision | Recall | Events detected | False segments |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Frozen MZ129 | 130 | 86 | 14 | 60.19% | 90.28% | 29/30 | 28 |
| Frozen MZ143 single frame | 144 | 67 | 0 | 68.25% | 100.00% | 30/30 | 27 |
| Frozen MZ145 causal | 143 | 57 | 1 | 71.50% | 99.31% | 30/30 | 21 |

| Family | MZ129 TP/FP/FN | MZ145 TP/FP/FN | FP change |
| --- | --- | --- | ---: |
| Substantial BODY | 34/22/2 | 35/3/1 | -19 |
| Suspended HEAD | 33/11/3 | 36/7/0 | -4 |
| Near rod / far wall | 36/25/0 | 36/29/0 | +4 |
| Shallow boundary pressure | 27/28/9 | 36/18/0 | -10 |

Ordinary frames improve103/58/5 to107/39/1 (108 positives,108 negatives).
Boundary-pressure frames improve27/28/9 to36/18/0 (36 positives,36 negatives).
Candidate removes53 incumbent false frames and introduces24 different false
frames, giving the net reduction29. Aggregate counts do not erase these new
errors. The single-frame arm shows much of the gain comes from representation
and learning; causal confirmation removes another10 false frames while delaying
one previously missed BODY onset by one sample relative to single-frame.

Relative to MZ129,25 previously detected event onsets are unchanged, three are
.25s earlier and one is .5s earlier; none are later. The extra recovered event
starts on time. The sole candidate FN is `mz146_substantial_body_scene4_in_00`,
which is also a baseline FN; both detect that episode at .25s. No incumbent TP
is lost. First-alert timing alone does not describe all frame-level behavior.

Both paired decisions are correct for87/144 pairs (MZ12947/144;
single-frame77/144). Underlying single-frame score order is correct128/144;
the temporal binary order has ties and is not a probability ranking. Candidate
has88 no-alert/UNKNOWN frames, including the one FN. UNKNOWN means no alert,
not a calibrated abstention or evidence of clear space. False sample-bin
duration decreases21.50s to14.25s at4Hz, not measured user interruption time.

## Gate and native-evidence accounting

The unchanged candidate passes the declared aggregate FP reduction, precision,
recall, event retention/timing and false-segment conditions. Every family's
recall is noninferior. **Only the rod-family FP condition fails.** The full joint
decision remains false; do not remove that condition after observing outcomes.

All128 potential ToF target slots and four public Radar slots are represented.
The evaluator counts27,872 returned native ToF contributor samples, including
4,751 corridor samples. No candidate non-alert frame contains any of those
returned corridor contributors. This is not proof of unsampled surface coverage,
unique return localization or preserved independent sensor alert votes.
There are467 valid public Radar returns. Per-return native Radar ownership is
`NOT_EVALUABLE` because this capture contract lacks that lineage; native AABBs
cannot substitute for it. Retain raw packets and the complete per-frame audit.

## Runtime, checks and retained evidence

One capture completed without restart:430.25s capture,549.99s owned UE process
time. UE descendants exited, the scheduled task was removed, and the exact
isolated DDC was deleted with absence verified. Worker raw capture and return
archive remain as durable evidence. Controller storage is through the canonical
`artifacts.local` junction on F:. No active compute allocations remain.

Frozen MZ129 runs in OpenCV5.0.0. Candidate uses the original CPU sklearn/OpenCV
implementation (`GPU_BACKEND_UNAVAILABLE`, same implementation as MZ143).
Measured candidate feature extraction is27.304s for288 frames (94.8ms/frame),
model plus causal readout1.566s, complete evaluation30.853s. These are controller
offline timings, not Android latency or throughput measurements. Two focused
source tests pass; both new inference/evaluation scripts compile and complete
successfully on the admitted matched capture.

Independent read-only review reproduces all metrics and the sole rod-family
gate failure, verifies677 hash bindings and all288 causal prefixes, and confirms
the frozen model and both thresholds match MZ143/MZ145. Its evidence is
`review/independent-audit.json` under this experiment's artifact root.

Evidence: `artifacts.local/work/mz146-fresh-corridor-confirmation-20260916/`:
`source/design-v1`, capture/return manifests and release receipts,
`incumbent-v1`, `evaluation-v1` predictions/seals, admission, complete metrics,
per-frame native audit and logs. Model and causal rule remain in the sealed
MZ143/MZ145 roots. Retain MZ145's consumed Development gain separately.

Intended terminal `terminal-dtr-mz146-fresh-causal-family-regression`, role
`NEGATIVE_CONTROL` for the unchanged recipe's joint all-family confirmation.
Fresh aggregate gain remains scoped evidence. A structurally different method
requires its own rationale and test; this consumed confirmation must not be
reused as fresh evidence or retuned to rescue the failed gate.

Supported registration fails at the existing ledger303 input-fingerprint
mismatch; inheritance reports unknown terminal. `decision.json` and both final
logs retain the intended scoped disposition and unchanged ledger hashes. This
metadata remains pending; no manual ledger edits or bypass were performed.
