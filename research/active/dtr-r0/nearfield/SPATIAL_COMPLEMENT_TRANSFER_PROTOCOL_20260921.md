# One frozen supplementary-policy transfer to new procedural layouts

User authorization: 2026-09-21, try the proposed single new-layout verification.
EXPLORE, prospective same-simulator Development. This is not protected final
testing, natural-scene evidence, or a safety claim. Original retained BCE test
remains unactivated; no original test labels or logits may be read.

Question: does the large consumed-dev Boundary rescue persist in independently
generated layouts under the exact frozen supplementary decision? The old model
failed full replacement; the later OR diagnostic retained A and recovered66
Boundary frames but group calibration added1 Core FP and unchanged hold added3
Boundary FP. Those original results/dispositions remain intact.

One new cohort:16 base groups, four per existing cuboid family,48 complete clips,
24 frames each at0.2s,1152 frames. Seed20260921; original parameter ranges and
material vocabulary, same camera, scene assets, rendering, single-return ToF
simulation and noise law. All INSIDE/BOUNDARY/OUTSIDE variants stay in their
group. Check camera-relative geometry against all40 original BCE groups and
the four earlier geometry cohorts, without reading model outcomes or protected
test labels. No source selected according to predictions or hard-case results.

Labels/admission use the established full rendered obstacle-volume intersection
with X=[-.3,.3],Y=[-.2,.9],Z=[.3,3]m and existing2cm Boundary band. The evaluator
may inspect native geometry for admission and labels; model inference receives
only RGB and64 single-return ranges, validity and calibrated angles. No competing
corridor surface or invisible target is admitted; any such case closes the cohort
as NOT_EVALUABLE without exclusions or replacements. Source admission on this
new Development cohort is disclosed, not blind test evaluation.

Exactly two arms: A with strong threshold0.4071309640537889 and definite bypass;
C=A OR(B logit>=7.6612162590026855). B uses the original head_last.pt, frozen
MobileNet feature checkpoint, image preprocessing and spatial head implementation.
No new fit, score calibration, rank loss, geometry screening, or threshold grid.
The high cutoff is consumed-development-derived and is frozen before acquisition.
Both arms receive identical original nonrecursive one-frame0.2s hold, reset at
clip boundaries. Retain current UNKNOWN independently of alert decisions.

Seal protocol, source, model/input/code hashes before capture. Verify capture
readiness and render bounds, materialize unchanged observations, then seal all
logits and current/held flags BEFORE evaluator label join. No model parameters
or policy choices may depend on new outcomes. Evaluate once, with all1152 frames.

Primary effect readout: Boundary current recall gain, plus per-group gains;
prospective rescue signal requires >=10 percentage points gain, gains in at
least8/16 groups, and retention of every A current/held flag and event onset.
This signal gate does not assert acceptable false-alert burden. Separately
report strict no-added-FP current and complete-event gates: Core and Boundary
each must have no added FP frames or false segments (current, and then held).
If rescue survives with costs, retain only COMPONENT evidence and quantify those
costs; if strict gates pass, retain a scoped controlled-development CHALLENGER,
without runtime promotion. If rescue fails, close this frozen supplement as a
scoped negative; source failure is NOT_EVALUABLE. No revised criterion follows.

Report TP/FP/FN, precision, recall, all-negative FPR, UNKNOWN, false-alert segments
and sampled duration, event recall/coverage, first in-event delay, pre-entry and
exit costs, per-family/group results and paired added/lost IDs. Native returned
target/corridor support is post-prediction audit only. Repeated frames are not
independent events; this16-group procedural cohort does not establish broad
generalization. No mask means no IoU claim. RGB-specific contribution is untested.

One capture, bounded1800s, one frozen inference, one evaluator join; no automatic
successor. Reuse local original UE scene and capture backend to keep acquisition
consistent; process-owned actors/UE descendants must be released in finally.
GPU-first frozen inference with existing measured backend selection; scalar
source admission and counting are TASK_NOT_GPU_SUITABLE. Mechanical recovery
requires retained receipts and unchanged scientific recipe; never discard an
unfavourable source or tune after outcomes.

Payload root: artifacts.local/work/ba-spatial-complement-transfer-20260921.
Preserve source, seals, predictions, final results, audit and registration failure
receipts. Attempt supported metadata CLI without bypassing ledger errors.
Finish scoped reports/current/disposition, focused checks, normal Git delivery;
end this experiment regardless of outcome. No demo/Android change.
