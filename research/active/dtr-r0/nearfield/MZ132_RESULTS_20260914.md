# MZ132: correct visible candidates create associations without net alert gain

**Keep MZ129; do not start RGB frontend training from this diagnostic.** On all
288 consumed frames, both anonymous visible-box and visible-mask arms remain
139 TP / 93 FP / 5 FN. BODY FP stays 30, missing the predeclared <=15 target.
Each arm removes two old BODY FP and adds two different BODY FP. All incumbent
true frames, 15 event first-alert times and 22 false segments are preserved.
No threshold rescue, candidate filtering, training or successor was run.

Terminal: `MZ132_IDEAL_VISIBLE_CONTOUR_NO_NET_GAIN`. Intended inheritance:
`NEGATIVE_CONTROL` for this complete visible-instance candidate interface with
the fixed MZ129 association/readout and required independent-coverage protection.
This does not reject RGB segmentation generally. The useful result is that
accurate candidates can establish new positive association proxies, while
unresolved merged returns and nonvisual coverage still sustain nuisance.

## Paired result

| Arm | TP | FP | FN | BODY FP | False segments | Lost incumbent TP |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| MZ129 | 139 | 93 | 5 | 30 | 22 | 0 |
| Anonymous correct visible boxes | 139 | 93 | 5 | 30 | 22 | 0 |
| Anonymous correct visible masks | 139 | 93 | 5 | 30 | 22 | 0 |

All arms have the same family totals: BODY 36/30/0, rod 34/25/2, boundary
36/35/0, HEAD 33/3/3 (TP/FP/FN). All 35 boundary FP remain in the denominator.
Maximum first-alert delay stays .25 s; every event time is identical, not just
the maximum. UNKNOWN remains 56 frames and is not clearance. False-bin duration
stays 23.25 s of this 4 Hz panel, not measured user interruption time.

The two candidates have identical frame outputs, despite different support
geometry. Removed FP are `mz123_substantial_body_pair0_out_00` and `_02`.
Added FP are `mz123_substantial_body_pair1_out_00` and `_01`.
Only2/30 original BODY FP are removed even before accounting for the additions;
the failed half-reduction target is not explained solely by the coverage repair.

## Did the contours reach actual triggering supports?

Yes. This differs from MZ130's zero-new-association result.

| Readout / support measure | Visible boxes | Visible masks |
| --- | ---: | ---: |
| Matched ToF return records, all frames | 3,296 | 3,580 |
| Previously unassociated ToF returns now assigned a proxy | 2,387 | 2,553 |
| Original FP frames with a new triggering ToF association | 32 | 34 |
| Original FP frames with a matched triggering ToF return | 63 | 65 |
| Original FP frames losing a triggering possible bit | 10 | 20 |
| Original FP frames removed | 2 | 2 |
| New Radar associations | 0 | 0 |

These are return records or frame counts as labeled, not independently verified
object identities. All ten BODY frames with changed triggering support are in
pair0. Two lose all supports. Eight still alert, each with a positive-weight
ToF component outside RGB coverage. The remaining twenty original BODY FP have
no matched triggering return. Across the 28 surviving original BODY FP, the
78 remaining positive-weight return records comprise 64 SIM_MERGED, four with
insufficient/competing zones, two unresolved signal matches, and eight matched
records whose retained outside-image part can still trigger. These counts are
records, not 78 obstacles. Radar does not explain away the principal BODY null:
all 28 still have sufficient ToF score on their own.

The two added BODY FP are also an independent-coverage effect. Both use zone58,
slot0: their visible tiles are not possible corridor support, but the retained
zone portion below image row360 is. MZ129 had limited these supports to a small
box ending at row362; the native zone reaches row369.30. The candidate preserves
that nonvisual portion, producing score0 to1. These additions must not be
misreported as RGB finding two new objects incorrectly, or removed afterward
to improve the table. The coverage protection was declared before scoring.

Radar's current association never accepts a candidate: 335 returns are ambiguous
and 13 are unassociated. The complete candidate set includes visible background
and floor, as well as all foreground objects. No semantic/hazard filtering was
used to make reciprocal assignment easier. All 172 previously associated Radar
returns therefore retain their original support through the declared unresolved
fallback; raw center support, full original flow carry and existing guards also
survive. **No Radar mask was applied.** This is an association coverage failure,
not evidence that precise Radar-associated masks were tested and ineffective.

## Source, interface and native evidence

The old source had RGB only, so a new UE pass re-rendered the exact frozen scene
geometry and camera states without calling the sensor simulator. It exports
all visible logical instances, including background/floor and each object's
noncollision texture tiles grouped with its parent. It does not infer silhouettes
from projected AABBs. Only anonymous 2D masks/boxes enter the predictor; render
IDs, source names, native bounds, distances and hazard labels remain evaluator-side.
Anonymous ordering depends only on 2D geometry.

Three fixed smoke masks are pixel-identical across the local and original worker
renderers. All 288 full frames pass native base-bound, camera, child-group, FOV,
render-target-size and exact nonempty palette checks. The result is
`GEOMETRY_PASS_RGB_NOT_IDENTICAL`: only 37 original-appearance RGB frames are
byte-identical; maximum frame MAE is .629 intensity levels and maximum channel
error25. No fitted RGB tolerance was used. Camera Rotator inputs require the
exact UE Python float32 constructor conversion; the maximum discrepancy from
that conversion is 2.22e-15 degrees, versus 4.80e-8 from raw decimal inputs.
Initial failed verifications are retained. Masks represent visible pixel centers
with AA disabled, not fractional subpixel coverage or a deployable RGB frontend.

MZ129 accepts boxes. After the user authorized the effective approach, the two
fixed contrasts were declared in the [brief](MZ132_VISIBLE_CONTOUR_BRIEF_20260914.md).
The mask arm changes overlap area and the spatial carrier, with the original
.25m cohorts, two-zone minimum, .8 coverage, .9 cosine, .1 margin, range/pose
bounds, zone weights and alert threshold. ToF mask padding remains2px. Radar
would retain one fixed plane anchor per associated box; its zero coverage here
is disclosed above. This is not a byte-identical fusion implementation.

An independent saved-output audit recomputes native truth, all frame/event
metrics and every matched ToF contributor projection. Boxes audit23,398 samples;
masks audit25,376. Neither newly excludes a previously retained sample, including
277 samples outside RGB coverage in each arm. The Radar native-refinement audit
has zero coverage because no new association/plane refinement occurs; unchanged
raw/carry/guard/fallback supports are checked separately. Do not turn that zero
coverage into a claim of correct Radar localization. Retained near-rod TP remain
alert outcomes; this experiment does not establish that their original alarms
were all supported by actual rod hits.

## Decision and delivery

The primary BODY target and no-new-FP condition fail. Accurate visible candidates
alone do not justify training a replacement frontend under this fixed complete
candidate interface, association and readout. Retain MZ129; stop this diagnostic
without filtering out background/floor, relaxing matches, deleting independent
support or trying another ToF contraction. A future change would need a separate
question and authorization, not a rescue of this consumed result.

Evidence root: `artifacts.local/work/mz132-visible-contour-20260914/`.
`capture-full-v1` holds all rendered source data and geometry/identity receipts;
`replay-v1` holds sealed anonymous candidates, both predictions, complete support
records, per-FP impact, native audits and summaries. `body-final-attribution.json`
explains surviving and added BODY supports; `independent-audit.json` checks the
saved results independently. All candidate predictions were persisted before
candidate scoring; these are consumed controlled Development data.

The existing MZ129 baseline and all288 RGB hashes reproduce in `readiness-v1`.
Four adapter tests plus the Rotator representation regression pass. Source
capture takes386.19s, with419.89s owned process lifetime on the original worker.
Host scalar prediction takes .331s for boxes and .401s for masks, excluding
source preparation, mask serialization and evaluation; not device latency.
UE/editor descendants are released. A task-owned455MB local DDC was removed;
shared worker caches and durable source/diagnostic evidence remain intact.
The worker retains its full source payload for recovery, with no running job.

```powershell
& 'E:\codex-tools\venvs\dual-loop-radial-geometry-lite-r0\Scripts\python.exe' research/active/dtr-r0/nearfield/run_mz132_visible_contour.py --capture artifacts.local/work/mz132-visible-contour-20260914/capture-full-v1 --output artifacts.local/work/mz132-visible-contour-20260914/replay-v1
```

Use a new output directory on reproduction. Registration remains blocked by the
pre-existing ledger303 input-fingerprint mismatch; intended inheritance is
recorded locally, with no manual ledger or inheritance bypass.
