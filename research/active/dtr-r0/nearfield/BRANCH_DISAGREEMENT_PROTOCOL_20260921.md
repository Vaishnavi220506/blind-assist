# Saved B/N disagreement: one bounded posthoc retention check

EXPLORE diagnostic on the already consumed1152-frame data-coverage evaluation.
No capture, feature extraction, fit, cutoff selection, native geometry query or
protected original test access. Preserve A/UNKNOWN and all existing dispositions.
B means B_control from136801f7: the old weights calibrated on the new dev split.
The original legacy B working point is a separate reference, not substituted.

Use sealed predictions and frame results to count, outside A's existing alerts,
common B/N, B-only, N-only and neither outputs. Report positive/negative frame IDs
by Core/Boundary, family, HEAD/BODY and complete layout; current and held outputs
are separate descriptive tables. Neither contains missed positives and unalerted
negatives, not emitted true/false positives.

Check all eight fixed global subsets of the three nonempty CURRENT binary
patterns. This is the complete memoryless retain/discard rule class using only
the two fixed alert bits outside A; it contains AND, OR, B, N, B-only, N-only,
XOR and A alone. Report every subset, choose/train no rule. Apply the unchanged
hold AFTER each current-frame combination; do not treat intersection of held
outputs as same-frame consensus. Preserve A in every candidate. Compute the
existing Core/Boundary incremental FP and segment caps, retained events/onsets,
and rescued/missed events. No new usefulness gate is invented.

This enumeration can test whether any rule in THIS narrow class retains rescue
within existing budgets on these consumed layouts. It is not an upper bound on
continuous-score fusion, input-conditioned or temporal verification, or any
method using new evidence. Family labels are report strata only. Reproduce the
user's family splice as descriptive arithmetic, not a runnable router or oracle.

Freeze this brief and implementation hashes before producing the diagnostic.
Keep source and output hashes, complete denominators, UNKNOWN and per-frame IDs.
Close after report/verification/structured disposition and scoped delivery. A
promising combination only motivates a separately specified unseen evaluation;
an unsuccessful check licenses no gate training or automatic successor.
