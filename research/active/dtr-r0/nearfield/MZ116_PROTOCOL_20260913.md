# MZ116: information diagnosis and resolution-limited Radar protection

EXPLORE, consumed Development. MZ115 outcomes motivated this work; it is not
fresh confirmation. Preserve every previous method/source and no threshold sweep.

1. Diagnose all returned merged ToF contributors and a correct-contributor oracle
   while preserving all other MZ115 supports. This is evaluator-only information
   potential, not a deployable output or proof that an observable method achieves it.
2. Construct a ToF-only output collision with the unchanged MZ115 reducer: same
   mean/strength/noise/status/zone but one case has a near corridor contributor.
   Do not claim equal RGB, Radar or history. No physical hardware claim.
3. Fixed observable correction: a box with width or height <=4px has no interior
   after the inherited2px working box uncertainty. Keep its proposal/association
   unchanged, but preserve current independently positive raw Radar if such a
   box otherwise cancels it. Mark HEIGHT_UNKNOWN. This is additive only: no
   invented range, new association, track refresh, ghost rejection or CLEAR.

Evaluate all768 consumed MZ107/108/109/112/113 frames against authenticated MZ113
combined_flow, and all240 consumed MZ115 frames against sealed allocation. Keep
sensor-model cohorts separate; aggregate only descriptive paired counts. For
MZ115 keep all ten existing distance points. Freeze code/parameters before this
comparison, serialize all predictions before this runner scores evaluator labels.
Prior outcomes are known; no new blind authority is implied by that sequencing.

Report paired TP/FP/FN, events, nuisance duration/count, every restored slot and
native identity. No independent ToF or prior alert may be removed. RGB absent
has no assigned tiny region and must not trigger the guard. CPU scalar/readout
work reuses authenticated RGB caches; no GPU work or new capture is required.

Acceptance: preserve all incumbent positives, report all new FP and restored
true supports. A targeted repair is not the requested overall breakthrough.
Keep mixed-range information limits separate from the pixel-resolution fix;
do not tune consumed planes or replace unknown ranges by their mean.
