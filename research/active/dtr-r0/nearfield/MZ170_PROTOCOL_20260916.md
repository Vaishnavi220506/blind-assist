# MZ170: unchanged four-expert mean on fresh scene configurations

EXPLORE controlled confirmation, 2026-09-16. Keep MZ129/default. Previous goal
turn was PROGRESS: MZ169 retained a predeclared mean control after two consumed
Development panels and independent native/causal audits. Its learned-stack arm
remains a closed negative recipe. This experiment changes the source sample only.

## Frozen hypothesis and method

MZ169 equal mean gives consumed dev24/1/0 and MZ158140/35/4, retaining all MZ129
and MZ145 true frames and first-warning times, family FP noninferiority, and
sampled native corridor support. Does that fixed combination generalize to one
new same-generator configuration sample? The experts share training/inputs;
their mean is not independent-sensor confidence.

Use exactly the MZ169 `mean-challenger-freeze.json`: original TRAIN192 MZ145
static, MZ148 unregistered and compensated, and MZ159 reflection models. For each
current frame, take their four probabilities (reflection averages its two public
views first), clip each to[1e-6,1-1e-6], convert to logit, and take an equal mean.
Causal low=-2.2479464718937394, high=-.21626992788314398, inherited from MZ146
meta calibration. Alert if current>=high OR current and preceding>=low; reset
preceding evidence across episode or non-.25s gap. No fitting or new calibration.
Nonalert is UNKNOWN. Preserve all original ToF/Radar slots and public inputs.

Implement a combined public feature adapter using the unchanged MZ159 original/
reflected feature extraction and MZ148 causal residuals. Before source dispatch,
verify all original dev48 feature arrays, four expert scores and mean outputs
against authenticated saved caches. Bind models, cutoffs, implementation,
protocol, sources and parity receipt in `recipe-freeze.json`. Full inference and
evaluation code are frozen before capture. No original test records are decoded.

## One new source

Unchanged MZ136 procedural families with fixed seed170016, renamed MZ170, all
288frames confirmation:24 scene configurations,48 six-frame episodes at.25s,
144 positive/144 negative design frames. Verify background geometry signatures
disjoint from seeds136014,146016,158016 using regenerated source design only.
No candidate predictions or captured outcomes select or replace scenes.

Keep one640x360 RGB,8x8 two-return ToF,Radar andIMU, inherited renderer/sensor
AST. One secondary-worker capture attempt,1200s owned lifetime, with the existing
recorded Zen-port allocator adapter. No automatic restart; observe/resume the
same job handle on timeout. Preserve shared worker resources. Release owned
processes/cache after path and identity checks; retain raw payload, logs, source
snapshots, transfer archive, hashes and receipts. No paid allocation.

This is fresh configuration sampling inside the same family priors, renderer
and sensor simulator; it is not new-distribution, natural, device or safety
evidence. Do not expand the source or interpret metadata validation as efficacy.

## Sealing, comparators and decision

Admit returned receipt/file hashes and source identity. In separate environments,
infer unchanged MZ129 (its original OpenCV5) and the four frozen experts/mean
(OpenCV4.10/sklearn CPU) from public observations. Seal all public predictions
before evaluator scoring. Expert arms and original static-AND-raw are disclosed
diagnostic comparators, never replacements selected after outcomes. MZ129 and
MZ145 static are the two decision comparators.

Require all on288 fresh frames:
- >=30% fewer FP than MZ129 and >=10% fewer than MZ145, with higher precision;
- every true frame of each comparator retained;
- every comparator-detected event retained with no later first warning;
- no family FP count above either comparator and no increase in false segments;
- no mean nonalert with a returned native ToF corridor contributor.

Report TP/FP/FN/TN, precision/recall, ordinary/pressure strata and family counts,
paired decisions and raw-score ordering, event retention/delays, false segments
and sampled-bin duration, UNKNOWN, support and final flags together. Native
Radar per-return lineage remains NOT_EVALUABLE; sampled ToF retention is not
full-surface coverage or proof of RGB-external completeness.

If all pass, retain a fresh same-generator controlled challenger and finish
scoped integration/evidence delivery. No App/default or product-safety promotion.
If any fails, close this fixed mean confirmation with raw gains and failures;
no weights, cutoffs, padding, family selectors, added OR or source rescue. The
broader goal remains active while other justified directions remain available.

## Runtime and recovery

One local baseline inference, one learned inference, one evaluation; each<=600s
and the local inference/evaluation phase<=1200s wall time with independent arms
allowed concurrently. Public parity preparation<=300s. Record CPU actual backend
and feature/readout times; existing OpenCV/sklearn path has
GPU_BACKEND_UNAVAILABLE, not a CUDA or Android latency result. Pure scoring and
metadata are small CPU tasks. Mechanical faults may be repaired with preserved
logs and sealed-product identity; never repeat completed scientific work or
change the recipe. Supported metadata commands only; preserve ledger303.
