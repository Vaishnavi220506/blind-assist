# Recover constructive witnesses without relaxing observations

2026-09-22 EXPLORE / numerical engineering diagnosis, newly authorized continuation.
Previous consumed deepening retained79correct model-conditional fixed-path decisions
but rejected97unique IN candidates.81endpoint cases still had valid old IN/OUT
witnesses. Unknown cause: raw MILP geometry, decoding/rounding, query membership,
or quantized replay. The rejected coordinates were not saved; reconstruct them
once in a separately named diagnostic replay, never overwrite old evidence.

## Frozen diagnostic and repair contrasts

Use exactly the258camera+bins histories sealed in observation-deepening/run-v1.
No source geometry, scene ID, truth or future trace enters candidate recovery.
Run the unchanged outer model once per label/history,516calls total, same original
1s/10000node budgets and environment. Capture raw vector, raw geometry, decoded
geometry and separate domain, query-label, and bin validation. Compare original
replayed outputs against frozen outputs; disclose differences rather than assume
reproduction. Do not rerun for a favorable candidate, sweep tolerances or enlarge
the model. Old modules remain unchanged and prediction snapshots are preserved.

One repair is fixed before diagnostic replay: for a rejected requested-IN decoded
rectangle, retain its width/thickness and project center coordinates onto the
closed interval of centers whose extent intersects the nominal query, restricted
to the original center domain. X interval[-.30-w/2,.30+w/2]; Z interval
[.30-thickness/2,3.00+thickness/2]. This is geometric projection, not an error-fit
epsilon or altered measurement. Accept only if the original unmodified observer,
original EPS-aware query and original domain validator replay every supplied bin.
Keep already valid candidates exactly; rejected OUT candidates are unchanged.
No additional MILP call. Failure remains UNKNOWN; a moved rectangle is only an
existence witness, never exclusion evidence. If exact floating-point projection
fails a fixture, fix its arithmetic before cohort execution, not using truth.

A simple comparator reuses only the validated initial-query witnesses from the
earlier observation-mechanisms run, frozen before this run. Candidates are sorted
by canonical geometry, checked against each public history and requested label.
No true source box or outcomes enter this library. This is consumed prototype
reuse, not an independently learned or deployable geometry prior. Choose first
compatible candidate deterministically. Do not union in newly solved other-case
witnesses or handpick a case's counterpart. Report recovered evidence separately
from numerical solver status and record any opposing-exclusion contradictions.

## Readout and decision

All baseline conditional decisions retain their original authority and rule:
own status0+validated witness, opposite outer-relaxation status2/no candidate.
Projection may supply the missing validated same-class witness only when own
original solver status0 holds. Cached witnesses alone never create a conditional
decision: they refine UNKNOWN reasons and audit exclusion consistency. An explicit
valid opposite witness overrides any numerical exclusion to UNKNOWN/conflict.
Report any contradictions as a defect, not as improvement. No previously UNKNOWN
case becomes a negative merely because a candidate could not be recovered.

Seal code/library/inputs before running; seal all outputs before joining old truth
and mapping each history to the five180scene modalities. Report raw/decoded/
projected validation failures, unique queries versus weighted cases, true opposing
ambiguity versus incomplete search, TP/FP/falseOUT/correctnegative/UNKNOWN, gained
and lost IDs, baseline preservation, solver calls and added validation cost.
Diagnosis improvement without new correct decisions is not recognition gain.
Single-rectangle/noise-free numerical model authority persists; no formal absence,
sensor, fresh-source, multiobject or safety claim. No new actual observation.

One scored diagnostic replay and one frozen projection only. If ineffective,
retain negative or partial result and stop this mechanism; no threshold/rounding
sweep or second recovery heuristic on the cohort. Independent saved-output audit,
focused fixture tests and scoped commit/push complete delivery. Preserve receipts
for existing metadata blockers. CPU TASK_NOT_GPU_SUITABLE; no persistent resources.
Artifacts: artifacts.local/work/ba-candidate-recovery-20260922/run-v1, refuse overwrite.
