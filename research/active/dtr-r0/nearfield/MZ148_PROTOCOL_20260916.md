# MZ148: causal background compensation as an additional observation

EXPLORE Development, 2026-09-16. MZ147 failed with a lost boundary TP; keep it
closed. MZ119 inferred metric translation from static ToF anchors and MZ135
transported whole ToF supports with feature flow. Neither method is reused here.

Question: can image residuals after dominant-background compensation improve
the frozen input representation when near ToF returns are intermittent? This
method supplies a learned image feature; it does not assert metric translation,
object depth, static-scene truth, pixel-to-ToF ownership or narrowed native
support. All MZ143 sensor/RGB features remain. Raw sensor contributors are not
pruned, and absent residuals do not establish free space.

Use current image and causal lags1,2,4 at .25s spacing. Fit ECC homography on
320x180 grayscale (50 iterations, epsilon1e-5, Gaussian5); warp original-size
reference into current coordinates. Accept finite converged alignment only with
correlation at least.8 and overlap at least.6; supply validity markers otherwise.
Remove median residual brightness bias, then summarize signed/absolute/edge
residuals and column persistence in a fixed8x16 grid. Episode/gap resets; no
future frames or backdating. Dominant-plane alignment may absorb foreground
motion or leave parallax/occlusion/exposure errors; the learner must cope with
that ambiguity. Fixed uncompensated residuals provide an explanatory ablation.

Declare compensated residual + existing MZ143 fused features as the candidate;
uncompensated + fused is diagnostic only. Both use unchanged MZ143 HGB settings,
TRAIN192 only, four original whole-scene OOF folds, TRAIN-derived low cutoff
and MZ145 high onset calibration. No sweep or selecting the ablation after dev.
Refit once and seal before Development predictions. IDs/family/geometry/truth
do not enter feature values; episode/time serve causal history control only.

First compare on consumed dev48: require all24 MZ129 TP, all five event first
alerts without extra delay, at most3 FP, no family FP increase against MZ129,
at most two false segments and no non-alert containing a returned native ToF
corridor contributor. This matches the useful MZ145 Development result rather
than declaring a weaker model successful against an easier comparator.
If it fails, stop without MZ146 candidate scoring, retuning or capture.

Only if that gate passes, score consumed MZ146288 as additional Development:
at least143 TP, every MZ129 true frame/event retained without extra delay,
fewer than57 FP, at most21 false segments, no family FP increase over MZ129,
and no non-alert containing a returned native ToF corridor contributor. Report
the full nuisance, pair, pressure, UNKNOWN and timing picture. MZ146 is already
consumed; a positive result requires separate unchanged fresh confirmation.

One run,600s cap, no original MZ136 test records and no new capture. Snapshot
code, hash input feature caches, preserve all outputs including failed gates.
CPU OpenCV/sklearn: GPU_BACKEND_UNAVAILABLE. Measure the real operation rather
than a no-op probe. All artifacts stay under
`artifacts.local/work/mz148-background-residual-20260916/`; processes exit and
no persistent or paid allocation remains. Mechanical recovery does not permit
changes to features, model parameters, thresholds or gates after scoring.
