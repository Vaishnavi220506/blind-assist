# One fixed multi-return feasibility pilot

2026-09-20. User selects controlled simulation and feasibility review. This is
consumed Development using all 36 layouts / 432 frames of
`artifacts.local/work/ba-core-workpoint-transfer-20260920`. It deliberately
includes the two known delayed HEAD onsets. It is not a fresh confirmation,
hardware experiment, or the proposed 24 complete approach/dwell/leave recordings.
The latest 74/10/0 Core demonstration and all frozen sources remain unchanged.

## Capability and falsifier

Question: does retaining a second separated synthetic return expose actual native
corridor contributors and improve the existing task decision at the frozen strong
threshold, including the cost on every Core negative and the Boundary stratum?
The old audit identified selection loss; it did not establish usable second
returns. This pilot changes the declared observation, not the scalar threshold.

One fixed hypothetical sensor law, no fit, grid, phase change or retry:

- Original nominal 45x45 / 8x8 boxes, depth lattice, 10 cm bins, inverse-square
  histogram, range [0.1,8)m and 5% dropout are unchanged.
- Slot 0 is bit-identical to the original noisy strongest-bin value, including
  missingness. It is never replaced by reference truth.
- Only if slot 0 is observed, slot 1 may be the highest-weight OTHER bin with
  at least 4 samples, at least 1% of winner weight, and mean depth separation
  at least 0.6 m from the original winner. Ties choose the lower bin index.
  These count/weight criteria are explicit uncalibrated assumptions, not ST
  detectability specifications. This optimistic bin law is not peak firmware,
  does not merge adjacent bins or model photons, materials, multipath or ambient.
- Slot 1 uses sigma=0.01+0.02*mean and a separate SHA-seeded zone stream. Original
  dropout/noise are unaffected. Reject noisy range outside [0.1,8)m. Both slots
  share original packet dropout; per-target dropout is not validated.
- Keep distance, synthetic sigma, proxy weight and SIM_VALID/SIM_NO_RETURN for
  each slot. They are synthetic quality, not measured range_sigma/confidence.
- Native depth is used only to generate new simulated observations. Pixel
  indices and ownership stay in evaluator-side files, never inference inputs.

## Three paired readouts

`strongest` uses original slot 0; `closest_exported` chooses the minimum finite
value from the same two slots; `two_returns` scores each slot separately and ORs
the independent definite/strong geometric decisions. It never fills the gap
between the supports, sums their scores, or invents sub-zone location. All use
T=.4071309640537889, the unchanged full-support geometry and definite bypass.
Report current decisions and identical nonrecursive one-adjacent-frame hold for
each readout. Keep Calibration at T0=.007085703945147101 as historical reference.
Held-only output is continuity, not additional current sensor evidence. UNKNOWN
retains its original meaning and silence does not prove free space.

## Execution and interpretation

Freeze protocol, input/code hashes; regenerate with exact original-slot parity;
seal observable arrays; predict without labels; seal predictions; then join
labels and native contributor attribution. Report TP/FP/FN, precision, recall,
FPR, event recall/first alert, false segments/sampled duration, BODY/HEAD and
background subgroups, Boundary separately, paired IDs and current UNKNOWN.
Audit whether recovered positive frames have native corridor contributors in
the added slot. This is a task detector: no mask/IoU improvement is claimed.
Scalar and small-array geometry runs on CPU: TASK_NOT_GPU_SUITABLE.

Prospective decision: a usable fixed readout needs at least one Core FN recovery
backed by added native corridor support, no Core TP loss or onset delay, and no
increase in Core FP count, false segments or sampled duration against its paired
strongest counterpart. Failure closes this fixed automatic-use recipe, not all
multi-return information. Additional contributors with excess FP are information
headroom only and do not automatically authorize training. Zero new support
stops this mechanism under this proxy. Regardless of outcome, no tuning, new
cohort, training, runtime promotion or successor follows in this round.

## Whole-route feasibility

Input retention is implementable, but real weak-return existence is still
NOT_EVALUABLE. Current packets are not acquired VL53 measurements. Small spatial
models/HGB and paired supervision are technically possible only after useful
observable information and grouped train/dev acquisition are established.
BODY/HEAD scores are not claims of precise intrazone localization. Complete event
evaluation needs approach/dwell/leave; the selected 12-frame approach clips cannot
measure cancellation, exit lag, repeated audio burden or real stream jitter.
Low cost needs a BOM; low interference needs episode/audio metrics. Preserve the
scoped camera-forward upper-body research positioning and measured limitations.
