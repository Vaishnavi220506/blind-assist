# Boundary counterexample separability: frozen information check

2026-09-22, EXPLORE. User authorized the proposed check after the fixed local
transfer failed zero-new-error conditions. Question: can the existing four12cm
actions distinguish opposite corridor relations that share initial observations,
or is the information absent under this sensor/action budget? No new selector,
model, thresholds, prior-bank expansion or alert policy is authorized here.

## Consumed mechanism evidence

Inspect saved transfer sources/initial and selected observations, plus original
bank forecasts, for the four newly wrong active decisions. Keep every surviving
opposite-label bank witness (five pairs for four sources). Do not create extra
views of these consumed cases. The shape tolerance alias and old false OUT remain
historical evidence; this test uses only the existing2D active sensor law and
cannot measure a3D silhouette/ToF shape-branch repair.

## New source fixed before any observations

Exactly90 pairs/180 scenes from the Cartesian product:
side in {-1,+1}, width in {.06,.18,.36}m, center depth in
{.85,1.35,1.85,2.35,2.85}m, boundary margin in {.005,.015,.030}m.
Each pair contains single boxes at X=side*(.30+width/2-margin) and
X=side*(.30+width/2+margin), identical depth/width/.04m thickness and4.20m wall.
The first intrudes by margin; the second clears by margin. Query is the original
fixed camera-frame full-solid rectangle. All180 geometries must be unique and
disjoint from both old653 and prior transfer1304 scenes; fail admission on overlap,
never filter outcomes. All pairs and initially distinguishable cases remain in
the output. No source search, alternative grids or margin selection after results.

Same8 ideal rays, .10m radial quantization and four actions +X/-X/+Z/-Z of.12m.
Known exact camera pose, noiseless rectangles and the original finite bank are
unchanged. Freeze source/config/code/protocol before observations. Generate
initial observations; freeze fixed, old-adaptive and positive-priority choices
from initial bins and original public forecasts before any actual future view.
Then enumerate all four actions **as an evaluator-only information diagnostic**.
Seal complete observations before opposite-truth evaluation. This is5 views per
scene/900 simulated view calls, not a two-view implemented policy. Chosen-policy
comparisons look up only their one sealed action; policy-equivalent cost is one
initial plus one12cm view. No oracle action is handed to a selector.

## Accounting and decisions

First partition pairs into initial bin signatures different/equal. Only the equal
subset can show new information from motion. For every such pair report:
separation by each action; existence of any separating action (pair-aware oracle);
fixed/old/positive action separation; missed separating opportunities and exact
IDs. Separating one witness pair does not prove correct classification against
all unknown geometries, and no TP/FP or new-policy accuracy is claimed.

Group equal-initial pairs by their shared initial bins. Compute the best number
of pair separations achievable with **one common action per observation class**,
with original action order ties. This is an evaluator-optimized class ceiling,
not a trained/executable selector or independent generalization result. Report
its gap to pair-aware oracle, fixed, and positive-priority. Also check whether
one action can separate every pair in each class; never select by hidden pair ID.

Complement the paired result with full180-scene opposite-label ambiguity: within
each initial signature, partition by each action's future signature and count
scenes whose label remains mixed. Report per-case best-action and class-common
best-action resolvability separately; those are finite-cohort evaluator ceilings,
not unrestricted geometry guarantees. No abstaining cases are dropped.

For pairs equal at all five quantized views, inspect raw analytic radial distances
from the SAME rays/poses, without changing the observation law: raw differences
at any view indicate loss through quantization; otherwise classify as equal raw
ray returns at numerical1e-12m tolerance (not a sensor accuracy assumption). Raw
ranges are evaluator-only and unavailable to existing selectors. Report actual
differences and do not sweep resolutions. No additional poses or sensor types.

- If no equal-initial pairs exist: initial-ambiguity question NOT_EVALUABLE.
- If none is separated by any allowed action: no action-information benefit on
  this exact cohort; retain UNKNOWN and do not build a selector here.
- If some separate: preserve the scoped information opportunity. If class-common
  ceiling exceeds current positive choice, this supports a later public-input
  mechanism investigation, not an implemented improvement. Split missed cases by
  original-bank initial no-match versus matched; absent prior coverage must not
  be mislabeled solely as a bad observation objective.
- Irreducible pairs bound claims locally. Mixed outcomes retain both opportunities
  and failures. No automatic successor, bank/tolerance change or rerun follows.

One scored cohort run, small standard-library CPU geometry
(TASK_NOT_GPU_SUITABLE), focused harness tests and independent saved-output
equation/count audit. Output under artifacts.local/work/ba-boundary-separability-20260922/run-v1;
refuse existing directory, preserve failure/metadata receipts, no persistent
worker or allocation. Same analytic generator with deliberately paired new
geometries is synthetic Development, not independent-source/natural/hardware
evidence or a continuous-geometry impossibility theorem.
