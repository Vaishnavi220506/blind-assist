# One frozen Core working-point transfer

2026-09-20. User-authorized conditional follow-up after the consumed-score
diagnostic found substantial headroom. Exactly one candidate and one new cohort.
Baseline Calibration stays0.007085703945147101; candidate is the diagnostic
event/onset ceiling **0.4071309640537889**. Keep the exact geometric score,
definite bypass, sensor generator, full intervals, UNKNOWN and camera profile.
No RGB/noRGB head, training, score redesign or extra threshold is evaluated.

The consumed Core288 diagnostic is71/10/1 versus72/126/0, with all12event onsets
retained and one0.2s trailing silent bin. Its BOUNDARY events fall12/12 to1/12.
This severe contact-coverage cost is disclosed before validation; the candidate
is Core-specific and cannot replace the full strict-task baseline on that basis.

## New source, fixed before capture

`core_workpoint_transfer_spec.py` defines36 complete clips/432 frames: six cuboid
geometry types x INSIDE/BOUNDARY/OUTSIDE x two backdrops,12 samples at0.2s.
All target dimensions, heights, signed placements, camera paths and backdrop
geometries change; source IDs and exact geometry signatures differ from the
selection cohort. First entry varies with type (1.0s or1.2s), not always1.2s.
Actual camera height is1.82m, matching the stored old specification. Volume stays
X[-.3,.3],Y[-.2,.9],Z[.3,3]m; complete extents and2cm boundary tolerance stay fixed.
Same Willow map, cube library, materials, rendering and nominal45 single-return
proxy remain explicit limitations: new arrangements, not natural or hardware
validation. No scenes are chosen or replaced from observed outcomes.

Reuse authenticated existing capture/source-admission helpers: exact spec/assets,
camera/mesh bounds/trace/readiness, visible target, <4 competing corridor pixels,
source seals and process-release receipts. Source failure is NOT_EVALUABLE;
retain logs and do not replace failed configurations. Mechanical fixes may keep
unchanged scientific inputs with exact repair receipts. Capture budget is one
complete432-frame run; launcher existing3600s bound covers startup/settling.

Freeze hashes and the candidate before capture. Construct observations from
native source in the source stage only; prediction receives public boxes/ranges
and scalar thresholds. Seal both complete432 prediction vectors before evaluator
label join. Reuse full-layout Core and Boundary accounting, plus full432 results.

## This pilot's acceptance and costs

Against the unchanged baseline on the same new frames require:
- retain every detected Core event and its first in-event alert, with nonempty
  Core events and negative opportunities;
- reduce Core false-alert frames/duration by at least25%, with no increased
  Core false segments; OUTSIDE and INSIDE-negative FP/segments each nonincreasing;
- each Core event is alerted for at least5/6 of its positive frames and has no
  contiguous silent run longer than0.2s, including a trailing run.

The25% materiality criterion and5/6/.2s continuity limits are fixed for this
single pilot, not permanent project requirements. They make the diagnostic's
observed tradeoff testable before new outcomes. No threshold adjustment follows
failure. Report all lost TP, subgroup counts, precision/recall/FPR, event coverage,
silence, pre-existing alerts, clip-first times and both negative phases. Boundary
costs are mandatory reporting, not an undisclosed Core tuning criterion.

Stop after one evaluation and delivery regardless of outcome. Pass retains only
a bounded Core-specific challenger; no App/default-baseline replacement or safety
claim. Fail closes this fixed candidate's transfer role, with the positive
consumed diagnostic intact. No second candidate, new seed or automatic algorithm
successor. Store all evidence under
`artifacts.local/work/ba-core-workpoint-transfer-20260920/`.

Placement: current primary host has the authenticated source assets and launcher
from the preceding transfer; the inspected worker has an older source revision.
Use the resident primary capture path for this bounded run to avoid synchronizing
a different UE source environment. UE renders on its configured graphics device;
source construction/scalar readout/accounting use CPU (TASK_NOT_GPU_SUITABLE).
Release all task-owned UE descendants; retain source/prediction/evaluation seals.
