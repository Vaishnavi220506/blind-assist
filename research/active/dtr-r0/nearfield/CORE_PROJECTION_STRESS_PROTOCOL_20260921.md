# Frozen Core: one horizontal projection-error sensitivity check

2026-09-21 EXPLORE. The user authorized advancement and clarified that hardware
is not connected and simulation should be used. Test one new input question,
not a repair of the consumed spatial-head or conditional-hold recipes.

## Question and fixed change

Does the retained Core policy keep its bounded event/false-alert benefit when
the reported ToF-to-camera projection has a small horizontal registration error?
The hypothesis is that full-zone support and fixed strong-plus-hold retain the
Core benefit under this specified error. Failure identifies calibration
sensitivity, not absence of RGB/ToF information or physical sensor performance.

Reuse all 1152 already-consumed frames / 48 complete clips / 16 layouts from
`artifacts.local/work/ba-spatial-complement-transfer-20260921`. There is no new
capture, fresh confirmation, training, protected-test access or source exclusion.
Keep every original range, validity, zone ID, timestamp, RGB identity, depth
interval and truth label. The tested decoder is geometry-only; this check cannot
attribute RGB benefit. Native depth/ownership are not needed or read.

The ONLY variable is a uniform horizontal translation of reported zone boxes:
`dx = -2, 0, +2` on the existing 256x192 projection lattice. Both horizontal
edges move together; zone size/order and vertical edges remain unchanged. This
equals -5/0/+5 pixels in the 640x360 native image, approximately 5.59 cm of
horizontal projection error at axial Z=3 m with the existing 100 degree HFOV.
The magnitude is a declared engineering stress point, not a measured device
error, tolerance bound, probability distribution, rigid yaw or sensor movement.
Original measurements are NOT regenerated from the shifted boxes. They retain
their original true footprint while inference receives the erroneous projection.
Both signs are mandatory symmetry controls, not candidates to select between.

For each input condition use unchanged Calibration T0=.007085703945147101,
strong T=.4071309640537889, definite-support bypass and nonrecursive .2 s
one-frame hold. Only previous genuine strong evidence extends an alert; reset
at clip or timestamp gaps. UNKNOWN uses the unchanged support rule under each
reported geometry and may change with that geometry; retain nominal UNKNOWN
as a comparison, never recode silent uncertainty as clear space. In particular,
an erroneous projection can produce erroneous definite-support decisions.

## Execution and evidence

Freeze this protocol, code identities and parent seals before prediction. Verify
all selected inputs against their existing seals. Hash labels as opaque files
for binding, but do not parse evaluator labels/metadata before prediction seal.
Zero-shift scores, current alerts, UNKNOWN, valid/definite counts and held alerts
must reproduce every stored A result; stop for an implementation mismatch.
Seal all three conditions' predictions before the evaluator join. Evaluate each
sign separately and the worst condition, without choosing a threshold or offset.

Reuse complete-event metrics and report Core/Boundary separately, all-negative
FPR, TP/FP/FN, event coverage/onset/gaps, false segments/duration, pre-entry and
post-exit burden, release timing, UNKNOWN and BODY/HEAD/layout subgroups. Also
report every changed frame versus nominal, false definite-support alerts and
nominal-positive losses, without changing cohort membership. Sampled durations
remain sample count times .2 s, not continuous real-time or voice burden.

Prospective descriptive robustness criteria reuse the prior Core usefulness
limits: all 16 Core events detected; each first in-event delay <=.2 s; each
event coverage >=5/6 and no silent run >1 sample; at least 50% fewer Core FP
than Calibration under the SAME input condition; nonincreasing Core false
segments and INSIDE-negative/OUTSIDE FP and segments versus that Calibration.
These are Development-informed criteria, not safety limits. Also report full
cost deltas against zero-shift strong+hold; passing relative to Calibration is
not evidence that misregistration causes no degradation. Historical outcomes
and gates are not revised.

Budget: one deterministic three-condition readout and one evaluation, followed
by a saved-output verification. CPU scalar 64-zone geometry is
TASK_NOT_GPU_SUITABLE; no network, model, UE or paid worker is required. Store
outputs under `artifacts.local/work/ba-core-projection-stress-20260921`.
Mechanical failures may be corrected with receipts while keeping scientific
inputs fixed. Stop after the result: no alternate magnitude, retuning, new
training, automatic successor, demo/default change or hardware claim.

If both signed conditions pass, retain this narrow sensitivity evidence as a
COMPONENT; if either fails, retain a diagnostic COMPONENT identifying the
failed condition and event/cost, without promoting a robust policy. Preserve
the original Core and all earlier negative controls in either case. Attempt
supported registration/inheritance and retain any existing metadata blocker.
