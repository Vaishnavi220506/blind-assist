# 对照回放交付 · 2026-09-22

本轮交付是可运行的证据浏览器，未修改告警主线、模型、校准或研究结论，也未创建新实验终点。使用已消费的 data-coverage / branch-disagreement 数据；没有读取新保护测试集。

## 可检查的结果

| 保持后 | A：TP/FP/FN | 一致性补报：TP/FP/FN | 事件 A→对照 | 误报片段 A→对照 |
| --- | --- | --- | --- | --- |
| Core | 159/25/17 | 172/26/4 | 15/16→16/16 | 20→21 |
| Boundary | 11/5/165 | 62/10/114 | 3/16→10/16 | 4→7 |

覆盖全部 16 布局、48 片段、1152 帧。导航标签含 10 个有新增正确报警的片段、3 个有新增误报的片段、6 个对照仍整段漏检的事件片段、8 个由 A 整段漏检变为对照检出的事件片段；标签可重叠。183 帧的 UNKNOWN 与对照保持后告警并存，界面独立呈现。

Core 局部收益仍成立，Boundary 成本仍超预算。导航和真值不参与算法路由。事件检出不代表事件全覆盖；时长是采样时长，不是端到端系统延迟。

## 验证

- 4 项 Python 测试覆盖片段重置、不可递归保持、身份拒绝、双模型一致性、事件前误报及事件内延迟。
- 12 项 JavaScript 断言覆盖筛选、无事件/未检出的区别、补报解释及独立 UNKNOWN。
- 独立审计核对封存依赖、1152 帧、96 组片段指标、所有事件/误报区间、原成本门槛；独立重建 1152 张 JPEG 与来源和显示哈希匹配。
- 实际浏览器检查默认 A、补漏/误报/整段漏检筛选、空结果、无事件显示、告警与 UNKNOWN 并存、逐帧定位、末帧重播及自动停止、切换片段重置；1440 和 375 像素视口没有页面横向溢出。检查中无 console error/warn。
- 本地 HTTP 预览完整可用。浏览器工具的 URL 策略拒绝 file://，未绕过；直接双击本地 HTML 尚未实测。包采用本地脚本和图片，不使用 fetch、外部 CDN 或远程依赖。
- 仓库结构检查未通过：既有 PROJECT_STATE、CURRENT_DECISION、路线 CURRENT 超预算，且存在 scripts/research 冷目录。本轮未修改这些文件；新回放目录与忽略产物路由已局部核对。

## 交付位置

仓库相对路径 `artifacts.local/work/ba-consensus-replay-20260922/`：

- `site/index.html` 与 `site/launch.cmd`：入口。
- `consensus-replay.zip`：完整可移动包。
- `site/build-receipt.json`：数据和图片来源凭据。
- `independent-replay-audit.json`：独立审计。
- `delivery-receipt.json`：最终包哈希与工程检查记录。

这是工程展示交付，不重复登记为科学实验。历史账本错误仍不由本轮修复或绕过。复现方法见本目录 README。
