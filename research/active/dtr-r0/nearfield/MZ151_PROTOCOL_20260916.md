# MZ151: expand real training scenes, then confirm the unchanged learning recipe

EXPLORE, 2026-09-16. MZ150's fitted/whole-scene OOF gap motivates a training
coverage test. Use the prior MZ143/MZ145 representation and learning/readout
recipe, not MZ150 virtual queries or MZ149 persistence. One candidate only.

Merge original MZ136 TRAIN192 with all already-consumed MZ146288 as TRAIN480:
40 physical scene groups,80 episodes,240 positive/240 negative frames. MZ146
is now explicitly training evidence and cannot validate this new model. Original
MZ136 dev48 stays outside fitting and calibration; original test records stay
unopened. Reuse authenticated original2,485-dimensional public feature matrices.
No native geometry, scene identity or source partition is a predictor feature.

Preserve the original four-fold assignments for TRAIN192. Sort the24 MZ146
scene-group IDs and assign successive groups cyclically to folds0..3; all paired
episodes and six-frame sequences remain together. This gives120 frames in each
fold. Fit unchanged HGB150 iterations/.05 learning rate/seven leaves/minimum
leaf8/L2=1/seed143016, no early stopping. Calibrate the unchanged MZ145 low/high
rule only on TRAIN480 OOF scores using incumbent MZ129 true frames; refit once
on all480. Record per-source/family OOF outcomes, then seal model/cutoffs.

First score consumed dev48. Require24TP, no lost incumbent event or delayed
first alert, FP<=3, false segments<=2, no per-family FP increase versus MZ129,
and no lost returned native ToF corridor contributors. If this gate fails,
stop before new capture; preserve any source preparation and exact outcome.
Do not change folds, cutoffs, tree parameters or data membership after scoring.

Only after dev passes, freeze one new source at seed151016: same unchanged
paired generator/renderer/sensor model,24 groups48episodes288frames, all assigned
confirmation. Geometry signatures must be disjoint from seed136014 and146016.
Source/native admission is outcome independent. Bind the candidate, frozen
original MZ145 and MZ129 code, cutoffs and this protocol before capture.
One capture, maximum1200s; no model-outcome selection or source replacement.

Fresh comparison includes MZ129, original MZ145, and expanded-training candidate.
Candidate must reduce FP by at least30% versus MZ129 and strictly versus MZ145,
improve precision versus MZ129, retain every MZ129 true frame/event with no later
first alert, and have recall no more than2percentage points below MZ145. Each
family's FP must not exceed MZ129; false segments must not exceed either
comparator. No candidate non-alert may contain a returned native ToF corridor
contributor. Report every family and pressure stratum, pairs, timing, raw counts,
UNKNOWN, removed/new errors and compute cost. UNKNOWN is not clear space.
Native per-return Radar lineage remains NOT_EVALUABLE; all public slots remain.

One training/dev run capped at300s on existing CPU sklearn implementation
(GPU_BACKEND_UNAVAILABLE). If admitted, one fresh evaluation capped at300s.
Model and predictions are sealed before matched fresh evaluator parsing.
These are same-generator controlled simulation claims only, with no App default
or physical/natural/safety promotion. Supported registration may remain pending
ledger303; do not bypass it. All payloads live under
`artifacts.local/work/mz151-expanded-training-20260916/`. Release owned processes,
temporary worker DDC/tasks, preserve raw evidence and handoff records.
