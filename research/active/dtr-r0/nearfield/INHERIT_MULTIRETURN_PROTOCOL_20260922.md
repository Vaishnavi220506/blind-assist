# Inherited multi-return: one wider-distance Development check

2026-09-22, authorized EXPLORE. Question: does the already retained hypothetical
second-return observation preserve useful obstacle information and improve the
fixed current/held readout on wider-distance, varied-size complete trajectories?
The old 432-frame current-frame gain and 576-frame Core held equality remain
intact. This is a different consumed cohort, not another fit of either old pilot.

Use only the existing query-occupancy evaluation partition: 576 frames, 16 base
layouts, 48 clips, 12 poses per clip. This is consumed same-generator controlled
Development, not fresh confirmation. The source poses approach and depart across
about 0.47--4.03 m; there is no dwell phase and no real-time walking evidence.
No new capture, training, hardware connection, default promotion or successor.

The unchanged `simulate_two` from `multireturn_pilot_20260920.py` constructs
8x8 two-slot public observations from saved native optical depth. Preserve the
original first-slot seed `query-occupancy/` + `sensor_noise_key` and assert exact
parity with the previously saved ToF values, including missing values. The second
slot uses the old 10 cm bins, >=4 samples, >=0.6 m native-mean separation,
>=1% strongest proxy weight, strongest qualifying bin, independent noise stream
and original first-slot dropout. No quantization, peak, noise or weight change.
This is additional hypothetical sensing, never an algorithm gain at equal input
or calibrated physical multi-target detectability. No CH/CNH hardware work.

Freeze three readouts: strongest, closest among the two exported slots, and
independent two-return decisions combined by OR. Every return keeps its separate
geometric support. Use the existing score, threshold 0.4071309640537889,
definite-support bypass and UNKNOWN semantics. Add the unchanged nonrecursive
one-frame hold (only a true current alert at the immediately preceding 0.2 s
sample renews support), giving six arms. No labels, native arrays, object bounds,
lineage, family or layout relation enter inference. Public identities contain
only sample index, id, clip, frame and timestamp.

Execute construct, predict and evaluate separately through `tools/ba.ps1 run
research-ue -RunSpec`. Construct may read native depth solely to simulate the
sensor and store private indices; it does not compute or read truth. Seal public
observations before prediction. Prediction reads only that public bundle and
the frozen plan, then seals every decision. Only evaluation joins labels,
original A decisions and native contributor geometry. Record SHA256 input,
code, protocol and output identities and preserve all failed receipts.

Report all-frame and complete Core (INSIDE+OUTSIDE)/Boundary, family, layer,
relation and base-layout counts; TP/FP/FN, precision, recall, FPR, UNKNOWN,
event counts, first in-event delay, interruptions, exit carryover, false segments
and sampled false duration. Retain all per-frame decisions and event identities.
Audit actual triggering-slot contributors against native target-in-corridor
pixels, separating restored information from final held decisions. No masks are
predicted, so no IoU or fine localization claim is available.

Retain information COMPONENT evidence if a native-backed new current TP exists;
this alone cannot justify an alert upgrade. A Core alert challenger requires a
native-backed positive-frame recovery or earlier onset/fewer gaps, with no Core
TP/event loss or delayed onset and no increase in Core FP, false segments,
sampled false duration or exit carryover. Report Boundary costs separately. The
two-return complexity has contribution only if it differs usefully from closest.
These criteria are descriptive Development retention, not product/safety gates.
If there is no useful gain, preserve the negative result and stop; do not tune.

Budget: one 576-frame generation, one sealed six-arm prediction, one 576-frame
evaluation; synthetic checks and saved-output/native independent audits are
validation, not extra scientific runs. Mechanical defects may be repaired with
their failed receipt and identities preserved, without changing the mechanism.
Small per-zone histogram/geometry work uses CPU TASK_NOT_GPU_SUITABLE, recording
host time including I/O, never claiming device latency. No persistent service.
