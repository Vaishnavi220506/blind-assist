# MZ164: camera-aware metric depth alerts on every frame

EXPLORE consumed original TRAIN192, 2026-09-16.
[Protocol](MZ164_PROTOCOL_20260916.md).

Decision: **MZ164_METRIC_PRIOR_NO_UNANCHORED_ALERT_GAIN**. The frozen known-camera
UniDepthV2 prior alerts on all192 frames: MZ129's91TP/46FP/5FN becomes96/96/0.
Five recovered true frames cost50 new false frames. Keep MZ129; close this exact
model, camera adapter and connected-corridor OR recipe as an alert negative
control. No threshold, resolution, checkpoint or fitting rescue follows.

There is limited metric agreement without target ToF returns, including one
newly alerted rod frame with1910 connected, depth-correct target pixels. That
same frame has29419 known false corridor pixels. Near-uniform predicted depth
fields can match a target's distance while also pulling its background forward;
the result does not establish useful target/background separation. The terminal
name denotes failure of useful joint alert benefit, not zero geometric agreement.
The broader obstacle-improvement goal remains active.

## Alert results

All arms see the same192 consumed TRAIN frames,96 positive and96 negative.
The predeclared candidate supplies public camera intrinsics; estimated-camera
inference is a control, not a retrospectively selected replacement.

| Arm | TP / FP / FN / TN | Precision | Recall | UNKNOWN | False segments | Both-correct pairs |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| MZ129 | 91 / 46 / 5 / 50 | 66.42% | 94.79% | 55 | 13 | 46/96 |
| Known camera | 96 / 96 / 0 / 0 | 50.00% | 100.00% | 0 | 20 | 0/96 |
| Known camera OR MZ129 | 96 / 96 / 0 / 0 | 50.00% | 100.00% | 0 | 20 | 0/96 |
| Estimated camera | 81 / 87 / 15 / 9 | 48.21% | 84.38% | 24 | 22 | 2/96 |
| Estimated camera OR MZ129 | 95 / 91 / 1 / 5 | 51.08% | 98.96% | 6 | 22 | 5/96 |

No candidate family distinguishes positive from negative: each has24TP/24FP/0FN.
MZ129 rod counts are22/9/2, boundary22/20/2, BODY24/12/0 and HEAD23/5/1.
Ordinary frames change69/26/3 to72/72/0; pressure frames22/20/2 to24/24/0.
The OR preserves all old true frames and20 detected events by construction.
The one baseline0.25s delay disappears, but continuous alerting cannot establish
useful event detection. False-alert sampled-bin duration rises11.5s to24.0s at
the source's4Hz cadence; this is not measured real-user interruption duration.
UNKNOWN means no alert, never evidence of clear space.

Of six declared checks, only `no_new_fp` fails. The other conditions include
true-frame/event retention, native retention and at least one depth-correct
no-target-return gain; passing them does not override the50 added false frames.

## Geometry and absent-return evidence

Reference is the saved source's analytic first-hit AABB geometry, not complete
rendered depth. Agreement requires absolute camera-axis depth error <=0.12m;
a recovered column needs agreement on at least half its target pixels. All
target pixels have finite model depths in both arms; finite coverage alone is
not successful geometry.

| Family,48 frames each | Estimated-camera recovered columns | Known-camera recovered columns | Known target MAE | Known too-near ring |
| --- | ---: | ---: | ---: | ---: |
| Rod | 141/423 (33.33%) | 75/423 (17.73%) | 0.447m | 62.47% |
| Boundary | 206/955 (21.57%) | 56/955 (5.86%) | 0.576m | 58.59% |
| BODY | 1393/8874 (15.70%) | 1452/8874 (16.36%) | 0.427m | 88.00% |
| HEAD | 152/3004 (5.06%) | 92/3004 (3.06%) | 0.717m | 21.71% |

The8pixel ring around the known silhouette measures background pulled too near.
Known-camera local surface precision is rod27.35%, boundary46.09%, BODY95.33%
and HEAD42.29%. Known intrinsics do not make the prior more accurate overall.

Returned contributor lineage and public slot status divide the192 frames into
149 VALID-supported, one MERGED-only and42 with no target-containing returned
ToF slot. These evaluator-only strata never enter prediction and do not certify
Radar absence. A native ray hit without a returned contributor is not counted
as a target observation.

| No target-containing returned ToF slot | Frames | Known recovered columns | Estimated recovered columns |
| --- | ---: | ---: | ---: |
| Rod | 23 | 31/166 | 52/166 |
| Boundary | 3 | 0/70 | 43/70 |
| BODY | 3 | 0/660 | 407/660 |
| HEAD | 13 | 87/687 | 152/687 |

Absent-return rod agreement is18.67% of columns in the candidate, but its ring
too-near rate is74.09% and local precision28.01%. The one MERGED-only rod frame
recovers0/9 columns in both arms. Earlier MZ153's24frames/175columns without a
VALID target return combine these two strata; those denominators must not be
treated as23frames/166columns with no returned target evidence of any status.

All five newly true candidate frames have no target-containing returned slot.
Only `mz136_near_rod_farwall_scene3_in_01` has a depth-correct connected target
patch (1910pixels). The other rod, HEAD and two boundary gains have zero such
pixels, despite some silhouette overlap. The qualifying rod frame's29419 known
false corridor pixels prevent interpreting its1910 correct pixels as a distinct
reconstructed obstacle. Four fixed, preselected saved-map examples show nearly
flat model fields; the complete plot and selection receipt are in the artifact
tree. No model was rerun to make the plot.

The native audit covers16537 returned ToF contributor samples,3193 in the
corridor. Neither OR arm suppresses an old alert or sensor input. The standalone
estimated-camera arm has10 nonalert frames containing native corridor support.
The candidate's zero such frames is trivial under all-frame alerting, not proof
of faithful surface reconstruction. Per-return native Radar lineage remains
NOT_EVALUABLE under the inherited source contract.

## Implementation, access and cost

The official metric-trained UniDepthV2 ViT-S is distinct from MZ157's relative
DA2 model plus ToF calibration. Source commit, checkpoint revision/hash and
the necessary364x644 camera-coordinate adapter are fixed in the protocol.
Both arms use identical RGB preprocessing and original360x640 public geometry
for final projection. The adapter converts integer pixel centres to upstream's
half-pixel convention; supplied rays agree within1.788e-7. The model's returned
camera matrix remains its learned estimate even when known rays are supplied.
It is audited separately and does not replace the public projection matrix.

All352 state entries,34184323 parameters, load strictly and equal the checkpoint.
Only unused training-loss construction is omitted; no learned branch changes.
Parameters and saved maps are float32; actual CUDA inference uses upstream
float16 autocast. Synthetic known/estimated/known replay is bit-identical, with
unchanged weight hashes. CPU/GPU synthetic median times are1.023s/0.0265s;
CUDA is selected from measured speed. Their numerical difference is disclosed:
maximum0.0122m, mean0.00698m, failing a1mm equivalence comparison. This does not
change the frozen all-CUDA comparison and is not claimed as arithmetic parity.

Public depth points in the existing body corridor must form an8-connected patch
of at least9pixels. Missing current IMU disables the added branch. OR retains
independent ToF/Radar evidence, including outside RGB; no return is clipped,
replaced or vetoed. Native/evaluator data are used only after sealing384 maps
and all public alert flags. The full source specification is decoded to select
TRAIN. Original dev/test raw records, evaluator records and RGB are not decoded.
The inherited sealed baseline cache's480 TRAIN flags are read, selecting the
original192; this is consumed Development, with no protected-test validation.

One run completes in142.12s within1200s. Cumulative CUDA prediction time is6.682s
known and5.416s estimated; peak allocated memory389148672bytes. Models are
released after prediction; no new UE capture, training or persistent worker.
Upstream CC BY-NC4.0 source/weights remain in ignored local research artifacts.

## Validation and disposition

Six focused synthetic tests pass: camera/preprocessing/output parity, input
validation, fixed connected-corridor readout, missing pose, true depth agreement
instead of mask overlap, and absent/MERGED return stratification. Independent
saved-output audit passes774 hash comparisons across613 unique paths and
independently reconstructs projection/components for all384 maps (88473600
pixels), matching the recorded flags, counts and public camera transforms.
It recomputes frame/family/stratum/pair/event/native/gate arithmetic, reduces
16537 saved native records and checks31924 saved-UV model-depth samples.

The auditor initially reports an incomplete manifest because Windows backslashes
were compared with forward slashes. All192 map-file hashes already matched.
Only that audit comparison is normalized; the initial failure and one-line
correction receipt are preserved. Scientific code and outputs remain unchanged.
No inference, RGB decode or original native-evaluator parse is repeated. Actual
supplied ray tensors and dense reference masks were not saved: their authenticated
error scalars/source implementation and saved mask arithmetic are checked, rather
than independently regenerating those tensors/masks.

Supported registration and inheritance assignment remain pending the pre-existing
`experiments/index.jsonl:303` input-fingerprint mismatch. Registration and then
unknown-terminal inheritance exit2; both ledger files are hash-unchanged. The
intended scoped role is NEGATIVE_CONTROL, recorded with exact commands/logs in
`decision.json`; no manual append, inherited-role fabrication or ledger bypass.

Retain the camera adapter and measured limited no-return agreement as research
evidence. A distinct target/background disambiguation mechanism or justified
source-domain comparison could change a future decision; this failed frozen
readout receives no rescue. MZ129 stays retained, with MZ158's separate partial
gain unchanged. Nothing here establishes natural-scene, hardware, default-App
or safety benefit.

Reproducible outputs, model bindings, plot, logs, checks and resource receipts:
`artifacts.local/work/mz164-camera-metric-prior-20260916/`. Scientific outputs
are in `run-v1/`; the independent saved-output script and audit are in `review/`.
