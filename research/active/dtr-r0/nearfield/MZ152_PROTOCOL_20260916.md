# MZ152: independent contained-return anchors plus learned ambiguous evidence

EXPLORE Development,2026-09-16. MZ151 improves matched OOF discrimination but
its low-confidence boundary/head positives force permissive global cutoffs.
Test a division of responsibility instead of another model/data/threshold sweep.
Use the frozen MZ151 model and saved OOF scores unchanged; no refit or new labels.

An additive ToF branch alerts when at least one usable, unmerged public return
has its entire unchanged MZ143 three-sigma full-zone envelope inside the fixed
body corridor x[.2,3.6],y[-.3,.3],z[.4,2.05], with valid public IMU. Keep all128
slots and all original sensor inputs. Do not use RGB visibility, foreground,
native contributors, identities or geometry as a condition. This branch narrows
no support and vetoes no independent evidence. Its absent output is UNKNOWN,
not clear space; modeled three-sigma containment is not a hardware guarantee.

TRAIN480 is the same consumed membership as MZ151. First check the fixed anchor
on this TRAIN only: require zero anchor false frames and at least one incumbent
true frame covered. Failure stops before dev. Calibrate the existing low/high
rule on the unchanged OOF scores, requiring learned retention only for incumbent
TRAIN true frames not already covered by the additive anchor. No weight/cutoff
search or offset tuning. Seal these parameters and source/model hashes before
dev anchor/prediction construction. Final decision is anchor OR learned alert.

Consumed dev48 gate:24TP, all incumbent events with no delayed first alert,
FP<=3, false segments<=2, each family's FP no greater than MZ129, and no
non-alert frame with a returned native ToF corridor contributor. Include paired
ordering, pressure strata, UNKNOWN, removed/new errors and independent branch
coverage/false frames. Native Radar per-return lineage remains NOT_EVALUABLE.
If the gate fails, close this exact two-branch recipe without tuning or capture.
If it passes, it is only a Development challenger needing a separately frozen
fresh comparison; MZ146 is training evidence, not a validation panel.

One scalar/cached-inference run,120s cap; CPU sklearn/NumPy implementation.
Record actual backend, keep all artifacts under
`artifacts.local/work/mz152-contained-return-20260916/`. No original MZ136 test
records, new source capture, App promotion or safety claim in this experiment.
