# MZ122: position-bound local fusion before pooling

One bounded paired Development experiment following the user's continuation.
MZ116 stays the mainline baseline; historical MZ120/MZ121 files stay immutable.
Question: does retaining RGB positions until after local ToF fusion improve
body-volume occupancy precision/recall while retaining the nuisance benefit?
MZ121 established a threshold explanation for some misses, not spatial fitting.

Two fresh fits: unchanged MZ120 early-pooling model and LocalFusionNet. Shared
RGB encoder, ToF/Radar encoders, query and final45-cell head start with identical
weights. The candidate adds a2→24position encoder and a50→24local-fusion layer
(1,296parameters, about11.5% above the11,305baseline). It retains49RGB samples,
adds measured projection positions, attends separately to the spatially relevant
ToF slots at each sample, fuses locally, then performs the original mean/max
pooling. Radar remains the original cell-level attention branch. This tests a
local position/fusion package, not a proof separating operation order from the
small extra capacity. No pretrained encoder, recurrent state, CNH, extra sensor,
new label, suppressor or changed corridor.

Use original MZ120 MZ115/MZ117336training/144development frame IDs and45-cell
labels. Native bounds/actor roles are training/evaluator-only. Position masks
derive solely from current RGB intrinsics, grid and ToF angular-zone bounds;
retain the original8px neighborhood pad and null token. Both arms keep encoded
invalid slots with explicit validity/missingness flags, rather than adding a new
hard validity mask only to the candidate. Missingness tokens are not measured
obstacle support. This matched handling was fixed before either training run.
Both arms have the same
800steps, batch8, AdamW .001/.0001, global class-weighted BCE and brightness/
contrast distributions. Freeze one shared sample schedule and one explicit
augmentation array, seed120013, before fitting. These shared augmentations are
a newly frozen pairing, not claimed byte-identical to MZ120's original RNG run.
Train each arm once to its final checkpoint, GPU-first,20minute cap per fit.

Evaluate all336training and144development frames with101thresholds. Both arms
use MZ121's same seven-criterion joint selector against MZ116; no fallback point
if infeasible. Training metrics never select the development operating point.
Primary useful effect: candidate is jointly feasible and has greater whole-grid
IoU on both training and development at the respective development-selected
points, with no increase in dev false-alert frames versus the paired early arm.
Also report HEAD/rod recall and wrong cells/precision; report whole-grid and
HEAD precision envelopes at recall targets.8,.9,.95 across each complete curve,
so extra illuminated cells cannot alone be called better spatial learning.
These are controlled Development comparisons, no universal/safety promotion.

If this joint effect fails, stop this fixed candidate and finish evidence/
delivery. If the early arm has no jointly selected operating point, mark the
paired selected-point comparison NOT_EVALUABLE rather than invent a fallback.
Do not tune, enlarge data or automatically launch another model. If
it succeeds, freeze both selected points before a single consumed MZ119 transfer
diagnostic; never select points from MZ119. No fresh capture in this bounded run.
Transfer improvement is not confirmation on unrelated scenes. Model outputs
remain AABB volume occupancy rather than physical identity of every echo.

Integrity checks: shared parameter identity; paired schedule/augmentation hashes;
split/label/source hashes; point-to-zone masking; permutation of intact
position-feature-return tuples preserves set pooling, while swapping features
alone must be able to change the late-fused output. Null remains always available.
Record actual backend, costs and final receipts; preserve checkpoint/evidence
and release foreground CUDA/process resources on exit. Structured registration
may remain pending the existing ledger303 fingerprint error; do not repair it
or claim acceptance as part of this experiment.
