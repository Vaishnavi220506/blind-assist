# MZ147: preserve joint pixel/range/body-query evidence

EXPLORE Development, 2026-09-16. One representation hypothesis, one fixed learner
and one sensor-only ablation. MZ146 is closed with aggregate gain and rod-family
regression; no MZ143/MZ145 model, cutoff, capture or old confirmation is altered.

MZ143 uses frame-level elementwise quantiles of competing proposal/range
hypotheses. Such pooling can retain a near range, a central edge and strong
contrast even when they belong to different regions. MZ147 instead samples
native pixels inside each public ToF zone at each valid regional front-plane
hypothesis. Preserve left/corridor/right pixel contrast, column persistence,
edge strength and foreground occupancy together per target slot. Range is a
regional hypothesis, not point ownership. Merged or outside-RGB returns keep
their raw features; absent image evidence is not clear space or a native veto.

Use the unchanged MZ143 raw sensor vector plus3,275 query features. Sensor-only
HGB is an ablation. The declared candidate is query HGB, with exactly MZ143's
HGB parameters (150 iterations, learning rate .05, seven leaves, minimum leaf8,
L2=1, no early stop, seed143016), no sweep or model selection. TRAIN192 from
MZ136 only, same four whole-scene OOF folds. Low cutoff is the minimum OOF score
on incumbent TRAIN true frames. Fit MZ145's high onset cutoff from those OOF
scores and TRAIN labels, preserving immediate required true onsets. Refit each
model once on TRAIN192, then seal before Development predictions. No IDs,
family, source geometry, pair member, truth or future frame enters inference.

First score the old consumed dev48. Keep the representation as an alert
candidate only if it retains every MZ129 true frame/event without extra delay,
has at most9 FP (at least30% fewer), no family FP increase, and no more than
four false segments. Report comparison to MZ145's24/3/0 as well. If this fails,
stop the recipe; do not tune, automatically try another learner or score MZ146
to find a rescuing result. Sensor-only is explanatory and cannot replace the
declared candidate after seeing outcomes.

Only if the initial gate passes, score the already-consumed MZ146288 as an
explicit additional Development check. Require at least143 TP, no lost MZ129
TP/event, no delayed MZ129 event, fewer than57 FP, at most21 false segments and
no per-family FP increase against MZ129. Keep all frame/family/event, pressure
strata, pairs, UNKNOWN, native ToF contributor and Radar audit limits visible.
Any gain here remains Development; unchanged fresh confirmation is separate.

One run, maximum600s; no new capture in this experiment, no original MZ136 test
records. Reuse verified sensor features only after hashes/order checks, seal new
features and code. CPU sklearn/OpenCV implementation: GPU_BACKEND_UNAVAILABLE;
record versions and measured times. Retain failures, snapshots and all results
under `artifacts.local/work/mz147-query-representation-20260916/`. Mechanical
errors may be repaired with logs; feature/learner/gate changes require a new
hypothesis. CPU process exits; no persistent allocations.
